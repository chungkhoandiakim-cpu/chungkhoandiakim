import React, { useState, useEffect, useCallback, useMemo } from "react";
import {
  LayoutDashboard, Eye, Target, BookOpen, Brain, Plus, Trash2,
  TrendingUp, TrendingDown, Upload, X, ChevronLeft, ChevronRight,
  Flame, ShieldCheck, AlertTriangle, ImageOff, Pencil, Download,
  CalendarDays, Check, ChevronDown, ChevronUp, Phone, MessageCircle,
  Send, Mail, Share2, Copy, Youtube, Music2, Compass, Layers, Gauge, Sparkles,
  Activity, Bell, BellRing, FileSpreadsheet, Printer, Users, UserPlus, Clock, Filter, BarChart3
} from "lucide-react";
import * as XLSX from "xlsx";
import {
  AreaChart, Area, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Cell,
} from "recharts";

export const LOGO_URI = "data:image/png;base64,";


/* ---------------------------------------------------------
   STORAGE HELPERS
--------------------------------------------------------- */
const K = {
  watchlist: "ts:watchlist",
  plan: "ts:plan",
  journal: "ts:journal",
  journalGold: "ts:journal-gold",
  journalForex: "ts:journal-forex",
  psych: "ts:psychology",
  market: "ts:market-journal",
  stockLogs: "ts:stock-logs",
  profile: "ts:profile",
  screener: "ts:screener",
  goldCapital: "ts:gold-capital",
  crm: "ts:crm-customers",
};

async function loadKey(key, fallback) {
  try {
    const res = await window.storage.get(key, false);
    if (res && res.value) return JSON.parse(res.value);
    return fallback;
  } catch (e) {
    return fallback;
  }
}
async function saveKey(key, value) {
  try {
    await window.storage.set(key, JSON.stringify(value), false);
  } catch (e) {
    console.error("storage save failed", key, e);
  }
}

function compressImage(file, maxW = 520, quality = 0.72) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => {
      const img = new Image();
      img.onload = () => {
        const scale = Math.min(1, maxW / img.width);
        const canvas = document.createElement("canvas");
        canvas.width = img.width * scale;
        canvas.height = img.height * scale;
        const ctx = canvas.getContext("2d");
        ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
        resolve(canvas.toDataURL("image/jpeg", quality));
      };
      img.onerror = reject;
      img.src = reader.result;
    };
    reader.onerror = reject;
    reader.readAsDataURL(file);
  });
}

function fmt(n) {
  if (n === undefined || n === null || isNaN(n)) return "—";
  return Number(n).toLocaleString("vi-VN");
}
function fmtBillion(n) {
  if (n === undefined || n === null || n === "" || isNaN(n)) return "—";
  const v = Number(n) / 1e9;
  return `${v.toLocaleString("vi-VN", { maximumFractionDigits: 2 })} tỷ`;
}
function fmtVol(n) {
  if (n === undefined || n === null || n === "" || isNaN(n)) return "—";
  const v = Number(n);
  const abs = Math.abs(v);
  if (abs >= 1e6) return `${(v / 1e6).toLocaleString("vi-VN", { maximumFractionDigits: 2 })}M`;
  if (abs >= 1e3) return `${(v / 1e3).toLocaleString("vi-VN", { maximumFractionDigits: 2 })}K`;
  return v.toLocaleString("vi-VN");
}
function todayISO() {
  return new Date().toISOString().slice(0, 10);
}

function downloadFile(filename, content, mime) {
  const blob = new Blob([content], { type: mime });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}

function toCSV(rows, columns) {
  const esc = (v) => {
    const s = v === undefined || v === null ? "" : String(v).replace(/"/g, '""');
    return /[",\n]/.test(s) ? `"${s}"` : s;
  };
  const header = columns.map((c) => c.label).join(",");
  const lines = rows.map((r) => columns.map((c) => esc(r[c.key])).join(","));
  return "\uFEFF" + [header, ...lines].join("\n");
}

/* ---------------------------------------------------------
   SHELL
--------------------------------------------------------- */
const NAV = [
  { id: "dashboard", label: "Tổng quan", icon: LayoutDashboard },
  { id: "watchlist", label: "Danh mục theo dõi", icon: Eye },
  { id: "performance", label: "Hiệu suất giao dịch", icon: Activity },
  { id: "plan", label: "Kế hoạch đầu tư", icon: Target },
  { id: "journal", label: "Nhật ký giao dịch", icon: BookOpen },
  { id: "market", label: "Nhật ký thị trường & Tâm lý", icon: CalendarDays },
  { id: "crm", label: "CRM Khách hàng", icon: Users },
];

export default function TradingSystem({ onLogout, userEmail, isAdmin, onOpenAdmin }) {
  const [tab, setTab] = useState("dashboard");
  const [loading, setLoading] = useState(true);

  const [watchlist, setWatchlist] = useState([]);
  const [plan, setPlan] = useState({ goal: "", capital: "", allocations: [], rules: [] });
  const [journal, setJournal] = useState([]);
  const [psych, setPsych] = useState({});
  const [market, setMarket] = useState([]);
  const [stockLogs, setStockLogs] = useState({});
  const [profile, setProfile] = useState({ name: "", title: "Môi giới chứng khoán", phone: "", zalo: "", facebook: "", telegram: "", email: "", youtube: "", tiktok: "" });
  const [screener, setScreener] = useState([]);
  const [journalGold, setJournalGold] = useState([]);
  const [journalForex, setJournalForex] = useState([]);
  const [goldCapital, setGoldCapital] = useState({ initialCapital: "", riskPerTrade: "1", maxDailyLoss: "3", maxConsecutiveLoss: "3" });
  const [crm, setCrm] = useState([]);

  useEffect(() => {
    (async () => {
      const [w, p, j, ps, mk, sl, pf, sc, jg, gc, jf, cr] = await Promise.all([
        loadKey(K.watchlist, []),
        loadKey(K.plan, { goal: "", capital: "", allocations: [], rules: [] }),
        loadKey(K.journal, []),
        loadKey(K.psych, {}),
        loadKey(K.market, []),
        loadKey(K.stockLogs, {}),
        loadKey(K.profile, { name: "", title: "Môi giới chứng khoán", phone: "", zalo: "", facebook: "", telegram: "", email: "", youtube: "", tiktok: "" }),
        loadKey(K.screener, []),
        loadKey(K.journalGold, []),
        loadKey(K.goldCapital, { initialCapital: "", riskPerTrade: "1", maxDailyLoss: "3", maxConsecutiveLoss: "3" }),
        loadKey(K.journalForex, []),
        loadKey(K.crm, []),
      ]);
      let mergedMarket = mk;
      if (ps && Object.keys(ps).length > 0) {
        const byDate = {};
        mergedMarket.forEach((m) => { byDate[m.date] = m; });
        Object.entries(ps).forEach(([date, pdata]) => {
          if (byDate[date]) {
            if (byDate[date].mood === undefined) byDate[date].mood = pdata.mood;
            if (byDate[date].discipline === undefined) byDate[date].discipline = pdata.discipline;
            if (!byDate[date].note) byDate[date].note = pdata.note || "";
            if (!byDate[date].image) byDate[date].image = pdata.image || null;
          } else {
            const entry = { id: Date.now() + Math.random(), date, vnindex: "", change: "", volume: "", foreignNet: "", breadth: "", mood: pdata.mood, discipline: pdata.discipline, note: pdata.note || "", image: pdata.image || null };
            mergedMarket = [...mergedMarket, entry];
            byDate[date] = entry;
          }
        });
        saveKey(K.market, mergedMarket);
        saveKey(K.psych, {});
      }
      let mergedGoldJournal = jg;
      if (jf && jf.length > 0) {
        mergedGoldJournal = [...jg, ...jf.map((t) => ({ ...t, symbolLabel: t.symbolLabel || t.symbol }))];
        saveKey(K.journalGold, mergedGoldJournal);
        saveKey(K.journalForex, []);
      }
      setWatchlist(w); setPlan(p); setJournal(j); setPsych({}); setMarket(mergedMarket); setStockLogs(sl); setProfile(pf); setScreener(sc); setJournalGold(mergedGoldJournal); setGoldCapital(gc); setJournalForex([]); setCrm(cr);
      setLoading(false);
    })();
  }, []);

  const persist = useCallback((key, setter) => (updater) => {
    setter((prev) => {
      const next = typeof updater === "function" ? updater(prev) : updater;
      saveKey(key, next);
      return next;
    });
  }, []);

  const setWatchlistP = persist(K.watchlist, setWatchlist);
  const setPlanP = persist(K.plan, setPlan);
  const setJournalP = persist(K.journal, setJournal);
  const setPsychP = persist(K.psych, setPsych);
  const setMarketP = persist(K.market, setMarket);
  const setStockLogsP = persist(K.stockLogs, setStockLogs);
  const setProfileP = persist(K.profile, setProfile);
  const setScreenerP = persist(K.screener, setScreener);
  const setJournalGoldP = persist(K.journalGold, setJournalGold);
  const setGoldCapitalP = persist(K.goldCapital, setGoldCapital);
  const setCrmP = persist(K.crm, setCrm);
  const setJournalForexP = persist(K.journalForex, setJournalForex);

  const stats = useMemo(() => {
    const closed = journal.filter((t) => t.pl !== "" && t.pl !== undefined && t.pl !== null && !isNaN(t.pl));
    const wins = closed.filter((t) => Number(t.pl) > 0).length;
    const winRate = closed.length ? Math.round((wins / closed.length) * 100) : 0;
    const totalPL = closed.reduce((s, t) => s + Number(t.pl || 0), 0);
    const withDisc = market
      .filter((m) => m.discipline !== undefined && m.discipline !== "")
      .sort((a, b) => (a.date < b.date ? 1 : -1))
      .slice(0, 14);
    const avgDisc = withDisc.length
      ? Math.round((withDisc.reduce((s, m) => s + Number(m.discipline || 0), 0) / withDisc.length) * 10) / 10
      : 0;
    return { closedCount: closed.length, winRate, totalPL, avgDisc, watchCount: watchlist.length };
  }, [journal, market, watchlist]);

  if (loading) {
    return (
      <div style={{ background: "#0a0e14" }} className="min-h-screen flex flex-col items-center justify-center gap-3 text-slate-500 font-mono text-sm">
        <img src={LOGO_URI} alt="Hoàng Địa Kim" className="w-14 h-14 rounded-full animate-pulse" />
        <span className="tracking-widest text-xs" style={{ background: "linear-gradient(90deg, #fde68a, #fbbf24)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>HOÀNG ĐỊA KIM</span>
        Đang tải hệ thống...
      </div>
    );
  }

  return (
    <div style={{ background: "#0a0e14", fontFamily: "'Inter', sans-serif" }} className="min-h-screen text-slate-200">
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500;600&display=swap');
        .font-display { font-family: 'Space Grotesk', sans-serif; }
        .font-data { font-family: 'JetBrains Mono', monospace; }
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-thumb { background: #263042; border-radius: 4px; }
        ::-webkit-scrollbar-track { background: transparent; }
      `}</style>

      <div className="flex">
        {/* SIDEBAR */}
        <aside style={{ background: "#0d1119", borderRight: "1px solid #1c2432" }} className="w-64 min-h-screen flex-shrink-0 hidden md:flex flex-col">
          <div
            className="px-5 py-5 flex items-center gap-3"
            style={{
              background: "linear-gradient(135deg, #1d1033 0%, #3a1550 45%, #0d1119 100%)",
              borderBottom: "1px solid #2a1840",
            }}
          >
            <img src={LOGO_URI} alt="Hoàng Địa Kim" className="w-11 h-11 rounded-full flex-shrink-0" style={{ boxShadow: "0 0 14px rgba(251,191,36,0.35)" }} />
            <div className="min-w-0">
              <div
                className="font-display font-bold text-base tracking-tight leading-tight"
                style={{
                  background: "linear-gradient(90deg, #fde68a, #fbbf24, #f59e0b)",
                  WebkitBackgroundClip: "text",
                  WebkitTextFillColor: "transparent",
                }}
              >
                HOÀNG ĐỊA KIM
              </div>
              <div className="text-[10px] text-slate-400 font-data mt-0.5 tracking-widest truncate">HỆ THỐNG ĐẦU TƯ CÁ NHÂN</div>
            </div>
          </div>
          <nav className="flex-1 py-4 px-3 space-y-1">
            {NAV.map((n) => {
              const Icon = n.icon;
              const active = tab === n.id;
              return (
                <button
                  key={n.id}
                  onClick={() => setTab(n.id)}
                  className="w-full flex items-center gap-3 px-3 py-2.5 rounded-md text-sm transition-colors"
                  style={{
                    background: active ? "linear-gradient(90deg, rgba(124,58,237,0.18), rgba(251,191,36,0.06))" : "transparent",
                    color: active ? "#fbbf24" : "#94a3b8",
                    borderLeft: active ? "2px solid #a855f7" : "2px solid transparent",
                  }}
                >
                  <Icon size={16} />
                  <span className="font-medium">{n.label}</span>
                </button>
              );
            })}
          </nav>
          <div className="px-6 py-4 text-[11px] text-slate-600 font-data" style={{ borderTop: "1px solid #1c2432" }}>
            Win rate: <span className="text-slate-400">{stats.winRate}%</span> · Kỷ luật: <span className="text-slate-400">{stats.avgDisc}/5</span>
          </div>
          {isAdmin && onOpenAdmin && (
            <div className="px-3 pb-2">
              <button
                onClick={onOpenAdmin}
                className="w-full flex items-center gap-2 px-3 py-2 rounded-md text-xs font-medium"
                style={{ background: "rgba(251,191,36,0.08)", color: "#fbbf24", border: "1px solid rgba(251,191,36,0.2)" }}
              >
                <ShieldCheck size={13} /> Quản lý người dùng
              </button>
            </div>
          )}
          {onLogout && (
            <div className="px-6 pb-4 flex items-center justify-between" style={{ borderTop: "1px solid #1c2432", paddingTop: 12 }}>
              <span className="text-[11px] text-slate-500 truncate">{userEmail}</span>
              <button onClick={onLogout} className="text-[11px] text-slate-500 hover:text-red-400 flex-shrink-0 ml-2">Đăng xuất</button>
            </div>
          )}
        </aside>

        {/* MOBILE TABS */}
        <div
          className="md:hidden fixed top-0 left-0 right-0 z-30 flex items-center gap-2 px-4 py-2.5"
          style={{ background: "linear-gradient(135deg, #1d1033 0%, #3a1550 45%, #0d1119 100%)", borderBottom: "1px solid #2a1840" }}
        >
          <img src={LOGO_URI} alt="Hoàng Địa Kim" className="w-7 h-7 rounded-full" />
          <span
            className="font-display font-bold text-sm tracking-tight"
            style={{ background: "linear-gradient(90deg, #fde68a, #fbbf24)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}
          >
            HOÀNG ĐỊA KIM
          </span>
        </div>
        <div className="md:hidden fixed bottom-0 left-0 right-0 z-30 flex" style={{ background: "#0d1119", borderTop: "1px solid #1c2432" }}>
          {NAV.map((n) => {
            const Icon = n.icon;
            const active = tab === n.id;
            return (
              <button key={n.id} onClick={() => setTab(n.id)} className="flex-1 flex flex-col items-center gap-1 py-2.5" style={{ color: active ? "#fbbf24" : "#64748b" }}>
                <Icon size={17} />
                <span className="text-[9px] font-medium">{n.label.split(" ")[0]}</span>
              </button>
            );
          })}
        </div>

        {/* MAIN */}
        <main className="flex-1 min-w-0 pb-20 md:pb-0 pt-14 md:pt-0">
          {tab === "dashboard" && <Dashboard stats={stats} journal={journal} journalGold={journalGold} market={market} watchlist={watchlist} profile={profile} setProfile={setProfileP} screener={screener} />}
          {tab === "watchlist" && <Watchlist watchlist={watchlist} setWatchlist={setWatchlistP} stockLogs={stockLogs} setStockLogs={setStockLogsP} />}
          {tab === "performance" && <PerformanceStats journal={journal} journalGold={journalGold} />}
          {tab === "plan" && <PlanView plan={plan} setPlan={setPlanP} />}
          {tab === "journal" && <Journal journal={journal} setJournal={setJournalP} journalGold={journalGold} setJournalGold={setJournalGoldP} goldCapital={goldCapital} setGoldCapital={setGoldCapitalP} />}
          {tab === "market" && <MarketJournal market={market} setMarket={setMarketP} />}
          {tab === "crm" && <CRM crm={crm} setCrm={setCrmP} />}
        </main>
      </div>
    </div>
  );
}

/* ---------------------------------------------------------
   SHARED UI
--------------------------------------------------------- */
function PageHeader({ title, sub }) {
  return (
    <div className="px-6 md:px-10 pt-8 pb-6">
      <div className="flex items-center gap-2.5 mb-1">
        <span className="inline-block w-1 h-5 rounded-full" style={{ background: "linear-gradient(180deg, #a855f7, #fbbf24)" }} />
        <h1 className="font-display font-bold text-2xl text-slate-100">{title}</h1>
      </div>
      {sub && <p className="text-sm text-slate-500 ml-3.5">{sub}</p>}
    </div>
  );
}
function Card({ children, className = "", style = {} }) {
  return (
    <div style={{ background: "#0d1119", border: "1px solid #1c2432", ...style }} className={`rounded-lg ${className}`}>
      {children}
    </div>
  );
}
function StatCard({ label, value, valueColor, icon: Icon, hint }) {
  return (
    <Card className="p-4">
      <div className="flex items-center justify-between mb-2">
        <span className="text-xs text-slate-500 font-medium uppercase tracking-wide">{label}</span>
        {Icon && <Icon size={14} className="text-slate-600" />}
      </div>
      <div className="font-data text-2xl font-semibold" style={{ color: valueColor || "#e2e8f0" }}>{value}</div>
      {hint && <div className="text-[11px] text-slate-600 mt-1">{hint}</div>}
    </Card>
  );
}
function Field({ label, children }) {
  return (
    <label className="block">
      <span className="text-xs text-slate-500 mb-1.5 block font-medium">{label}</span>
      {children}
    </label>
  );
}
const inputStyle = {
  background: "#0a0e14",
  border: "1px solid #263042",
  color: "#e2e8f0",
};
function Input(props) {
  return <input {...props} style={{ ...inputStyle, ...props.style }} className={`w-full px-3 py-2 rounded-md text-sm outline-none focus:border-amber-500/60 ${props.className || ""}`} />;
}
function Textarea(props) {
  return <textarea {...props} style={{ ...inputStyle, ...props.style }} className={`w-full px-3 py-2 rounded-md text-sm outline-none focus:border-amber-500/60 resize-none ${props.className || ""}`} />;
}
function Btn({ children, onClick, variant = "primary", className = "", type = "button" }) {
  const styles = {
    primary: { background: "linear-gradient(90deg, #f59e0b, #fbbf24)", color: "#1a1206", fontWeight: 600 },
    ghost: { background: "transparent", color: "#94a3b8", border: "1px solid #263042" },
    danger: { background: "transparent", color: "#f87171", border: "1px solid #3f1f22" },
  };
  return (
    <button type={type} onClick={onClick} style={styles[variant]} className={`px-3.5 py-2 rounded-md text-sm font-medium inline-flex items-center gap-1.5 transition-opacity hover:opacity-85 ${className}`}>
      {children}
    </button>
  );
}

/* ---------------------------------------------------------
   DASHBOARD
--------------------------------------------------------- */
function Dashboard({ stats, journal, journalGold, market, watchlist, profile, setProfile, screener }) {
  const recent = [...journal].sort((a, b) => (a.date < b.date ? 1 : -1)).slice(0, 5);
  const activeAlerts = [];
  watchlist.forEach((w) => {
    (w.alerts || []).forEach((a) => {
      const triggered = w.price && a.price && (a.condition === "trên" ? Number(w.price) >= Number(a.price) : Number(w.price) <= Number(a.price));
      if (triggered) activeAlerts.push({ ticker: w.ticker, condition: a.condition, price: a.price, current: w.price });
    });
  });

  function exportExcel() {
    const wb = XLSX.utils.book_new();
    const wl = watchlist.map((w) => ({ Ma: w.ticker, Nganh: w.sector, GiaHienTai: w.price, GiaMucTieu: w.target, CatLo: w.cut, GhiChu: w.note }));
    const jStock = journal.map((j) => ({ Ma: j.ticker, Ngay: j.date, Chieu: j.side, Gia: j.price, KhoiLuong: j.qty, LaiLo: j.pl, CamXuc: j.emotion, LyDo: j.reason }));
    const jGold = journalGold.map((j) => ({ CapMa: j.symbolLabel, Ngay: j.date, Chieu: j.direction, KhungGio: j.timeframe, Song: j.wave, Entry: j.entry, SL: j.sl, TP: j.tp, KhoiLuong: j.lot, LaiLo: j.pl, CamXuc: j.emotion }));
    const mk = market.map((m) => ({ Ngay: m.date, VNIndex: m.vnindex, ThayDoi: m.change, KhoiLuong: m.volume, KhoiNgoaiRong: m.foreignNet, DoRong: m.breadth, TamTrang: m.mood, KyLuat: m.discipline, GhiChu: m.note }));
    const sc = (screener || []).map((s) => ({ Ma: s.symbol, ThiTruong: s.market, Ngay: s.date, Chieu: s.direction, DiemOB: s.obScore, KetLuan: s.verdictLabel }));
    if (wl.length) XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(wl), "Danh muc theo doi");
    if (jStock.length) XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(jStock), "NKGD Chung khoan");
    if (jGold.length) XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(jGold), "NKGD Vang Dau BTC");
    if (mk.length) XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(mk), "Nhat ky thi truong");
    if (sc.length) XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(sc), "Bo loc SMC");
    if (wb.SheetNames.length === 0) XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet([{ ThongBao: "Chua co du lieu" }]), "Trong");
    XLSX.writeFile(wb, `hoang-dia-kim-du-lieu-${todayISO()}.xlsx`);
  }

  function printReport() {
    const stockStats = computeGroupStats(journal);
    const goldStats = computeGroupStats(journalGold);
    const win = window.open("", "_blank");
    if (!win) return;
    win.document.write(`
      <html><head><title>Bao cao Hoang Dia Kim ${todayISO()}</title>
      <style>
        body{font-family:Arial,sans-serif;padding:24px;color:#111}
        h1{font-size:20px;margin-bottom:4px}
        h2{font-size:15px;margin-top:24px;border-bottom:1px solid #ccc;padding-bottom:4px}
        table{width:100%;border-collapse:collapse;margin-top:8px;font-size:12px}
        th,td{border:1px solid #ddd;padding:6px 8px;text-align:left}
        th{background:#f3f3f3}
        .stat{display:inline-block;margin-right:24px;margin-bottom:8px}
        .stat b{font-size:16px;display:block}
      </style></head><body>
      <h1>Báo cáo hệ thống đầu tư — Hoàng Địa Kim</h1>
      <div>Ngày xuất: ${todayISO()}</div>
      <h2>Chứng khoán</h2>
      <div class="stat">Số lệnh đã chốt<b>${stockStats.closedCount}</b></div>
      <div class="stat">Tỷ lệ thắng<b>${stockStats.winRate}%</b></div>
      <div class="stat">Tổng lãi/lỗ<b>${fmtBillion(stockStats.totalPL)}</b></div>
      <h2>Vàng · Dầu · BTC</h2>
      <div class="stat">Số lệnh đã chốt<b>${goldStats.closedCount}</b></div>
      <div class="stat">Tỷ lệ thắng<b>${goldStats.winRate}%</b></div>
      <div class="stat">Tổng lãi/lỗ<b>${fmt(goldStats.totalPL)}</b></div>
      <h2>Danh mục theo dõi (${watchlist.length} mã)</h2>
      <table><tr><th>Mã</th><th>Ngành</th><th>Giá hiện tại</th><th>Mục tiêu</th><th>Cắt lỗ</th></tr>
      ${watchlist.map((w) => `<tr><td>${w.ticker}</td><td>${w.sector || ""}</td><td>${fmt(w.price)}</td><td>${fmt(w.target)}</td><td>${fmt(w.cut)}</td></tr>`).join("")}
      </table>
      <h2>Giao dịch chứng khoán gần đây</h2>
      <table><tr><th>Mã</th><th>Ngày</th><th>Chiều</th><th>Giá</th><th>KL</th><th>Lãi/Lỗ</th></tr>
      ${journal.slice(0, 30).map((j) => `<tr><td>${j.ticker}</td><td>${j.date}</td><td>${j.side}</td><td>${fmt(j.price)}</td><td>${fmt(j.qty)}</td><td>${fmt(j.pl)}</td></tr>`).join("")}
      </table>
      </body></html>
    `);
    win.document.close();
    setTimeout(() => win.print(), 400);
  }

  return (
    <div>
      <div className="flex items-start justify-between px-6 md:px-10 pt-8 pb-6">
        <div>
          <div className="flex items-center gap-2.5 mb-1">
            <span className="inline-block w-1 h-5 rounded-full" style={{ background: "linear-gradient(180deg, #a855f7, #fbbf24)" }} />
            <h1 className="font-display font-bold text-2xl text-slate-100">Tổng quan</h1>
          </div>
          <p className="text-sm text-slate-500 ml-3.5">Bức tranh toàn cảnh về danh mục, kỷ luật và hiệu suất giao dịch của bạn.</p>
        </div>
        <div className="flex gap-2 flex-shrink-0">
          <Btn variant="ghost" onClick={exportExcel}><FileSpreadsheet size={14} /> Xuất Excel</Btn>
          <Btn variant="ghost" onClick={printReport}><Printer size={14} /> In / Xuất PDF</Btn>
        </div>
      </div>
      <div className="px-6 md:px-10 mb-8">
        <ContactCard profile={profile} setProfile={setProfile} />
      </div>

      {activeAlerts.length > 0 && (
        <div className="px-6 md:px-10 mb-8">
          <Card className="p-4" style={{ background: "rgba(251,191,36,0.06)", border: "1px solid rgba(251,191,36,0.3)" }}>
            <div className="text-sm font-semibold text-amber-300 mb-2 flex items-center gap-2"><BellRing size={15} /> Cảnh báo giá đang kích hoạt ({activeAlerts.length})</div>
            <div className="flex flex-wrap gap-2">
              {activeAlerts.map((a, i) => (
                <span key={i} className="text-xs px-2.5 py-1.5 rounded-full font-data" style={{ background: "#0a0e14", color: "#fbbf24" }}>
                  {a.ticker}: giá {a.condition} {fmt(a.price)} (hiện {fmt(a.current)})
                </span>
              ))}
            </div>
          </Card>
        </div>
      )}

      <div className="px-6 md:px-10 grid grid-cols-2 lg:grid-cols-4 gap-3 mb-8">
        <StatCard label="Lãi/Lỗ đã chốt" value={`${stats.totalPL >= 0 ? "+" : ""}${fmtBillion(stats.totalPL)}`} valueColor={stats.totalPL >= 0 ? "#34d399" : "#f87171"} icon={stats.totalPL >= 0 ? TrendingUp : TrendingDown} hint={`${stats.closedCount} lệnh đã đóng`} />
        <StatCard label="Tỷ lệ thắng" value={`${stats.winRate}%`} icon={ShieldCheck} hint="Trên các lệnh đã chốt" />
        <StatCard label="Điểm kỷ luật (14N)" value={`${stats.avgDisc}/5`} icon={Brain} valueColor={stats.avgDisc >= 3.5 ? "#34d399" : stats.avgDisc >= 2.5 ? "#fbbf24" : "#f87171"} />
        <StatCard label="Mã đang theo dõi" value={stats.watchCount} icon={Eye} />
      </div>

      <div className="px-6 md:px-10 grid grid-cols-1 lg:grid-cols-5 gap-4 mb-8">
        <Card className="p-5 lg:col-span-3">
          <div className="text-sm font-semibold text-slate-300 mb-4 flex items-center gap-2"><Flame size={15} className="text-amber-400" /> Bản đồ nhiệt thị trường & tâm lý (90 ngày)</div>
          <MiniHeatmap market={market} />
        </Card>
        <Card className="p-5 lg:col-span-2">
          <div className="text-sm font-semibold text-slate-300 mb-4">Giao dịch gần đây</div>
          {recent.length === 0 ? (
            <EmptyHint text="Chưa có giao dịch nào được ghi nhận." />
          ) : (
            <div className="space-y-2">
              {recent.map((t) => (
                <div key={t.id} className="flex items-center justify-between text-sm py-1.5" style={{ borderBottom: "1px solid #161d2a" }}>
                  <div>
                    <span className="font-data font-semibold text-slate-200">{t.ticker}</span>
                    <span className="text-slate-600 ml-2 text-xs">{t.date}</span>
                  </div>
                  <span className="font-data" style={{ color: t.side === "Mua" ? "#34d399" : "#f87171" }}>{t.side}</span>
                </div>
              ))}
            </div>
          )}
        </Card>
      </div>
    </div>
  );
}

function ContactCard({ profile, setProfile }) {
  const [editing, setEditing] = useState(false);
  const [draft, setDraft] = useState(profile);
  const [copied, setCopied] = useState(false);

  function startEdit() {
    setDraft(profile);
    setEditing(true);
  }
  function save() {
    setProfile(draft);
    setEditing(false);
  }
  function cancel() {
    setDraft(profile);
    setEditing(false);
  }

  const hasAnyInfo = profile.name || profile.phone || profile.zalo || profile.facebook || profile.telegram || profile.email || profile.youtube || profile.tiktok;

  function buildShareText() {
    const lines = [];
    if (profile.name) lines.push(profile.name + (profile.title ? ` — ${profile.title}` : ""));
    if (profile.phone) lines.push(`Điện thoại: ${profile.phone}`);
    if (profile.zalo) lines.push(`Zalo: ${profile.zalo}`);
    if (profile.facebook) lines.push(`Facebook: ${profile.facebook}`);
    if (profile.telegram) lines.push(`Telegram: ${profile.telegram}`);
    if (profile.youtube) lines.push(`YouTube: ${profile.youtube}`);
    if (profile.tiktok) lines.push(`TikTok: ${profile.tiktok}`);
    if (profile.email) lines.push(`Email: ${profile.email}`);
    return lines.join("\n");
  }
  async function copyShare() {
    try {
      await navigator.clipboard.writeText(buildShareText());
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch (e) {
      // clipboard may be unavailable; ignore silently
    }
  }

  if (editing) {
    return (
      <Card
        className="p-5"
        style={{ background: "linear-gradient(135deg, #1d1033 0%, #3a1550 55%, #0d1119 100%)", border: "1px solid #3a2050" }}
      >
        <div className="text-sm font-semibold text-amber-300 mb-4 flex items-center gap-2"><Share2 size={15} /> Giới thiệu</div>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
          <Input placeholder="Họ tên / Tên hiển thị" value={draft.name} onChange={(e) => setDraft({ ...draft, name: e.target.value })} />
          <Input placeholder="Chức danh (VD: Môi giới chứng khoán)" value={draft.title} onChange={(e) => setDraft({ ...draft, title: e.target.value })} />
          <Input placeholder="Số điện thoại" value={draft.phone} onChange={(e) => setDraft({ ...draft, phone: e.target.value })} />
          <Input placeholder="Zalo (số hoặc link)" value={draft.zalo} onChange={(e) => setDraft({ ...draft, zalo: e.target.value })} />
          <Input placeholder="Facebook (link)" value={draft.facebook} onChange={(e) => setDraft({ ...draft, facebook: e.target.value })} />
          <Input placeholder="Telegram (username hoặc link)" value={draft.telegram} onChange={(e) => setDraft({ ...draft, telegram: e.target.value })} />
          <Input placeholder="YouTube (link kênh)" value={draft.youtube} onChange={(e) => setDraft({ ...draft, youtube: e.target.value })} />
          <Input placeholder="TikTok (link hoặc @username)" value={draft.tiktok} onChange={(e) => setDraft({ ...draft, tiktok: e.target.value })} />
          <Input placeholder="Email" value={draft.email} onChange={(e) => setDraft({ ...draft, email: e.target.value })} />
        </div>
        <div className="flex justify-end gap-2 mt-4">
          <Btn variant="ghost" onClick={cancel}>Hủy</Btn>
          <Btn onClick={save}><Check size={15} /> Lưu</Btn>
        </div>
      </Card>
    );
  }

  return (
    <Card
      className="p-5"
      style={{ background: "linear-gradient(135deg, #1d1033 0%, #3a1550 55%, #0d1119 100%)", border: "1px solid #3a2050" }}
    >
      <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">
        <div className="flex items-center gap-4">
          <img src={LOGO_URI} alt="Hoàng Địa Kim" className="w-14 h-14 rounded-full flex-shrink-0" style={{ boxShadow: "0 0 14px rgba(251,191,36,0.35)" }} />
          <div>
            <div className="text-[11px] text-amber-300/80 uppercase tracking-wide font-medium mb-0.5">Giới thiệu</div>
            {profile.name ? (
              <>
                <div className="font-display font-bold text-lg text-slate-100">{profile.name}</div>
                <div className="text-xs text-slate-400">{profile.title}</div>
              </>
            ) : (
              <div className="text-sm text-slate-400">Thêm phần giới thiệu của bạn để hiển thị ở trang chủ</div>
            )}
          </div>
        </div>
        <div className="flex items-center gap-2 flex-shrink-0">
          {hasAnyInfo && (
            <Btn variant="ghost" onClick={copyShare}>
              {copied ? <><Check size={14} /> Đã sao chép</> : <><Copy size={14} /> Sao chép để chia sẻ</>}
            </Btn>
          )}
          <button onClick={startEdit} className="text-slate-400 hover:text-amber-400 p-2"><Pencil size={15} /></button>
        </div>
      </div>
      {hasAnyInfo && (
        <div className="flex flex-wrap gap-x-6 gap-y-2 mt-4 pt-4" style={{ borderTop: "1px solid #2a1840" }}>
          {profile.phone && (
            <a href={`tel:${profile.phone.replace(/\s/g, "")}`} className="flex items-center gap-2 text-sm text-slate-300 hover:text-amber-400">
              <Phone size={14} /> {profile.phone}
            </a>
          )}
          {profile.zalo && (
            <span className="flex items-center gap-2 text-sm text-slate-300">
              <MessageCircle size={14} /> Zalo: {profile.zalo}
            </span>
          )}
          {profile.facebook && (
            <a href={profile.facebook.startsWith("http") ? profile.facebook : `https://${profile.facebook}`} target="_blank" rel="noopener noreferrer" className="flex items-center gap-2 text-sm text-slate-300 hover:text-amber-400">
              <Share2 size={14} /> Facebook
            </a>
          )}
          {profile.telegram && (
            <span className="flex items-center gap-2 text-sm text-slate-300">
              <Send size={14} /> {profile.telegram}
            </span>
          )}
          {profile.youtube && (
            <a href={profile.youtube.startsWith("http") ? profile.youtube : `https://${profile.youtube}`} target="_blank" rel="noopener noreferrer" className="flex items-center gap-2 text-sm text-slate-300 hover:text-amber-400">
              <Youtube size={14} /> YouTube
            </a>
          )}
          {profile.tiktok && (
            <a href={profile.tiktok.startsWith("http") ? profile.tiktok : `https://www.tiktok.com/@${profile.tiktok.replace(/^@/, "")}`} target="_blank" rel="noopener noreferrer" className="flex items-center gap-2 text-sm text-slate-300 hover:text-amber-400">
              <Music2 size={14} /> TikTok
            </a>
          )}
          {profile.email && (
            <a href={`mailto:${profile.email}`} className="flex items-center gap-2 text-sm text-slate-300 hover:text-amber-400">
              <Mail size={14} /> {profile.email}
            </a>
          )}
        </div>
      )}
    </Card>
  );
}

function EmptyHint({ text }) {
  return <div className="text-sm text-slate-600 py-6 text-center">{text}</div>;
}

function ImageLightbox({ image, caption, onClose }) {
  if (!image) return null;
  return (
    <div
      className="fixed inset-0 z-[100] flex items-center justify-center p-4 md:p-10"
      style={{ background: "rgba(5,7,10,0.94)" }}
      onClick={onClose}
    >
      <button
        onClick={onClose}
        className="absolute top-4 right-4 md:top-6 md:right-6 text-slate-300 hover:text-white p-2.5 rounded-full z-10"
        style={{ background: "rgba(255,255,255,0.08)" }}
      >
        <X size={20} />
      </button>
      <div className="max-w-5xl w-full flex flex-col items-center" onClick={(e) => e.stopPropagation()}>
        <img src={image} alt="Xem đầy đủ" className="max-h-[78vh] w-auto rounded-lg border border-slate-700 object-contain" style={{ boxShadow: "0 20px 60px rgba(0,0,0,0.6)" }} />
        {caption && (
          <div className="mt-4 px-4 py-2.5 rounded-lg text-sm text-slate-100 text-center max-w-2xl" style={{ background: "#0d1119", border: "1px solid #1c2432" }}>
            {caption}
          </div>
        )}
      </div>
    </div>
  );
}

function ZoomableThumb({ src, alt, className, onClick }) {
  return (
    <div className={`relative group cursor-pointer ${className || ""}`} onClick={onClick}>
      <img src={src} alt={alt} className="w-full h-full object-cover rounded border border-slate-700" />
      <div className="absolute inset-0 rounded flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity" style={{ background: "rgba(10,14,20,0.55)" }}>
        <Sparkles size={14} className="text-amber-300" />
      </div>
    </div>
  );
}

/* ---------------------------------------------------------
   WATCHLIST
--------------------------------------------------------- */
const EMOTIONS = [
  { label: "Tự tin", color: "#34d399" },
  { label: "Bình tĩnh", color: "#38bdf8" },
  { label: "Lo lắng", color: "#fbbf24" },
  { label: "Sợ hãi", color: "#f87171" },
  { label: "Tham lam", color: "#a855f7" },
  { label: "FOMO", color: "#f472b6" },
];
function emotionColor(label) {
  const e = EMOTIONS.find((x) => x.label === label);
  return e ? e.color : "#475569";
}
function EmotionDot(props) {
  const { cx, cy, payload } = props;
  if (cx === undefined || cy === undefined) return null;
  return <circle cx={cx} cy={cy} r={4} fill={emotionColor(payload.emotion)} stroke="#0a0e14" strokeWidth={1.5} />;
}
function PsychPriceTooltip({ active, payload }) {
  if (!active || !payload || !payload.length) return null;
  const d = payload[0].payload;
  return (
    <div style={{ background: "#0d1119", border: "1px solid #263042", borderRadius: 6, padding: "8px 10px", fontSize: 12, maxWidth: 200 }}>
      <div style={{ color: "#94a3b8" }}>{d.fullDate}</div>
      <div className="font-data font-semibold" style={{ color: "#fbbf24" }}>{fmt(d.price)}</div>
      <div className="flex items-center gap-1.5 mt-1" style={{ color: emotionColor(d.emotion) }}>
        <span className="w-2 h-2 rounded-full" style={{ background: emotionColor(d.emotion) }} /> {d.emotion}
      </div>
      {d.note && <div style={{ color: "#64748b", fontStyle: "italic", marginTop: 4 }}>{d.note}</div>}
    </div>
  );
}

function Watchlist({ watchlist, setWatchlist, stockLogs, setStockLogs }) {
  const empty = { ticker: "", sector: "", price: "", target: "", cut: "", note: "", image: null };
  const [form, setForm] = useState(empty);
  const [editingId, setEditingId] = useState(null);
  const [busy, setBusy] = useState(false);
  const [showForm, setShowForm] = useState(false);
  const [expandedId, setExpandedId] = useState(null);
  const [lightbox, setLightbox] = useState(null);

  async function handleImage(e) {
    const file = e.target.files[0];
    if (!file) return;
    setBusy(true);
    try {
      const dataUrl = await compressImage(file, 400, 0.7);
      setForm((f) => ({ ...f, image: dataUrl }));
    } finally {
      setBusy(false);
    }
  }

  function save() {
    if (!form.ticker.trim()) return;
    if (editingId) {
      setWatchlist((prev) => prev.map((w) => (w.id === editingId ? { ...form, id: editingId, ticker: form.ticker.toUpperCase() } : w)));
    } else {
      setWatchlist((prev) => [{ id: Date.now(), ...form, ticker: form.ticker.toUpperCase() }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(w) {
    setEditingId(w.id);
    setForm({ ticker: w.ticker, sector: w.sector, price: w.price, target: w.target, cut: w.cut, note: w.note, image: w.image || null });
    setShowForm(true);
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(empty);
    setShowForm(false);
  }
  function remove(id) {
    setWatchlist((prev) => prev.filter((w) => w.id !== id));
    setStockLogs((prev) => {
      const next = { ...prev };
      delete next[id];
      return next;
    });
    if (editingId === id) cancelEdit();
    if (expandedId === id) setExpandedId(null);
  }
  function toggleExpand(id) {
    setExpandedId((prev) => (prev === id ? null : id));
  }

  return (
    <div>
      <PageHeader title="Danh mục theo dõi" sub="Theo dõi mã tiềm năng và ghi nhật ký biến động giá hằng ngày — xu hướng, khối lượng và tâm lý theo từng mã." />
      <div className="px-6 md:px-10">
        <div className="flex justify-end mb-4">
          <Btn onClick={() => (showForm ? cancelEdit() : (setShowForm(true), setEditingId(null), setForm(empty)))}>
            {showForm ? <><X size={15} /> Đóng</> : <><Plus size={15} /> Thêm mã mới</>}
          </Btn>
        </div>

        {showForm && (
          <Card className="p-4 mb-6">
            {editingId && (
              <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
                <Pencil size={12} /> Đang chỉnh sửa {form.ticker || "mã"}
              </div>
            )}
            <div className="grid grid-cols-2 md:grid-cols-6 gap-3">
              <Input placeholder="Mã CK" value={form.ticker} onChange={(e) => setForm({ ...form, ticker: e.target.value })} />
              <Input placeholder="Ngành" value={form.sector} onChange={(e) => setForm({ ...form, sector: e.target.value })} />
              <Input placeholder="Giá hiện tại" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} />
              <Input placeholder="Giá mục tiêu" value={form.target} onChange={(e) => setForm({ ...form, target: e.target.value })} />
              <Input placeholder="Cắt lỗ" value={form.cut} onChange={(e) => setForm({ ...form, cut: e.target.value })} />
              <Btn onClick={save}>{editingId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Thêm mã</>}</Btn>
            </div>
            <div className="flex flex-col md:flex-row gap-3 mt-3">
              <Input className="flex-1" placeholder="Ghi chú luận điểm đầu tư..." value={form.note} onChange={(e) => setForm({ ...form, note: e.target.value })} />
              <label className="flex items-center justify-center gap-1.5 text-xs rounded-md cursor-pointer px-3 py-2 whitespace-nowrap" style={inputStyle}>
                <Upload size={13} /> {busy ? "Đang tải..." : form.image ? "Đã chọn ảnh" : "Tải hình ảnh"}
                <input type="file" accept="image/*" onChange={handleImage} className="hidden" />
              </label>
              {form.image && (
                <div className="flex items-center gap-2">
                  <img src={form.image} alt="preview" className="h-9 w-9 object-cover rounded border border-slate-700" />
                  <button onClick={() => setForm({ ...form, image: null })} className="text-slate-600 hover:text-red-400"><X size={14} /></button>
                </div>
              )}
            </div>
          </Card>
        )}

        {watchlist.length === 0 ? (
          <Card className="p-10"><EmptyHint text="Chưa có mã nào trong danh mục theo dõi. Thêm mã đầu tiên ở trên." /></Card>
        ) : (
          <div className="space-y-3">
            {watchlist.map((w) => {
              const upside = w.price && w.target ? (((w.target - w.price) / w.price) * 100).toFixed(1) : null;
              const logs = (stockLogs[w.id] || []).slice().sort((a, b) => (a.date < b.date ? -1 : 1));
              const lastLog = logs[logs.length - 1];
              const isOpen = expandedId === w.id;
              return (
                <Card key={w.id} className="overflow-hidden">
                  <div
                    className="p-4 flex flex-col md:flex-row md:items-center gap-3 md:gap-6 cursor-pointer"
                    onClick={() => toggleExpand(w.id)}
                  >
                    {w.image ? (
                      <ZoomableThumb
                        src={w.image}
                        alt={w.ticker}
                        className="w-14 h-14 flex-shrink-0"
                        onClick={(e) => { e.stopPropagation(); setLightbox({ image: w.image, caption: `${w.ticker} · ${w.sector || "—"}${w.note ? " · " + w.note : ""}` }); }}
                      />
                    ) : (
                      <div className="w-14 h-14 rounded border border-dashed flex items-center justify-center flex-shrink-0" style={{ borderColor: "#263042" }}>
                        <ImageOff size={14} className="text-slate-700" />
                      </div>
                    )}
                    <div className="w-20 flex-shrink-0">
                      <div className="font-data font-bold text-amber-400 text-lg flex items-center gap-1.5">
                        {w.ticker}
                        {(w.alerts || []).some((a) => (a.condition === "trên" ? Number(w.price) >= Number(a.price) : Number(w.price) <= Number(a.price))) && (
                          <BellRing size={13} className="text-amber-400" />
                        )}
                      </div>
                      <div className="text-[11px] text-slate-600">{w.sector || "—"}</div>
                    </div>
                    <div className="flex gap-6 text-sm flex-1 flex-wrap">
                      <div><div className="text-[11px] text-slate-600">Giá hiện tại</div><div className="font-data">{fmt(w.price)}</div></div>
                      <div><div className="text-[11px] text-slate-600">Mục tiêu</div><div className="font-data text-emerald-400">{fmt(w.target)}</div></div>
                      <div><div className="text-[11px] text-slate-600">Cắt lỗ</div><div className="font-data text-red-400">{fmt(w.cut)}</div></div>
                      {upside && <div><div className="text-[11px] text-slate-600">Tiềm năng</div><div className="font-data" style={{ color: upside >= 0 ? "#34d399" : "#f87171" }}>{upside}%</div></div>}
                      {lastLog && (
                        <div>
                          <div className="text-[11px] text-slate-600">Nhật ký gần nhất</div>
                          <div className="font-data text-slate-400 flex items-center gap-1.5">
                            {lastLog.date}
                            <span className="w-2 h-2 rounded-sm" style={{ background: emotionColor(lastLog.emotion) }} />
                          </div>
                        </div>
                      )}
                    </div>
                    <div className="text-xs text-slate-500 flex-1 italic hidden lg:block">{w.note}</div>
                    <div className="flex items-center gap-3 self-start md:self-center" onClick={(e) => e.stopPropagation()}>
                      <button onClick={() => startEdit(w)} className="text-slate-600 hover:text-amber-400"><Pencil size={15} /></button>
                      <button onClick={() => remove(w.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={15} /></button>
                      <button onClick={() => toggleExpand(w.id)} className="text-slate-500 hover:text-purple-400">
                        {isOpen ? <ChevronUp size={17} /> : <ChevronDown size={17} />}
                      </button>
                    </div>
                  </div>
                  {isOpen && (
                    <div style={{ borderTop: "1px solid #1c2432" }}>
                      <StockDetailPanel stock={w} logs={logs} setStockLogs={setStockLogs} onUpdateStock={(patch) => setWatchlist((prev) => prev.map((x) => (x.id === w.id ? { ...x, ...patch } : x)))} />
                    </div>
                  )}
                </Card>
              );
            })}
          </div>
        )}
      </div>
      <ImageLightbox image={lightbox?.image} caption={lightbox?.caption} onClose={() => setLightbox(null)} />
    </div>
  );
}

function StockDetailPanel({ stock, logs, setStockLogs, onUpdateStock }) {
  const emptyLog = { date: todayISO(), price: "", volume: "", emotion: EMOTIONS[0].label, note: "" };
  const [form, setForm] = useState(emptyLog);
  const [editingLogId, setEditingLogId] = useState(null);
  const [alertForm, setAlertForm] = useState({ price: "", condition: "trên" });

  const alerts = stock.alerts || [];
  function addAlert() {
    if (!alertForm.price) return;
    const next = [...alerts, { id: Date.now(), price: alertForm.price, condition: alertForm.condition }];
    onUpdateStock({ alerts: next });
    setAlertForm({ price: "", condition: "trên" });
  }
  function removeAlert(id) {
    onUpdateStock({ alerts: alerts.filter((a) => a.id !== id) });
  }
  function isAlertTriggered(a) {
    if (!stock.price || isNaN(Number(stock.price)) || isNaN(Number(a.price))) return false;
    return a.condition === "trên" ? Number(stock.price) >= Number(a.price) : Number(stock.price) <= Number(a.price);
  }

  const chartData = logs.map((l) => ({
    id: l.id,
    date: l.date.slice(5),
    fullDate: l.date,
    price: Number(l.price) || 0,
    volume: Number(l.volume) || 0,
    emotion: l.emotion,
    note: l.note,
  }));
  const descLogs = [...logs].sort((a, b) => (a.date < b.date ? 1 : -1));

  function upsertLog() {
    if (!form.date || form.price === "") return;
    setStockLogs((prev) => {
      const arr = prev[stock.id] ? [...prev[stock.id]] : [];
      if (editingLogId) {
        const idx = arr.findIndex((x) => x.id === editingLogId);
        if (idx > -1) arr[idx] = { ...form, id: editingLogId };
      } else {
        const idx = arr.findIndex((x) => x.date === form.date);
        if (idx > -1) arr[idx] = { ...form, id: arr[idx].id };
        else arr.push({ ...form, id: Date.now() });
      }
      return { ...prev, [stock.id]: arr };
    });
    cancelEditLog();
  }
  function startEditLog(l) {
    setEditingLogId(l.id);
    setForm({ date: l.date, price: l.price, volume: l.volume, emotion: l.emotion, note: l.note });
  }
  function cancelEditLog() {
    setEditingLogId(null);
    setForm(emptyLog);
  }
  function removeLog(id) {
    setStockLogs((prev) => ({ ...prev, [stock.id]: (prev[stock.id] || []).filter((x) => x.id !== id) }));
    if (editingLogId === id) cancelEditLog();
  }

  return (
    <div className="p-5" style={{ background: "#0a0e14" }}>
      {chartData.length === 0 ? (
        <div className="mb-5"><EmptyHint text="Chưa có nhật ký biến động cho mã này. Ghi nhận ngày đầu tiên bên dưới để bắt đầu vẽ biểu đồ." /></div>
      ) : (
        <>
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-3">
            <div>
              <div className="text-xs text-slate-500 mb-2 font-medium">Xu hướng giá kết hợp tâm lý</div>
              <ResponsiveContainer width="100%" height={180}>
                <AreaChart data={chartData}>
                  <defs>
                    <linearGradient id={`price-${stock.id}`} x1="0" y1="0" x2="0" y2="1">
                      <stop offset="0%" stopColor="#fbbf24" stopOpacity={0.35} />
                      <stop offset="100%" stopColor="#fbbf24" stopOpacity={0} />
                    </linearGradient>
                  </defs>
                  <CartesianGrid strokeDasharray="3 3" stroke="#1c2432" vertical={false} />
                  <XAxis dataKey="date" stroke="#475569" tick={{ fontSize: 10 }} />
                  <YAxis stroke="#475569" tick={{ fontSize: 10 }} domain={["auto", "auto"]} width={44} />
                  <Tooltip content={<PsychPriceTooltip />} />
                  <Area
                    type="monotone"
                    dataKey="price"
                    stroke="#fbbf24"
                    fill={`url(#price-${stock.id})`}
                    strokeWidth={2}
                    dot={<EmotionDot />}
                    activeDot={{ r: 6, stroke: "#0a0e14", strokeWidth: 2 }}
                  />
                </AreaChart>
              </ResponsiveContainer>
              <div className="flex flex-wrap gap-3 mt-2">
                {EMOTIONS.map((e) => (
                  <div key={e.label} className="flex items-center gap-1.5 text-[11px] text-slate-500">
                    <span className="w-2.5 h-2.5 rounded-full" style={{ background: e.color }} /> {e.label}
                  </div>
                ))}
              </div>
            </div>
            <div>
              <div className="text-xs text-slate-500 mb-2 font-medium">Khối lượng giao dịch</div>
              <ResponsiveContainer width="100%" height={180}>
                <BarChart data={chartData}>
                  <CartesianGrid strokeDasharray="3 3" stroke="#1c2432" vertical={false} />
                  <XAxis dataKey="date" stroke="#475569" tick={{ fontSize: 10 }} />
                  <YAxis stroke="#475569" tick={{ fontSize: 10 }} width={44} tickFormatter={(v) => fmtVol(v)} />
                  <Tooltip
                    contentStyle={{ background: "#0d1119", border: "1px solid #263042", fontSize: 12, borderRadius: 6 }}
                    labelStyle={{ color: "#94a3b8" }}
                    formatter={(v) => [fmtVol(v), "Khối lượng"]}
                  />
                  <Bar dataKey="volume" fill="#a855f7" radius={[3, 3, 0, 0]} />
                </BarChart>
              </ResponsiveContainer>
            </div>
          </div>
        </>
      )}

      <Card className="p-4 mb-4" style={{ background: "#0d1119" }}>
        <div className="text-xs text-slate-500 mb-2 font-medium flex items-center gap-1.5"><Bell size={13} /> Cảnh báo giá (so với giá hiện tại: {fmt(stock.price)})</div>
        {alerts.length > 0 && (
          <div className="space-y-1.5 mb-3">
            {alerts.map((a) => {
              const triggered = isAlertTriggered(a);
              return (
                <div key={a.id} className="flex items-center gap-2 text-sm px-3 py-2 rounded-md" style={{ background: triggered ? "#fbbf2414" : "#0a0e14", border: triggered ? "1px solid #fbbf2440" : "1px solid transparent" }}>
                  {triggered ? <BellRing size={14} className="text-amber-400 flex-shrink-0" /> : <Bell size={14} className="text-slate-600 flex-shrink-0" />}
                  <span className="text-slate-300">Giá {a.condition} <span className="font-data font-semibold">{fmt(a.price)}</span></span>
                  {triggered && <span className="text-[11px] text-amber-400 font-medium">Đã đạt ngưỡng</span>}
                  <button onClick={() => removeAlert(a.id)} className="ml-auto text-slate-600 hover:text-red-400"><X size={13} /></button>
                </div>
              );
            })}
          </div>
        )}
        <div className="flex gap-2">
          <select value={alertForm.condition} onChange={(e) => setAlertForm({ ...alertForm, condition: e.target.value })} style={inputStyle} className="px-2 py-2 rounded-md text-sm outline-none">
            <option value="trên">Khi giá lên trên</option>
            <option value="dưới">Khi giá xuống dưới</option>
          </select>
          <Input placeholder="Mức giá" value={alertForm.price} onChange={(e) => setAlertForm({ ...alertForm, price: e.target.value })} />
          <Btn variant="ghost" onClick={addAlert}><Plus size={14} /> Thêm</Btn>
        </div>
      </Card>

      <Card className="p-4 mb-4" style={{ background: "#0d1119" }}>
        {editingLogId && (
          <div className="text-xs text-amber-400 mb-2 flex items-center gap-2">
            <Pencil size={12} /> Đang sửa nhật ký ngày {form.date} — <button onClick={cancelEditLog} className="underline text-slate-500">hủy</button>
          </div>
        )}
        <div className="grid grid-cols-2 md:grid-cols-5 gap-3">
          <Input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} />
          <Input placeholder="Giá đóng cửa" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} />
          <Input placeholder="Khối lượng (cp)" value={form.volume} onChange={(e) => setForm({ ...form, volume: e.target.value })} />
          <select value={form.emotion} onChange={(e) => setForm({ ...form, emotion: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {EMOTIONS.map((e) => <option key={e.label}>{e.label}</option>)}
          </select>
          <Btn onClick={upsertLog}>{editingLogId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Ghi nhận</>}</Btn>
        </div>
        <Input className="mt-3" placeholder="Ghi chú diễn biến trong ngày..." value={form.note} onChange={(e) => setForm({ ...form, note: e.target.value })} />
      </Card>

      {descLogs.length > 0 && (
        <div className="space-y-1.5">
          {descLogs.map((l) => (
            <div key={l.id} className="flex flex-wrap items-center gap-3 text-sm px-3 py-2 rounded-md" style={{ background: "#0d1119" }}>
              <span className="w-2 h-2 rounded-sm flex-shrink-0" style={{ background: emotionColor(l.emotion) }} />
              <span className="font-data text-slate-500 w-24">{l.date}</span>
              <span className="font-data font-medium w-20">{fmt(l.price)}</span>
              <span className="text-xs text-slate-600 w-28">KL {fmtVol(l.volume)}</span>
              <span className="text-xs text-slate-500 flex-1 truncate italic min-w-[80px]">{l.note}</span>
              <button onClick={() => startEditLog(l)} className="text-slate-600 hover:text-amber-400"><Pencil size={13} /></button>
              <button onClick={() => removeLog(l.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={13} /></button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}

/* ---------------------------------------------------------
   PLAN
--------------------------------------------------------- */
function PlanView({ plan, setPlan }) {
  const [newAlloc, setNewAlloc] = useState({ sector: "", percent: "" });
  const [newRule, setNewRule] = useState("");

  function addAlloc() {
    if (!newAlloc.sector.trim()) return;
    setPlan((p) => ({ ...p, allocations: [...(p.allocations || []), newAlloc] }));
    setNewAlloc({ sector: "", percent: "" });
  }
  function removeAlloc(i) {
    setPlan((p) => ({ ...p, allocations: p.allocations.filter((_, idx) => idx !== i) }));
  }
  function addRule() {
    if (!newRule.trim()) return;
    setPlan((p) => ({ ...p, rules: [...(p.rules || []), newRule] }));
    setNewRule("");
  }
  function removeRule(i) {
    setPlan((p) => ({ ...p, rules: p.rules.filter((_, idx) => idx !== i) }));
  }
  const totalPct = (plan.allocations || []).reduce((s, a) => s + Number(a.percent || 0), 0);

  return (
    <div>
      <PageHeader title="Kế hoạch đầu tư" sub="Định hình mục tiêu, phân bổ vốn theo ngành và nguyên tắc giao dịch của riêng bạn." />
      <div className="px-6 md:px-10 grid grid-cols-1 lg:grid-cols-2 gap-5">
        <Card className="p-5">
          <div className="text-sm font-semibold text-slate-300 mb-4">Mục tiêu & vốn</div>
          <div className="space-y-3">
            <Field label="Mục tiêu đầu tư">
              <Textarea rows={3} value={plan.goal} onChange={(e) => setPlan({ ...plan, goal: e.target.value })} placeholder="VD: Tăng trưởng vốn 20%/năm, ưu tiên bảo toàn vốn..." />
            </Field>
            <Field label="Tổng vốn phân bổ">
              <Input value={plan.capital} onChange={(e) => setPlan({ ...plan, capital: e.target.value })} placeholder="VD: 1,000,000,000" />
            </Field>
          </div>
        </Card>

        <Card className="p-5">
          <div className="flex items-center justify-between mb-4">
            <div className="text-sm font-semibold text-slate-300">Phân bổ theo ngành</div>
            <span className="text-xs font-data" style={{ color: totalPct === 100 ? "#34d399" : "#fbbf24" }}>{totalPct}%</span>
          </div>
          <div className="space-y-2 mb-3">
            {(plan.allocations || []).map((a, i) => (
              <div key={i} className="flex items-center gap-3">
                <span className="text-xs w-24 truncate text-slate-400">{a.sector}</span>
                <div className="flex-1 h-2 rounded-full" style={{ background: "#161d2a" }}>
                  <div className="h-2 rounded-full bg-amber-400" style={{ width: `${a.percent}%` }} />
                </div>
                <span className="text-xs font-data w-10 text-right">{a.percent}%</span>
                <button onClick={() => removeAlloc(i)} className="text-slate-600 hover:text-red-400"><X size={13} /></button>
              </div>
            ))}
            {(!plan.allocations || plan.allocations.length === 0) && <EmptyHint text="Chưa thiết lập phân bổ." />}
          </div>
          <div className="flex gap-2">
            <Input placeholder="Ngành" value={newAlloc.sector} onChange={(e) => setNewAlloc({ ...newAlloc, sector: e.target.value })} />
            <Input placeholder="%" style={{ width: 70 }} value={newAlloc.percent} onChange={(e) => setNewAlloc({ ...newAlloc, percent: e.target.value })} />
            <Btn variant="ghost" onClick={addAlloc}><Plus size={14} /></Btn>
          </div>
        </Card>

        <Card className="p-5 lg:col-span-2">
          <div className="text-sm font-semibold text-slate-300 mb-4">Nguyên tắc giao dịch (kỷ luật vào/ra lệnh)</div>
          <div className="space-y-2 mb-3">
            {(plan.rules || []).map((r, i) => (
              <div key={i} className="flex items-start gap-2 text-sm py-1.5" style={{ borderBottom: "1px solid #161d2a" }}>
                <ShieldCheck size={14} className="text-amber-400 mt-0.5 flex-shrink-0" />
                <span className="flex-1 text-slate-300">{r}</span>
                <button onClick={() => removeRule(i)} className="text-slate-600 hover:text-red-400"><X size={13} /></button>
              </div>
            ))}
            {(!plan.rules || plan.rules.length === 0) && <EmptyHint text="Chưa có nguyên tắc nào. Hãy thêm quy tắc kỷ luật của bạn." />}
          </div>
          <div className="flex gap-2">
            <Input placeholder="VD: Không mua đuổi quá 5% từ điểm phá vỡ" value={newRule} onChange={(e) => setNewRule(e.target.value)} />
            <Btn variant="ghost" onClick={addRule}><Plus size={14} /></Btn>
          </div>
        </Card>
      </div>
    </div>
  );
}

/* ---------------------------------------------------------
   JOURNAL
--------------------------------------------------------- */
function Journal({ journal, setJournal, journalGold, setJournalGold, goldCapital, setGoldCapital }) {
  const [subTab, setSubTab] = useState("stock"); // "stock" | "gold"
  return (
    <div>
      <PageHeader title="Nhật ký giao dịch" sub="Ghi lại từng lệnh — lý do vào lệnh, cảm xúc và ảnh chụp biểu đồ." />
      <div className="px-6 md:px-10">
        <div className="flex gap-2 mb-6 flex-wrap">
          <button
            onClick={() => setSubTab("stock")}
            className="px-4 py-2 rounded-md text-sm font-medium flex items-center gap-2"
            style={subTab === "stock" ? { background: "linear-gradient(90deg,#7c3aed,#a855f7)", color: "#fff" } : { background: "#0d1119", color: "#94a3b8", border: "1px solid #1c2432" }}
          >
            <BookOpen size={15} /> Chứng khoán
          </button>
          <button
            onClick={() => setSubTab("gold")}
            className="px-4 py-2 rounded-md text-sm font-medium flex items-center gap-2"
            style={subTab === "gold" ? { background: "linear-gradient(90deg,#7c3aed,#a855f7)", color: "#fff" } : { background: "#0d1119", color: "#94a3b8", border: "1px solid #1c2432" }}
          >
            <Compass size={15} /> Vàng · Dầu · BTC · Forex (SMC/ICT)
          </button>
        </div>
        {subTab === "stock" && <StockJournal journal={journal} setJournal={setJournal} />}
        {subTab === "gold" && <GoldOilBtcJournal journal={journalGold} setJournal={setJournalGold} capital={goldCapital} setCapital={setGoldCapital} />}
      </div>
    </div>
  );
}

function StockJournal({ journal, setJournal }) {
  const empty = { ticker: "", date: todayISO(), side: "Mua", price: "", qty: "", reason: "", emotion: "Bình tĩnh", pl: "", image: null };
  const [form, setForm] = useState(empty);
  const [editingId, setEditingId] = useState(null);
  const [busy, setBusy] = useState(false);
  const [lightbox, setLightbox] = useState(null);

  async function handleImage(e) {
    const file = e.target.files[0];
    if (!file) return;
    setBusy(true);
    try {
      const dataUrl = await compressImage(file);
      setForm((f) => ({ ...f, image: dataUrl }));
    } finally {
      setBusy(false);
    }
  }
  function save() {
    if (!form.ticker.trim()) return;
    if (editingId) {
      setJournal((prev) => prev.map((j) => (j.id === editingId ? { ...form, id: editingId, ticker: form.ticker.toUpperCase() } : j)));
    } else {
      setJournal((prev) => [{ id: Date.now(), ...form, ticker: form.ticker.toUpperCase() }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(j) {
    setEditingId(j.id);
    setForm({ ticker: j.ticker, date: j.date, side: j.side, price: j.price, qty: j.qty, reason: j.reason, emotion: j.emotion, pl: j.pl, image: j.image || null });
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(empty);
  }
  function remove(id) {
    setJournal((prev) => prev.filter((j) => j.id !== id));
    if (editingId === id) cancelEdit();
  }

  const emotions = ["Bình tĩnh", "Tự tin", "Sợ hãi", "Tham lam", "FOMO", "Do dự"];

  return (
    <div>
      <Card className="p-4 mb-6">
        {editingId && (
          <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
            <Pencil size={12} /> Đang chỉnh sửa {form.ticker || "lệnh"} — <button onClick={cancelEdit} className="underline text-slate-500">hủy</button>
          </div>
        )}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-3">
          <Input placeholder="Mã CK" value={form.ticker} onChange={(e) => setForm({ ...form, ticker: e.target.value })} />
          <Input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} />
          <select value={form.side} onChange={(e) => setForm({ ...form, side: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            <option>Mua</option><option>Bán</option>
          </select>
          <select value={form.emotion} onChange={(e) => setForm({ ...form, emotion: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {emotions.map((e) => <option key={e}>{e}</option>)}
          </select>
          <Input placeholder="Giá" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} />
          <Input placeholder="Khối lượng (cp)" value={form.qty} onChange={(e) => setForm({ ...form, qty: e.target.value })} />
          <Input placeholder="Lãi/Lỗ (VNĐ, nếu đã chốt)" value={form.pl} onChange={(e) => setForm({ ...form, pl: e.target.value })} />
          <label className="flex items-center justify-center gap-1.5 text-xs rounded-md cursor-pointer" style={{ ...inputStyle, padding: "8px" }}>
            <Upload size={13} /> {busy ? "Đang tải..." : form.image ? "Đã chọn ảnh" : "Tải ảnh biểu đồ"}
            <input type="file" accept="image/*" onChange={handleImage} className="hidden" />
          </label>
        </div>
        <Textarea rows={2} placeholder="Lý do vào lệnh / luận điểm..." value={form.reason} onChange={(e) => setForm({ ...form, reason: e.target.value })} />
        <div className="flex items-center justify-between mt-3">
          {form.image ? (
            <div className="flex items-center gap-2">
              <img src={form.image} alt="preview" className="h-12 rounded border border-slate-700" />
              <button onClick={() => setForm({ ...form, image: null })} className="text-xs text-slate-500 hover:text-red-400">Xóa ảnh</button>
            </div>
          ) : <span />}
          <Btn onClick={save}>{editingId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Ghi giao dịch</>}</Btn>
        </div>
      </Card>

      {journal.length === 0 ? (
        <Card className="p-10"><EmptyHint text="Chưa có giao dịch nào. Ghi lại lệnh đầu tiên ở trên." /></Card>
      ) : (
        <div className="space-y-3">
          {journal.map((j) => (
            <Card key={j.id} className="p-4 flex flex-col md:flex-row gap-4">
              {j.image ? (
                <ZoomableThumb
                  src={j.image}
                  alt={j.ticker}
                  className="w-full md:w-32 h-24 flex-shrink-0"
                  onClick={() => setLightbox({ image: j.image, caption: `${j.ticker} · ${j.date} · ${j.side} · Giá ${fmt(j.price)}${j.reason ? " · " + j.reason : ""}` })}
                />
              ) : (
                <div className="w-full md:w-32 h-24 rounded border border-dashed flex items-center justify-center flex-shrink-0" style={{ borderColor: "#263042" }}>
                  <ImageOff size={16} className="text-slate-700" />
                </div>
              )}
              <div className="flex-1">
                <div className="flex items-center gap-3 flex-wrap">
                  <span className="font-data font-bold text-amber-400">{j.ticker}</span>
                  <span className="text-xs text-slate-600">{j.date}</span>
                  <span className="text-xs font-data" style={{ color: j.side === "Mua" ? "#34d399" : "#f87171" }}>{j.side}</span>
                  <span className="text-xs px-2 py-0.5 rounded-full" style={{ background: "#1a2130", color: "#94a3b8" }}>{j.emotion}</span>
                  {j.pl !== "" && <span className="text-xs font-data" style={{ color: Number(j.pl) >= 0 ? "#34d399" : "#f87171" }}>{Number(j.pl) >= 0 ? "+" : ""}{fmtBillion(j.pl)}</span>}
                </div>
                <div className="text-xs text-slate-500 mt-1">Giá {fmt(j.price)} · KL {fmtVol(j.qty)}</div>
                {j.reason && <div className="text-sm text-slate-400 mt-1.5">{j.reason}</div>}
              </div>
              <div className="flex gap-3 self-start">
                <button onClick={() => startEdit(j)} className="text-slate-600 hover:text-amber-400"><Pencil size={15} /></button>
                <button onClick={() => remove(j.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={15} /></button>
              </div>
            </Card>
          ))}
        </div>
      )}
      <ImageLightbox image={lightbox?.image} caption={lightbox?.caption} onClose={() => setLightbox(null)} />
    </div>
  );
}

const GOLD_SYMBOLS = [
  "XAUUSD (Vàng)", "Dầu WTI", "Dầu Brent", "BTC/USDT", "ETH/USDT",
  "EURUSD", "GBPUSD", "USDJPY", "AUDUSD", "USDCAD", "NZDUSD", "USDCHF", "EURJPY", "GBPJPY",
  "Khác",
];
const GOLD_EMOTIONS = ["Bình tĩnh", "Tự tin", "Sợ hãi", "Tham lam", "FOMO", "Do dự"];
const TIMEFRAMES = ["M1", "M15", "H1", "H4", "D1", "W1", "MN"];
const ABC_WAVES = [
  { id: "A", label: "Sóng A", hint: "Nhịp sóng gốc — nơi bắt nguồn xu hướng, sau khi thanh khoản bị quét" },
  { id: "B", label: "Sóng B", hint: "Đỉnh/đáy hồi ngược — VÙNG VÀO LỆNH theo phương pháp ICT (entry tại đây)" },
  { id: "C", label: "Sóng C", hint: "Đợt điều chỉnh tiếp theo — VÙNG CHỐT LỜI mục tiêu" },
];

function computeGoldRiskAmount(capital) {
  const cap = Number(capital.initialCapital);
  const pct = Number(capital.riskPerTrade);
  if (!capital.initialCapital || !capital.riskPerTrade || isNaN(cap) || isNaN(pct)) return null;
  return Math.round((cap * pct) / 100);
}

function fmtUSD(n) {
  if (n === undefined || n === null || n === "" || isNaN(Number(n))) return "—";
  const v = Number(n);
  return `$${v.toLocaleString("en-US", { maximumFractionDigits: 2 })}`;
}

function GoldCapitalPanel({ capital, setCapital, journal }) {
  const [editing, setEditing] = useState(false);
  const [draft, setDraft] = useState(capital);
  const riskAmount = computeGoldRiskAmount(capital);

  const closedPL = journal
    .filter((t) => t.pl !== "" && t.pl !== undefined && t.pl !== null && !isNaN(Number(t.pl)))
    .reduce((s, t) => s + Number(t.pl), 0);
  const initial = Number(capital.initialCapital);
  const hasCapital = capital.initialCapital && !isNaN(initial);
  const currentCapital = hasCapital ? initial + closedPL : null;
  const changePct = hasCapital && initial !== 0 ? Math.round((closedPL / initial) * 10000) / 100 : null;

  function startEdit() {
    setDraft(capital);
    setEditing(true);
  }
  function save() {
    setCapital(draft);
    setEditing(false);
  }

  if (editing) {
    return (
      <Card className="p-5 mb-4">
        <div className="text-sm font-semibold text-amber-300 mb-3 flex items-center gap-2"><ShieldCheck size={15} /> Quản trị vốn & rủi ro</div>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          <Input placeholder="Vốn ban đầu (USD)" value={draft.initialCapital} onChange={(e) => setDraft({ ...draft, initialCapital: e.target.value })} />
          <Input placeholder="Rủi ro tối đa / lệnh (%)" value={draft.riskPerTrade} onChange={(e) => setDraft({ ...draft, riskPerTrade: e.target.value })} />
          <Input placeholder="Rủi ro tối đa / ngày (%)" value={draft.maxDailyLoss} onChange={(e) => setDraft({ ...draft, maxDailyLoss: e.target.value })} />
          <Input placeholder="Số lệnh thua liên tiếp tối đa" value={draft.maxConsecutiveLoss} onChange={(e) => setDraft({ ...draft, maxConsecutiveLoss: e.target.value })} />
        </div>
        <div className="flex justify-end gap-2 mt-3">
          <Btn variant="ghost" onClick={() => setEditing(false)}>Hủy</Btn>
          <Btn onClick={save}><Check size={15} /> Lưu</Btn>
        </div>
      </Card>
    );
  }

  return (
    <Card className="p-5 mb-4">
      <div className="flex items-center justify-between mb-3">
        <div className="text-sm font-semibold text-amber-300 flex items-center gap-2"><ShieldCheck size={15} /> Quản trị vốn & rủi ro (USD)</div>
        <button onClick={startEdit} className="text-slate-500 hover:text-amber-400"><Pencil size={14} /></button>
      </div>
      {hasCapital ? (
        <>
          <div className="grid grid-cols-2 md:grid-cols-3 gap-4 mb-4 pb-4" style={{ borderBottom: "1px solid #1c2432" }}>
            <div>
              <div className="text-[11px] text-slate-600">Vốn ban đầu</div>
              <div className="font-data font-semibold text-slate-200">{fmtUSD(capital.initialCapital)}</div>
            </div>
            <div>
              <div className="text-[11px] text-slate-600">Vốn hiện tại</div>
              <div className="font-data font-bold text-lg" style={{ color: closedPL >= 0 ? "#34d399" : "#f87171" }}>{fmtUSD(currentCapital)}</div>
            </div>
            <div>
              <div className="text-[11px] text-slate-600">Tăng/Giảm</div>
              <div className="font-data font-semibold flex items-center gap-1" style={{ color: closedPL >= 0 ? "#34d399" : "#f87171" }}>
                {closedPL >= 0 ? <TrendingUp size={14} /> : <TrendingDown size={14} />}
                {closedPL >= 0 ? "+" : ""}{fmtUSD(closedPL)} ({changePct >= 0 ? "+" : ""}{changePct}%)
              </div>
            </div>
          </div>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
            <div>
              <div className="text-[11px] text-slate-600">Vốn ban đầu</div>
              <div className="font-data font-semibold text-slate-200">{fmtUSD(capital.initialCapital)}</div>
            </div>
            <div>
              <div className="text-[11px] text-slate-600">Rủi ro/lệnh</div>
              <div className="font-data font-semibold text-amber-400">{capital.riskPerTrade}% {riskAmount !== null && <span className="text-slate-500">(~{fmtUSD(riskAmount)})</span>}</div>
            </div>
            <div>
              <div className="text-[11px] text-slate-600">Rủi ro/ngày tối đa</div>
              <div className="font-data font-semibold text-red-400">{capital.maxDailyLoss}%</div>
            </div>
            <div>
              <div className="text-[11px] text-slate-600">Lệnh thua liên tiếp tối đa</div>
              <div className="font-data font-semibold text-slate-200">{capital.maxConsecutiveLoss}</div>
            </div>
          </div>
        </>
      ) : (
        <div className="text-sm text-slate-500">Chưa thiết lập vốn ban đầu — bấm bút chì để nhập.</div>
      )}
    </Card>
  );
}

const LOT_CONTRACT_SIZE = {
  "XAUUSD (Vàng)": 100,
  "Dầu WTI": 1000,
  "Dầu Brent": 1000,
  "BTC/USDT": 1,
  "ETH/USDT": 1,
  "EURUSD": 100000,
  "GBPUSD": 100000,
  "USDJPY": 100000,
  "AUDUSD": 100000,
  "USDCAD": 100000,
  "NZDUSD": 100000,
  "USDCHF": 100000,
  "EURJPY": 100000,
  "GBPJPY": 100000,
  "Khác": 1,
};

function computeInstrumentPnl({ symbol, direction, entry, exit, lot, contractSize }) {
  const e = Number(entry), x = Number(exit), l = Number(lot);
  const cs = contractSize !== undefined ? Number(contractSize) : Number(LOT_CONTRACT_SIZE[symbol] ?? 1);
  const valid = entry !== "" && exit !== "" && lot !== "" && !isNaN(e) && !isNaN(x) && !isNaN(l) && !isNaN(cs);
  if (!valid) return null;
  const diff = direction === "Long" ? x - e : e - x;
  return diff * cs * l;
}

function LotPnlCalculator({ onApply }) {
  const [open, setOpen] = useState(false);
  const [symbol, setSymbol] = useState("XAUUSD (Vàng)");
  const [contractSize, setContractSize] = useState(LOT_CONTRACT_SIZE["XAUUSD (Vàng)"]);
  const [direction, setDirection] = useState("Long");
  const [entry, setEntry] = useState("");
  const [exit, setExit] = useState("");
  const [lot, setLot] = useState("");

  function changeSymbol(s) {
    setSymbol(s);
    setContractSize(LOT_CONTRACT_SIZE[s] ?? 1);
  }

  const pnl = computeInstrumentPnl({ symbol, direction, entry, exit, lot, contractSize });

  return (
    <Card className="p-5 mb-4">
      <button onClick={() => setOpen(!open)} className="w-full flex items-center justify-between text-left">
        <div className="text-sm font-semibold text-amber-300 flex items-center gap-2"><Gauge size={15} /> Công cụ tính Lot &amp; Lãi/Lỗ nhanh (USD)</div>
        {open ? <ChevronUp size={16} className="text-slate-500" /> : <ChevronDown size={16} className="text-slate-500" />}
      </button>
      {open && (
        <div className="mt-4">
          <div className="text-[11px] text-slate-500 mb-3">Dùng để tính thử độc lập. Khi ghi lệnh thật, form bên dưới sẽ tự tính lãi/lỗ cho bạn — không cần copy qua đây.</div>
          <div className="grid grid-cols-2 md:grid-cols-3 gap-3 mb-3">
            <select value={symbol} onChange={(e) => changeSymbol(e.target.value)} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
              {Object.keys(LOT_CONTRACT_SIZE).map((s) => <option key={s}>{s}</option>)}
            </select>
            <Input placeholder="Khối lượng hợp đồng / lot (đvcs)" value={contractSize} onChange={(e) => setContractSize(e.target.value)} />
            <div className="flex rounded-md overflow-hidden border" style={{ borderColor: "#263042" }}>
              <button onClick={() => setDirection("Long")} className="flex-1 text-sm font-medium py-2" style={{ background: direction === "Long" ? "#34d39922" : "transparent", color: direction === "Long" ? "#34d399" : "#64748b" }}>Long</button>
              <button onClick={() => setDirection("Short")} className="flex-1 text-sm font-medium py-2" style={{ background: direction === "Short" ? "#f8717122" : "transparent", color: direction === "Short" ? "#f87171" : "#64748b" }}>Short</button>
            </div>
          </div>
          <div className="grid grid-cols-3 gap-3 mb-3">
            <Input placeholder="Giá vào (Entry)" value={entry} onChange={(e) => setEntry(e.target.value)} />
            <Input placeholder="Giá thoát (Exit)" value={exit} onChange={(e) => setExit(e.target.value)} />
            <Input placeholder="Khối lượng (lot)" value={lot} onChange={(e) => setLot(e.target.value)} />
          </div>
          <div className="text-xs text-slate-500">
            Kết quả: {pnl !== null ? (
              <span className="font-data font-bold text-base ml-1" style={{ color: pnl >= 0 ? "#34d399" : "#f87171" }}>{pnl >= 0 ? "+" : ""}{fmtUSD(pnl)}</span>
            ) : (
              <span className="text-slate-600">nhập đủ giá trị để tính</span>
            )}
          </div>
          <div className="text-[11px] text-slate-600 mt-2">Khối lượng hợp đồng mặc định: Vàng 100oz/lot · Dầu 1000 thùng/lot · BTC/ETH 1 coin/lot · Forex 100.000 đơn vị/lot chuẩn — chỉnh lại nếu sàn bạn dùng quy định khác. Với các cặp không yết giá theo USD (VD USDJPY, EURJPY...), kết quả ra theo đơn vị tiền yết giá, cần tự quy đổi thêm sang USD.</div>
        </div>
      )}
    </Card>
  );
}

function GoldOilBtcJournal({ journal, setJournal, capital, setCapital }) {
  const empty = {
    symbol: "XAUUSD (Vàng)", customSymbol: "", date: todayISO(), direction: "Long", timeframe: "H4", wave: "B",
    entry: "", sl: "", tp: "", lot: "", contractSize: "", riskPercent: "",
    smc: { structure: false, liquiditySource: false, divergence: false, obFvgHtf: false, waveBC: false, entryAtFoot: false },
    killzone: false, newsClear: false,
    status: "Đang mở", exitPrice: "",
    emotion: "Bình tĩnh", reason: "", pl: "", image: null,
  };
  const [form, setForm] = useState(empty);
  const [editingId, setEditingId] = useState(null);
  const [busy, setBusy] = useState(false);
  const [lightbox, setLightbox] = useState(null);

  const rr = computeRR(form);
  const riskAmount = computeGoldRiskAmount(capital);
  const riskWithinLimit = !form.riskPercent || !capital.riskPerTrade ? null : Number(form.riskPercent) <= Number(capital.riskPerTrade);
  const effectiveContractSize = form.contractSize !== "" ? form.contractSize : LOT_CONTRACT_SIZE[form.symbol] ?? 1;
  const autoPnl = form.status === "Đã đóng"
    ? computeInstrumentPnl({ symbol: form.symbol, direction: form.direction, entry: form.entry, exit: form.exitPrice, lot: form.lot, contractSize: effectiveContractSize })
    : null;

  const conditions = [
    { label: "Bước 1: Xác định cấu trúc", passed: form.smc.structure },
    { label: "Bước 2: Thanh khoản & nguồn gốc sóng", passed: form.smc.liquiditySource },
    { label: "Bước 3: Yếu tố phân kỳ", passed: form.smc.divergence },
    { label: "Bước 4: OB/FVG khung lớn", passed: form.smc.obFvgHtf },
    { label: "Bước 5: Sóng B → C (ICT)", passed: form.smc.waveBC },
    { label: "Bước 6: Entry sát chân sóng đã quét", passed: form.smc.entryAtFoot },
    { label: "R:R đạt tối thiểu 1:2", passed: rr !== null && rr >= 2 },
    { label: "Rủi ro trong hạn mức cho phép", passed: riskWithinLimit === true },
    { label: "Đúng khung giờ giao dịch tốt (Kill Zone)", passed: form.killzone },
    { label: "Không có tin tức quan trọng sắp ra", passed: form.newsClear },
  ];
  const conditionsMet = conditions.filter((c) => c.passed).length;

  async function handleImage(e) {
    const file = e.target.files[0];
    if (!file) return;
    setBusy(true);
    try {
      const dataUrl = await compressImage(file);
      setForm((f) => ({ ...f, image: dataUrl }));
    } finally {
      setBusy(false);
    }
  }
  function save() {
    const symbolLabel = form.symbol === "Khác" ? form.customSymbol.trim().toUpperCase() : form.symbol;
    if (!symbolLabel) return;
    const finalPl = form.status === "Đã đóng" && autoPnl !== null ? String(Math.round(autoPnl * 100) / 100) : "";
    const entry = { ...form, symbolLabel, pl: finalPl, conditionsMet, conditionsTotal: conditions.length };
    if (editingId) {
      setJournal((prev) => prev.map((j) => (j.id === editingId ? { ...entry, id: editingId } : j)));
    } else {
      setJournal((prev) => [{ id: Date.now(), ...entry }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(j) {
    setEditingId(j.id);
    setForm({ ...empty, ...j });
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(empty);
  }
  function remove(id) {
    setJournal((prev) => prev.filter((j) => j.id !== id));
    if (editingId === id) cancelEdit();
  }
  function toggleSmc(id) {
    setForm((f) => ({ ...f, smc: { ...f.smc, [id]: !f.smc[id] } }));
  }

  return (
    <div>
      <KillzoneClock />
      <GoldCapitalPanel capital={capital} setCapital={setCapital} journal={journal} />
      <LotPnlCalculator />

      <Card className="p-4 mb-6">
        {editingId && (
          <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
            <Pencil size={12} /> Đang chỉnh sửa lệnh — <button onClick={cancelEdit} className="underline text-slate-500">hủy</button>
          </div>
        )}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-3">
          <select value={form.symbol} onChange={(e) => setForm({ ...form, symbol: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {GOLD_SYMBOLS.map((s) => <option key={s}>{s}</option>)}
          </select>
          {form.symbol === "Khác" && (
            <Input placeholder="Nhập tên cặp/mã" value={form.customSymbol} onChange={(e) => setForm({ ...form, customSymbol: e.target.value })} />
          )}
          <Input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} />
          <div className="flex rounded-md overflow-hidden border" style={{ borderColor: "#263042" }}>
            <button onClick={() => setForm({ ...form, direction: "Long" })} className="flex-1 text-sm font-medium py-2 flex items-center justify-center gap-1" style={{ background: form.direction === "Long" ? "#34d39922" : "transparent", color: form.direction === "Long" ? "#34d399" : "#64748b" }}>
              <TrendingUp size={14} /> Long
            </button>
            <button onClick={() => setForm({ ...form, direction: "Short" })} className="flex-1 text-sm font-medium py-2 flex items-center justify-center gap-1" style={{ background: form.direction === "Short" ? "#f8717122" : "transparent", color: form.direction === "Short" ? "#f87171" : "#64748b" }}>
              <TrendingDown size={14} /> Short
            </button>
          </div>
          <select value={form.emotion} onChange={(e) => setForm({ ...form, emotion: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {GOLD_EMOTIONS.map((e) => <option key={e}>{e}</option>)}
          </select>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-2 gap-3 mb-3">
          <div>
            <div className="text-[11px] text-slate-500 mb-1.5">Khung thời gian</div>
            <div className="flex flex-wrap gap-1.5">
              {TIMEFRAMES.map((tf) => (
                <button
                  key={tf}
                  onClick={() => setForm({ ...form, timeframe: tf })}
                  className="px-3 py-1.5 rounded-md text-xs font-data font-medium"
                  style={{ background: form.timeframe === tf ? "linear-gradient(90deg,#f59e0b,#fbbf24)" : "#0a0e14", color: form.timeframe === tf ? "#1a1206" : "#94a3b8", border: "1px solid #263042" }}
                >
                  {tf}
                </button>
              ))}
            </div>
          </div>
          <div>
            <div className="text-[11px] text-slate-500 mb-1.5">Sóng ABC (ICT)</div>
            <div className="flex gap-1.5">
              {ABC_WAVES.map((w) => (
                <button
                  key={w.id}
                  onClick={() => setForm({ ...form, wave: w.id })}
                  title={w.hint}
                  className="flex-1 px-3 py-1.5 rounded-md text-xs font-medium"
                  style={{ background: form.wave === w.id ? "linear-gradient(90deg,#7c3aed,#a855f7)" : "#0a0e14", color: form.wave === w.id ? "#fff" : "#94a3b8", border: "1px solid #263042" }}
                >
                  {w.label}
                </button>
              ))}
            </div>
            <div className="text-[11px] text-slate-500 mt-1">{ABC_WAVES.find((w) => w.id === form.wave)?.hint}</div>
          </div>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-5 gap-3 mb-1">
          <Input placeholder="Entry" value={form.entry} onChange={(e) => setForm({ ...form, entry: e.target.value })} />
          <Input placeholder="Stop Loss" value={form.sl} onChange={(e) => setForm({ ...form, sl: e.target.value })} />
          <Input placeholder="Take Profit" value={form.tp} onChange={(e) => setForm({ ...form, tp: e.target.value })} />
          <Input placeholder="Khối lượng (lot)" value={form.lot} onChange={(e) => setForm({ ...form, lot: e.target.value })} />
          <Input placeholder="Rủi ro dự kiến (%)" value={form.riskPercent} onChange={(e) => setForm({ ...form, riskPercent: e.target.value })} />
        </div>

        <div className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-1 mt-2">
          <div className="flex rounded-md overflow-hidden border" style={{ borderColor: "#263042" }}>
            <button onClick={() => setForm({ ...form, status: "Đang mở" })} className="flex-1 text-xs font-medium py-2" style={{ background: form.status === "Đang mở" ? "#fbbf2422" : "transparent", color: form.status === "Đang mở" ? "#fbbf24" : "#64748b" }}>Đang mở</button>
            <button onClick={() => setForm({ ...form, status: "Đã đóng" })} className="flex-1 text-xs font-medium py-2" style={{ background: form.status === "Đã đóng" ? "#34d39922" : "transparent", color: form.status === "Đã đóng" ? "#34d399" : "#64748b" }}>Đã đóng</button>
          </div>
          {form.status === "Đã đóng" && (
            <>
              <Input placeholder="Giá đóng lệnh (Exit)" value={form.exitPrice} onChange={(e) => setForm({ ...form, exitPrice: e.target.value })} />
              <Input placeholder={`KL hợp đồng (mặc định ${LOT_CONTRACT_SIZE[form.symbol] ?? 1})`} value={form.contractSize} onChange={(e) => setForm({ ...form, contractSize: e.target.value })} />
              <div className="flex items-center px-3 rounded-md text-sm font-data font-semibold" style={{ background: "#0a0e14", border: `1px solid ${autoPnl === null ? "#263042" : autoPnl >= 0 ? "#34d39955" : "#f8717155"}`, color: autoPnl === null ? "#64748b" : autoPnl >= 0 ? "#34d399" : "#f87171" }}>
                {autoPnl === null ? "Nhập đủ để tính lãi/lỗ" : `${autoPnl >= 0 ? "+" : ""}${fmtUSD(autoPnl)}`}
              </div>
            </>
          )}
        </div>
        {form.status === "Đã đóng" && (
          <div className="text-[11px] text-slate-600 mb-2">Lãi/lỗ tự tính từ Entry, Exit, Khối lượng lot và Khối lượng hợp đồng — không cần nhập tay.</div>
        )}
        <div className="flex flex-wrap items-center gap-2 mb-3">
          {rr !== null && (
            <span className="text-xs font-data px-2 py-1 rounded-full" style={{ background: rr >= 2 ? "#34d39922" : "#f8717122", color: rr >= 2 ? "#34d399" : "#f87171" }}>R:R = 1:{rr}</span>
          )}
          {riskWithinLimit !== null && (
            <span className="text-xs font-data px-2 py-1 rounded-full" style={{ background: riskWithinLimit ? "#34d39922" : "#f8717122", color: riskWithinLimit ? "#34d399" : "#f87171" }}>
              {riskWithinLimit ? "Rủi ro trong hạn mức" : `Vượt hạn mức ${capital.riskPerTrade}%/lệnh`}
            </span>
          )}
        </div>

        <div className="mb-3">
          <div className="text-xs text-slate-500 mb-2">Phương pháp vào lệnh — 6 bước</div>
          <div className="space-y-1.5">
            <StepToggle
              title="Bước 1: Xác định cấu trúc"
              hint="Xác định xu hướng chính trên khung lớn (HH-HL cho Long, LH-LL cho Short)"
              checked={form.smc.structure}
              onToggle={() => toggleSmc("structure")}
              size="small"
            />
            <StepToggle
              title="Bước 2: Tìm thanh khoản & nguồn gốc sóng sau khi quét"
              hint="Xác định vùng thanh khoản đã bị quét, và điểm khởi nguồn (chân) của nhịp sóng mới hình thành từ đó"
              checked={form.smc.liquiditySource}
              onToggle={() => toggleSmc("liquiditySource")}
              size="small"
            />
            <StepToggle
              title="Bước 3: Yếu tố phân kỳ"
              hint="Kiểm tra phân kỳ RSI hoặc phân kỳ liên thị trường (SMT) tại vùng vừa quét thanh khoản — xác nhận thêm khả năng đảo chiều trước khi đi tiếp"
              checked={form.smc.divergence}
              onToggle={() => toggleSmc("divergence")}
              size="small"
            />
            <StepToggle
              title="Bước 4: Tìm OB/FVG khung lớn để vào lệnh khung nhỏ"
              hint="Xác định Order Block/FVG trên khung lớn, chờ giá hồi về rồi tìm điểm vào chính xác trên khung nhỏ"
              checked={form.smc.obFvgHtf}
              onToggle={() => toggleSmc("obFvgHtf")}
              size="small"
            />
            <StepToggle
              title="Bước 5: Sóng ABC (ICT) — vào lệnh đỉnh sóng B, chốt tại C"
              hint="Xác định sóng B (đỉnh/đáy hồi ngược) làm vùng vào lệnh, mục tiêu chốt lời tại vùng sóng C"
              checked={form.smc.waveBC}
              onToggle={() => toggleSmc("waveBC")}
              size="small"
            />
            <StepToggle
              title="Bước 6: Entry càng sát chân sóng càng tốt"
              hint="Chỉ vào lệnh khi chân sóng đó đã thực sự quét thanh khoản — entry càng gần chân sóng, R:R càng tối ưu"
              checked={form.smc.entryAtFoot}
              onToggle={() => toggleSmc("entryAtFoot")}
              size="small"
            />
          </div>
        </div>

        <div className="mb-3">
          <div className="text-xs text-slate-500 mb-2 flex items-center justify-between">
            <span>Điều kiện đủ trước khi vào lệnh</span>
            <span className="font-data" style={{ color: conditionsMet === conditions.length ? "#34d399" : "#94a3b8" }}>{conditionsMet}/{conditions.length}</span>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-1.5">
            {conditions.map((c, i) => (
              <div key={i} className="flex items-center gap-2 text-xs py-1">
                <span className="w-4 h-4 rounded-full flex items-center justify-center flex-shrink-0 text-[10px] font-bold" style={{ background: c.passed ? "#34d399" : "#2a1840", color: c.passed ? "#0a0e14" : "#64748b" }}>{c.passed ? "✓" : i + 1}</span>
                <span style={{ color: c.passed ? "#cbd5e1" : "#64748b" }}>{c.label}</span>
              </div>
            ))}
          </div>
          <div className="flex gap-2 mt-2">
            <StepToggle title="Đúng Kill Zone" checked={form.killzone} onToggle={() => setForm({ ...form, killzone: !form.killzone })} size="small" />
            <StepToggle title="Không có tin quan trọng" checked={form.newsClear} onToggle={() => setForm({ ...form, newsClear: !form.newsClear })} size="small" />
          </div>
        </div>

        <Textarea rows={2} placeholder="Lý do vào lệnh / luận điểm SMC..." value={form.reason} onChange={(e) => setForm({ ...form, reason: e.target.value })} />
        <div className="flex items-center justify-between mt-3">
          <label className="flex items-center gap-1.5 text-xs rounded-md cursor-pointer px-3 py-2" style={inputStyle}>
            <Upload size={13} /> {busy ? "Đang tải..." : form.image ? "Đã chọn ảnh" : "Tải ảnh biểu đồ"}
            <input type="file" accept="image/*" onChange={handleImage} className="hidden" />
          </label>
          <Btn onClick={save}>{editingId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Ghi giao dịch</>}</Btn>
        </div>
        {form.image && (
          <div className="mt-3 flex items-center gap-2">
            <img src={form.image} alt="preview" className="h-12 rounded border border-slate-700" />
            <button onClick={() => setForm({ ...form, image: null })} className="text-xs text-slate-500 hover:text-red-400">Xóa ảnh</button>
          </div>
        )}
      </Card>

      {journal.length === 0 ? (
        <Card className="p-10"><EmptyHint text="Chưa có giao dịch nào. Ghi lại lệnh Vàng/Dầu/BTC đầu tiên theo phương pháp SMC ở trên." /></Card>
      ) : (
        <div className="space-y-3">
          {journal.map((j) => {
            const jrr = computeRR(j);
            const smcOk = j.smc ? Object.values(j.smc).filter(Boolean).length : 0;
            return (
              <Card key={j.id} className="p-4 flex flex-col md:flex-row gap-4">
                {j.image ? (
                  <ZoomableThumb
                    src={j.image}
                    alt={j.symbolLabel}
                    className="w-full md:w-32 h-24 flex-shrink-0"
                    onClick={() => setLightbox({ image: j.image, caption: `${j.symbolLabel} · ${j.date} · ${j.direction} · Entry ${fmt(j.entry)}${j.reason ? " · " + j.reason : ""}` })}
                  />
                ) : (
                  <div className="w-full md:w-32 h-24 rounded border border-dashed flex items-center justify-center flex-shrink-0" style={{ borderColor: "#263042" }}>
                    <ImageOff size={16} className="text-slate-700" />
                  </div>
                )}
                <div className="flex-1">
                  <div className="flex items-center gap-3 flex-wrap">
                    <span className="font-data font-bold text-amber-400">{j.symbolLabel}</span>
                    <span className="text-xs text-slate-600">{j.date}</span>
                    <span className="text-[11px] font-data px-1.5 py-0.5 rounded" style={{ background: "#1a2130", color: "#94a3b8" }}>{j.timeframe}</span>
                    <span className="text-xs font-medium px-2 py-0.5 rounded-full" style={{ background: j.direction === "Short" ? "#f8717122" : "#34d39922", color: j.direction === "Short" ? "#f87171" : "#34d399" }}>{j.direction}</span>
                    <span className="text-[11px] text-purple-300">Sóng {j.wave}</span>
                    <span className="text-xs px-2 py-0.5 rounded-full" style={{ background: "#1a2130", color: "#94a3b8" }}>{j.emotion}</span>
                    <span className="text-[11px] font-data text-slate-500">Điều kiện {j.conditionsMet ?? "-"}/{j.conditionsTotal ?? 10}</span>
                    {jrr !== null && <span className="text-xs font-data" style={{ color: jrr >= 2 ? "#34d399" : "#f87171" }}>R:R 1:{jrr}</span>}
                    {j.pl !== "" && <span className="text-xs font-data" style={{ color: Number(j.pl) >= 0 ? "#34d399" : "#f87171" }}>{Number(j.pl) >= 0 ? "+" : ""}{fmt(j.pl)}</span>}
                  </div>
                  <div className="text-xs text-slate-500 mt-1">Entry {fmt(j.entry)} · SL {fmt(j.sl)} · TP {fmt(j.tp)} · KL {fmt(j.lot)}</div>
                  {j.reason && <div className="text-sm text-slate-400 mt-1.5">{j.reason}</div>}
                </div>
                <div className="flex gap-3 self-start">
                  <button onClick={() => startEdit(j)} className="text-slate-600 hover:text-amber-400"><Pencil size={15} /></button>
                  <button onClick={() => remove(j.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={15} /></button>
                </div>
              </Card>
            );
          })}
        </div>
      )}
      <ImageLightbox image={lightbox?.image} caption={lightbox?.caption} onClose={() => setLightbox(null)} />
    </div>
  );
}

/* ---------------------------------------------------------
   FOREX — ICT SILVER BULLET
--------------------------------------------------------- */
const FOREX_PAIRS = ["EURUSD", "GBPUSD", "USDJPY", "AUDUSD", "USDCAD", "NZDUSD", "USDCHF", "EURJPY", "GBPJPY", "XAUUSD (Vàng)", "Khác"];
const FOREX_EMOTIONS = ["Bình tĩnh", "Tự tin", "Sợ hãi", "Tham lam", "FOMO", "Do dự"];
const KILLZONES = [
  { id: "london", label: "London Killzone", startMin: 14 * 60, endMin: 15 * 60 },
  { id: "nyam", label: "New York AM Killzone", startMin: 21 * 60, endMin: 22 * 60 },
  { id: "nypm", label: "New York PM Killzone", startMin: 1 * 60, endMin: 2 * 60 },
];
const SILVER_BULLET_STEPS = [
  { id: "htfBias", label: "1. Xác định bias HTF (H4/D1) — lệnh cùng chiều xu hướng chính" },
  { id: "sessionRange", label: "2. Đã ghi nhận đỉnh/đáy phiên trước làm vùng thanh khoản mục tiêu" },
  { id: "inKillzone", label: "3. Đang trong đúng khung giờ Killzone" },
  { id: "sweep", label: "4. Giá đã quét thanh khoản (sweep) trong khung giờ này" },
  { id: "mss", label: "5. Có MSS xác nhận trên M5/M15 ngay sau sweep" },
  { id: "fvg", label: "6. Có FVG mới hình thành từ chính cú MSS đó" },
  { id: "entry50", label: "7. Entry tại 50% FVG, SL ngoài điểm quét, TP tại thanh khoản đối diện" },
];

function getVNNowMinutes() {
  const now = new Date();
  const utcMin = now.getUTCHours() * 60 + now.getUTCMinutes();
  return (utcMin + 7 * 60) % (24 * 60);
}
function minutesToHHMM(min) {
  const m = ((min % (24 * 60)) + 24 * 60) % (24 * 60);
  const h = Math.floor(m / 60);
  const mm = m % 60;
  return `${String(h).padStart(2, "0")}:${String(mm).padStart(2, "0")}`;
}
function getKillzoneStatuses(nowMin) {
  return KILLZONES.map((kz) => {
    let start = kz.startMin, end = kz.endMin;
    // xử lý khung qua đêm (VD 01:00-02:00) — không cần chỉnh vì start<end ở đây, nhưng giữ tổng quát
    const active = nowMin >= start && nowMin < end;
    let diffToStart = start - nowMin;
    if (diffToStart < 0) diffToStart += 24 * 60;
    return { ...kz, active, minutesToStart: diffToStart };
  });
}

function KillzoneClock() {
  const [nowMin, setNowMin] = useState(getVNNowMinutes());
  useEffect(() => {
    const t = setInterval(() => setNowMin(getVNNowMinutes()), 30000);
    return () => clearInterval(t);
  }, []);
  const zones = getKillzoneStatuses(nowMin);
  const activeZone = zones.find((z) => z.active);
  const nextZone = [...zones].sort((a, b) => a.minutesToStart - b.minutesToStart)[0];

  return (
    <Card className="p-5 mb-4">
      <div className="flex items-center justify-between mb-4">
        <div className="text-sm font-semibold text-amber-300 flex items-center gap-2"><Gauge size={15} /> Đồng hồ Killzone (giờ Việt Nam)</div>
        <span className="font-data text-sm text-slate-400">{minutesToHHMM(nowMin)}</span>
      </div>
      {activeZone ? (
        <div className="flex items-center gap-2 text-sm px-3 py-2 rounded-md mb-3" style={{ background: "#34d39914", border: "1px solid #34d39940", color: "#34d399" }}>
          <Flame size={14} /> Đang trong {activeZone.label} — cửa sổ vào lệnh Silver Bullet đang mở!
        </div>
      ) : (
        <div className="flex items-center gap-2 text-sm px-3 py-2 rounded-md mb-3" style={{ background: "#94a3b814", border: "1px solid #94a3b830", color: "#94a3b8" }}>
          Ngoài khung giờ Killzone — {nextZone.label} bắt đầu sau {Math.floor(nextZone.minutesToStart / 60)}h{String(nextZone.minutesToStart % 60).padStart(2, "0")}
        </div>
      )}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-2">
        {zones.map((z) => (
          <div key={z.id} className="rounded-md p-3" style={{ background: z.active ? "#34d39912" : "#0a0e14", border: `1px solid ${z.active ? "#34d39940" : "#1c2432"}` }}>
            <div className="text-xs font-medium" style={{ color: z.active ? "#34d399" : "#94a3b8" }}>{z.label}</div>
            <div className="font-data text-sm text-slate-300 mt-1">{minutesToHHMM(z.startMin)} – {minutesToHHMM(z.endMin)}</div>
          </div>
        ))}
      </div>
      <div className="text-[11px] text-slate-600 mt-3">Giờ mùa hè Mỹ (DST) — kiểm tra lại lịch nếu giao dịch sát thời điểm đổi giờ mùa đông/hè.</div>
    </Card>
  );
}

function ForexJournal({ journal, setJournal }) {
  const empty = {
    symbol: "EURUSD", customSymbol: "", date: todayISO(), killzone: "london", direction: "Long",
    entry: "", sl: "", tp: "", lot: "",
    steps: { htfBias: false, sessionRange: false, inKillzone: false, sweep: false, mss: false, fvg: false, entry50: false },
    emotion: "Bình tĩnh", reason: "", pl: "", image: null,
  };
  const [form, setForm] = useState(empty);
  const [editingId, setEditingId] = useState(null);
  const [busy, setBusy] = useState(false);

  const rr = computeRR(form);
  const stepsCount = Object.values(form.steps).filter(Boolean).length;

  function toggleStep(id) {
    setForm((f) => ({ ...f, steps: { ...f.steps, [id]: !f.steps[id] } }));
  }
  async function handleImage(e) {
    const file = e.target.files[0];
    if (!file) return;
    setBusy(true);
    try {
      const dataUrl = await compressImage(file);
      setForm((f) => ({ ...f, image: dataUrl }));
    } finally {
      setBusy(false);
    }
  }
  function save() {
    const symbolLabel = form.symbol === "Khác" ? form.customSymbol.trim().toUpperCase() : form.symbol;
    if (!symbolLabel) return;
    const entry = { ...form, symbolLabel, stepsCount, stepsTotal: SILVER_BULLET_STEPS.length };
    if (editingId) {
      setJournal((prev) => prev.map((j) => (j.id === editingId ? { ...entry, id: editingId } : j)));
    } else {
      setJournal((prev) => [{ id: Date.now(), ...entry }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(j) {
    setEditingId(j.id);
    setForm({ ...empty, ...j });
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(empty);
  }
  function remove(id) {
    setJournal((prev) => prev.filter((j) => j.id !== id));
    if (editingId === id) cancelEdit();
  }

  const killzoneLabel = KILLZONES.find((k) => k.id === form.killzone)?.label || "";

  return (
    <div>
      <KillzoneClock />

      <Card className="p-4 mb-6">
        {editingId && (
          <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
            <Pencil size={12} /> Đang chỉnh sửa lệnh — <button onClick={cancelEdit} className="underline text-slate-500">hủy</button>
          </div>
        )}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-3">
          <select value={form.symbol} onChange={(e) => setForm({ ...form, symbol: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {FOREX_PAIRS.map((s) => <option key={s}>{s}</option>)}
          </select>
          {form.symbol === "Khác" && (
            <Input placeholder="Nhập tên cặp" value={form.customSymbol} onChange={(e) => setForm({ ...form, customSymbol: e.target.value })} />
          )}
          <Input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} />
          <select value={form.killzone} onChange={(e) => setForm({ ...form, killzone: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {KILLZONES.map((k) => <option key={k.id} value={k.id}>{k.label}</option>)}
          </select>
          <div className="flex rounded-md overflow-hidden border" style={{ borderColor: "#263042" }}>
            <button onClick={() => setForm({ ...form, direction: "Long" })} className="flex-1 text-sm font-medium py-2 flex items-center justify-center gap-1" style={{ background: form.direction === "Long" ? "#34d39922" : "transparent", color: form.direction === "Long" ? "#34d399" : "#64748b" }}>
              <TrendingUp size={14} /> Long
            </button>
            <button onClick={() => setForm({ ...form, direction: "Short" })} className="flex-1 text-sm font-medium py-2 flex items-center justify-center gap-1" style={{ background: form.direction === "Short" ? "#f8717122" : "transparent", color: form.direction === "Short" ? "#f87171" : "#64748b" }}>
              <TrendingDown size={14} /> Short
            </button>
          </div>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-5 gap-3 mb-1">
          <Input placeholder="Entry (50% FVG)" value={form.entry} onChange={(e) => setForm({ ...form, entry: e.target.value })} />
          <Input placeholder="Stop Loss" value={form.sl} onChange={(e) => setForm({ ...form, sl: e.target.value })} />
          <Input placeholder="Take Profit" value={form.tp} onChange={(e) => setForm({ ...form, tp: e.target.value })} />
          <Input placeholder="Khối lượng (lot)" value={form.lot} onChange={(e) => setForm({ ...form, lot: e.target.value })} />
          <select value={form.emotion} onChange={(e) => setForm({ ...form, emotion: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
            {FOREX_EMOTIONS.map((e) => <option key={e}>{e}</option>)}
          </select>
        </div>
        {rr !== null && (
          <div className="mb-3">
            <span className="text-xs font-data px-2 py-1 rounded-full" style={{ background: rr >= 2 ? "#34d39922" : "#f8717122", color: rr >= 2 ? "#34d399" : "#f87171" }}>R:R = 1:{rr}</span>
          </div>
        )}

        <div className="mb-3">
          <div className="text-xs text-slate-500 mb-2 flex items-center justify-between">
            <span>Checklist ICT Silver Bullet — {killzoneLabel}</span>
            <span className="font-data" style={{ color: stepsCount === SILVER_BULLET_STEPS.length ? "#34d399" : "#94a3b8" }}>{stepsCount}/{SILVER_BULLET_STEPS.length}</span>
          </div>
          <div className="space-y-1.5">
            {SILVER_BULLET_STEPS.map((s) => (
              <StepToggle key={s.id} title={s.label} checked={form.steps[s.id]} onToggle={() => toggleStep(s.id)} size="small" />
            ))}
          </div>
        </div>

        <Textarea rows={2} placeholder="Ghi chú thêm (đỉnh/đáy phiên trước, ghi chú MSS/FVG...)" value={form.reason} onChange={(e) => setForm({ ...form, reason: e.target.value })} />
        <div className="grid grid-cols-2 gap-3 mt-3">
          <Input placeholder="Lãi/Lỗ (nếu đã chốt)" value={form.pl} onChange={(e) => setForm({ ...form, pl: e.target.value })} />
          <label className="flex items-center justify-center gap-1.5 text-xs rounded-md cursor-pointer" style={inputStyle}>
            <Upload size={13} /> {busy ? "Đang tải..." : form.image ? "Đã chọn ảnh" : "Tải ảnh biểu đồ"}
            <input type="file" accept="image/*" onChange={handleImage} className="hidden" />
          </label>
        </div>
        <div className="flex items-center justify-between mt-3">
          {form.image ? (
            <div className="flex items-center gap-2">
              <img src={form.image} alt="preview" className="h-12 rounded border border-slate-700" />
              <button onClick={() => setForm({ ...form, image: null })} className="text-xs text-slate-500 hover:text-red-400">Xóa ảnh</button>
            </div>
          ) : <span />}
          <Btn onClick={save}>{editingId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Ghi giao dịch</>}</Btn>
        </div>
      </Card>

      {journal.length === 0 ? (
        <Card className="p-10"><EmptyHint text="Chưa có giao dịch nào. Ghi lại lệnh Forex đầu tiên theo ICT Silver Bullet ở trên." /></Card>
      ) : (
        <div className="space-y-3">
          {journal.map((j) => {
            const jrr = computeRR(j);
            const kz = KILLZONES.find((k) => k.id === j.killzone)?.label || j.killzone;
            return (
              <Card key={j.id} className="p-4 flex flex-col md:flex-row gap-4">
                {j.image ? (
                  <img src={j.image} alt={j.symbolLabel} className="w-full md:w-32 h-24 object-cover rounded border border-slate-700 flex-shrink-0" />
                ) : (
                  <div className="w-full md:w-32 h-24 rounded border border-dashed flex items-center justify-center flex-shrink-0" style={{ borderColor: "#263042" }}>
                    <ImageOff size={16} className="text-slate-700" />
                  </div>
                )}
                <div className="flex-1">
                  <div className="flex items-center gap-3 flex-wrap">
                    <span className="font-data font-bold text-amber-400">{j.symbolLabel}</span>
                    <span className="text-xs text-slate-600">{j.date}</span>
                    <span className="text-[11px] px-1.5 py-0.5 rounded" style={{ background: "#1a2130", color: "#94a3b8" }}>{kz}</span>
                    <span className="text-xs font-medium px-2 py-0.5 rounded-full" style={{ background: j.direction === "Short" ? "#f8717122" : "#34d39922", color: j.direction === "Short" ? "#f87171" : "#34d399" }}>{j.direction}</span>
                    <span className="text-xs px-2 py-0.5 rounded-full" style={{ background: "#1a2130", color: "#94a3b8" }}>{j.emotion}</span>
                    <span className="text-[11px] font-data text-slate-500">Bước {j.stepsCount ?? "-"}/{j.stepsTotal ?? 7}</span>
                    {jrr !== null && <span className="text-xs font-data" style={{ color: jrr >= 2 ? "#34d399" : "#f87171" }}>R:R 1:{jrr}</span>}
                    {j.pl !== "" && <span className="text-xs font-data" style={{ color: Number(j.pl) >= 0 ? "#34d399" : "#f87171" }}>{Number(j.pl) >= 0 ? "+" : ""}{fmt(j.pl)}</span>}
                  </div>
                  <div className="text-xs text-slate-500 mt-1">Entry {fmt(j.entry)} · SL {fmt(j.sl)} · TP {fmt(j.tp)} · KL {fmt(j.lot)}</div>
                  {j.reason && <div className="text-sm text-slate-400 mt-1.5">{j.reason}</div>}
                </div>
                <div className="flex gap-3 self-start">
                  <button onClick={() => startEdit(j)} className="text-slate-600 hover:text-amber-400"><Pencil size={15} /></button>
                  <button onClick={() => remove(j.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={15} /></button>
                </div>
              </Card>
            );
          })}
        </div>
      )}
    </div>
  );
}
const MOOD_COLORS = ["#161d2a", "#4c1d1d", "#7c3a17", "#7a5f14", "#2f6b4f", "#1f9d61"];
function scoreColor(score) {
  if (score === undefined) return MOOD_COLORS[0];
  const idx = Math.max(0, Math.min(5, Math.round(score)));
  return MOOD_COLORS[idx];
}
function buildDays(count) {
  const days = [];
  const now = new Date();
  for (let i = count - 1; i >= 0; i--) {
    const d = new Date(now);
    d.setDate(now.getDate() - i);
    days.push(d.toISOString().slice(0, 10));
  }
  return days;
}
function marketByDate(market) {
  const map = {};
  market.forEach((m) => { map[m.date] = m; });
  return map;
}

function MarketJournal({ market, setMarket }) {
  const empty = { date: todayISO(), vnindex: "", change: "", volume: "", foreignNet: "", breadth: "", mood: 3, discipline: 3, note: "", images: [] };
  const [form, setForm] = useState(empty);
  const [editingId, setEditingId] = useState(null);
  const [busy, setBusy] = useState(false);
  const [yearFilter, setYearFilter] = useState("all");
  const [lightbox, setLightbox] = useState(null);

  const byDate = useMemo(() => marketByDate(market), [market]);

  function normalizeEntry(m) {
    return {
      date: m.date, vnindex: m.vnindex ?? "", change: m.change ?? "", volume: m.volume ?? "",
      foreignNet: m.foreignNet ?? "", breadth: m.breadth ?? "", mood: m.mood ?? 3, discipline: m.discipline ?? 3,
      note: m.note ?? "", images: m.images || (m.image ? [m.image] : []),
    };
  }

  function selectHeatmapDay(date) {
    const existing = byDate[date];
    if (existing) {
      setEditingId(existing.id);
      setForm(normalizeEntry(existing));
    } else {
      setEditingId(null);
      setForm({ ...empty, date });
    }
    window.scrollTo({ top: 0, behavior: "smooth" });
  }

  async function handleImages(e) {
    const files = Array.from(e.target.files || []).slice(0, 4 - form.images.length);
    if (!files.length) return;
    setBusy(true);
    try {
      const urls = await Promise.all(files.map((f) => compressImage(f, 640, 0.75)));
      setForm((f) => ({ ...f, images: [...f.images, ...urls].slice(0, 4) }));
    } finally {
      setBusy(false);
    }
  }
  function removeImage(idx) {
    setForm((f) => ({ ...f, images: f.images.filter((_, i) => i !== idx) }));
  }

  function save() {
    if (!form.date) return;
    if (editingId) {
      setMarket((prev) => prev.map((m) => (m.id === editingId ? { ...form, id: editingId } : m)));
    } else if (byDate[form.date]) {
      setMarket((prev) => prev.map((m) => (m.date === form.date ? { ...form, id: m.id } : m)));
    } else {
      setMarket((prev) => [{ id: Date.now(), ...form }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(m) {
    setEditingId(m.id);
    setForm(normalizeEntry(m));
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(empty);
  }
  function remove(id) {
    setMarket((prev) => prev.filter((m) => m.id !== id));
    if (editingId === id) cancelEdit();
  }

  const years = useMemo(() => {
    const s = new Set(market.map((m) => (m.date || "").slice(0, 4)).filter(Boolean));
    return ["all", ...Array.from(s).sort().reverse()];
  }, [market]);

  const filtered = useMemo(() => {
    return [...market]
      .filter((m) => yearFilter === "all" || (m.date || "").startsWith(yearFilter))
      .sort((a, b) => (a.date < b.date ? 1 : -1));
  }, [market, yearFilter]);

  const columns = [
    { key: "date", label: "Ngay" },
    { key: "vnindex", label: "VNIndex" },
    { key: "change", label: "ThayDoi(%)" },
    { key: "volume", label: "KhoiLuong" },
    { key: "foreignNet", label: "KhoiNgoaiRong" },
    { key: "breadth", label: "DoRong" },
    { key: "mood", label: "TamTrang" },
    { key: "discipline", label: "KyLuat" },
    { key: "note", label: "GhiChu" },
  ];

  function exportCSV() {
    const csv = toCSV(filtered, columns);
    downloadFile(`nhat-ky-thi-truong-tam-ly-${yearFilter}.csv`, csv, "text/csv;charset=utf-8");
  }
  function exportJSON() {
    downloadFile(`nhat-ky-thi-truong-tam-ly-${yearFilter}.json`, JSON.stringify(filtered, null, 2), "application/json");
  }

  return (
    <div>
      <PageHeader title="Nhật ký thị trường & Tâm lý" sub="Ghi nhận diễn biến thị trường và trạng thái tâm lý mỗi ngày trong cùng một nơi — lưu trữ nhiều năm, tải file bất cứ lúc nào." />
      <div className="px-6 md:px-10">
        <Card className="p-5 mb-6">
          <div className="text-sm font-semibold text-slate-200 mb-4 flex items-center gap-2"><Flame size={15} className="text-amber-400" /> Bản đồ nhiệt thị trường & tâm lý</div>
          <FullHeatmap market={market} selected={form.date} onSelect={selectHeatmapDay} />
          <div className="flex items-center gap-4 mt-4 text-[12px] text-slate-400">
            <span>Thấp</span>
            {MOOD_COLORS.slice(1).map((c, i) => <div key={i} className="w-3.5 h-3.5 rounded-sm" style={{ background: c }} />)}
            <span>Cao</span>
            <span className="ml-2 text-slate-500">(bấm một ô để ghi/sửa ngày đó)</span>
          </div>
        </Card>

        <Card className="p-4 mb-6">
          {editingId ? (
            <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
              <Pencil size={12} /> Đang chỉnh sửa ngày {form.date} — <button onClick={cancelEdit} className="underline text-slate-500">hủy</button>
            </div>
          ) : (
            <div className="text-xs text-slate-400 mb-3">Ghi nhận ngày {form.date}</div>
          )}
          <div className="grid grid-cols-2 md:grid-cols-6 gap-3">
            <Input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} />
            <Input placeholder="VNIndex" value={form.vnindex} onChange={(e) => setForm({ ...form, vnindex: e.target.value })} />
            <Input placeholder="Thay đổi %" value={form.change} onChange={(e) => setForm({ ...form, change: e.target.value })} />
            <Input placeholder="Khối lượng (cp)" value={form.volume} onChange={(e) => setForm({ ...form, volume: e.target.value })} />
            <Input placeholder="Khối ngoại ròng (VNĐ)" value={form.foreignNet} onChange={(e) => setForm({ ...form, foreignNet: e.target.value })} />
            <Input placeholder="Độ rộng (tăng/giảm)" value={form.breadth} onChange={(e) => setForm({ ...form, breadth: e.target.value })} />
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
            <Field label={`Tâm trạng (${form.mood}/5)`}>
              <input type="range" min={1} max={5} value={form.mood} onChange={(e) => setForm({ ...form, mood: Number(e.target.value) })} className="w-full accent-amber-400" />
            </Field>
            <Field label={`Kỷ luật tuân thủ kế hoạch (${form.discipline}/5)`}>
              <input type="range" min={1} max={5} value={form.discipline} onChange={(e) => setForm({ ...form, discipline: Number(e.target.value) })} className="w-full accent-amber-400" />
            </Field>
          </div>

          <Textarea
            className="mt-3 text-[15px] leading-relaxed"
            rows={3}
            placeholder="Nhận định thị trường & ghi chú tâm lý trong ngày..."
            value={form.note}
            onChange={(e) => setForm({ ...form, note: e.target.value })}
            style={{ color: "#f1f5f9" }}
          />

          <div className="mt-4">
            <div className="text-xs text-slate-400 mb-2 font-medium">Ảnh minh họa (tối đa 4 ảnh — biểu đồ, chỉ số, tâm trạng...)</div>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-2">
              {form.images.map((img, idx) => (
                <div key={idx} className="relative rounded-md overflow-hidden border" style={{ borderColor: "#263042", background: "#000", height: 110 }}>
                  <img src={img} alt={`ảnh ${idx + 1}`} className="w-full h-full object-contain" />
                  <button onClick={() => removeImage(idx)} className="absolute top-1 right-1 rounded-full p-1" style={{ background: "rgba(10,14,20,0.8)" }}>
                    <X size={12} className="text-slate-300" />
                  </button>
                </div>
              ))}
              {form.images.length < 4 && (
                <label className="flex flex-col items-center justify-center gap-1.5 text-xs rounded-md cursor-pointer border border-dashed" style={{ borderColor: "#263042", height: 110, color: "#94a3b8" }}>
                  <Upload size={16} />
                  {busy ? "Đang tải..." : `Thêm ảnh (${form.images.length}/4)`}
                  <input type="file" accept="image/*" multiple onChange={handleImages} className="hidden" />
                </label>
              )}
            </div>
          </div>

          <div className="flex justify-end mt-4">
            <Btn onClick={save}>{editingId || byDate[form.date] ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Ghi nhận</>}</Btn>
          </div>
        </Card>

        <div className="flex flex-col md:flex-row md:items-center justify-between gap-3 mb-4">
          <div className="flex items-center gap-2">
            <span className="text-xs text-slate-400">Năm:</span>
            <select value={yearFilter} onChange={(e) => setYearFilter(e.target.value)} style={inputStyle} className="px-2 py-1.5 rounded-md text-xs outline-none">
              {years.map((y) => <option key={y} value={y}>{y === "all" ? "Tất cả" : y}</option>)}
            </select>
            <span className="text-xs text-slate-500">{filtered.length} bản ghi</span>
          </div>
          <div className="flex gap-2">
            <Btn variant="ghost" onClick={exportCSV}><Download size={14} /> Tải CSV</Btn>
            <Btn variant="ghost" onClick={exportJSON}><Download size={14} /> Tải JSON</Btn>
          </div>
        </div>

        {filtered.length === 0 ? (
          <Card className="p-10"><EmptyHint text="Chưa có bản ghi nào. Ghi nhận ngày đầu tiên ở trên hoặc bấm vào bản đồ nhiệt." /></Card>
        ) : (
          <div className="space-y-3">
            {filtered.map((m) => {
              const images = m.images || (m.image ? [m.image] : []);
              return (
                <Card key={m.id} className="p-4">
                  <div className="flex flex-col md:flex-row gap-4">
                    <div className="flex-1">
                      <div className="flex items-center gap-4 flex-wrap">
                        <span className="text-xs text-slate-400 font-data">{m.date}</span>
                        {m.vnindex !== "" && <span className="font-data font-semibold text-slate-100 text-base">{fmt(m.vnindex)}</span>}
                        {m.change !== "" && (
                          <span className="text-xs font-data font-semibold" style={{ color: Number(m.change) >= 0 ? "#34d399" : "#f87171" }}>
                            {Number(m.change) >= 0 ? "+" : ""}{m.change}%
                          </span>
                        )}
                        {m.volume && <span className="text-xs text-slate-300">KL: {fmtVol(m.volume)}</span>}
                        {m.foreignNet && <span className="text-xs text-slate-300">KN ròng: {fmtBillion(m.foreignNet)}</span>}
                        {m.breadth && <span className="text-xs px-2 py-0.5 rounded-full" style={{ background: "#1a2130", color: "#cbd5e1" }}>{m.breadth}</span>}
                        {m.mood !== undefined && (
                          <span className="text-xs px-2 py-0.5 rounded-full flex items-center gap-1" style={{ background: "#1a2130", color: "#cbd5e1" }}>
                            <span className="w-2 h-2 rounded-sm" style={{ background: scoreColor((Number(m.mood) + Number(m.discipline)) / 2) }} />
                            Tâm lý {m.mood}/5 · Kỷ luật {m.discipline}/5
                          </span>
                        )}
                      </div>
                      {m.note && <div className="text-[15px] text-slate-200 leading-relaxed mt-2">{m.note}</div>}
                    </div>
                    <div className="flex gap-3 self-start">
                      <button onClick={() => startEdit(m)} className="text-slate-500 hover:text-amber-400"><Pencil size={15} /></button>
                      <button onClick={() => remove(m.id)} className="text-slate-500 hover:text-red-400"><Trash2 size={15} /></button>
                    </div>
                  </div>
                  {images.length > 0 && (
                    <div className="grid grid-cols-2 md:grid-cols-4 gap-2 mt-3">
                      {images.map((img, idx) => (
                        <div
                          key={idx}
                          className="rounded-md overflow-hidden border cursor-pointer relative group"
                          style={{ borderColor: "#263042", background: "#000", height: 130 }}
                          onClick={() => setLightbox({ image: img, caption: `${m.date}${m.vnindex !== "" ? " · VNIndex " + fmt(m.vnindex) : ""}${m.note ? " · " + m.note : ""}` })}
                        >
                          <img src={img} alt={`${m.date} ảnh ${idx + 1}`} className="w-full h-full object-contain" />
                          <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity" style={{ background: "rgba(10,14,20,0.5)" }}>
                            <Sparkles size={14} className="text-amber-300" />
                          </div>
                        </div>
                      ))}
                    </div>
                  )}
                </Card>
              );
            })}
          </div>
        )}
      </div>
      <ImageLightbox image={lightbox?.image} caption={lightbox?.caption} onClose={() => setLightbox(null)} />
    </div>
  );
}

function MiniHeatmap({ market }) {
  const map = marketByDate(market);
  const days = buildDays(90);
  const weeks = [];
  for (let i = 0; i < days.length; i += 7) weeks.push(days.slice(i, i + 7));
  return (
    <div className="flex gap-1 overflow-x-auto pb-1">
      {weeks.map((w, wi) => (
        <div key={wi} className="flex flex-col gap-1">
          {w.map((d) => {
            const e = map[d];
            const avg = e && e.mood !== undefined ? (Number(e.mood) + Number(e.discipline)) / 2 : undefined;
            return <div key={d} title={d} className="w-3 h-3 rounded-sm" style={{ background: scoreColor(avg) }} />;
          })}
        </div>
      ))}
    </div>
  );
}

function FullHeatmap({ market, selected, onSelect }) {
  const map = marketByDate(market);
  const days = buildDays(98);
  const weeks = [];
  for (let i = 0; i < days.length; i += 7) weeks.push(days.slice(i, i + 7));
  return (
    <div className="flex gap-1 overflow-x-auto pb-2">
      {weeks.map((w, wi) => (
        <div key={wi} className="flex flex-col gap-1">
          {w.map((d) => {
            const e = map[d];
            const avg = e && e.mood !== undefined ? (Number(e.mood) + Number(e.discipline)) / 2 : undefined;
            const isSel = d === selected;
            return (
              <button
                key={d}
                title={`${d}${e && e.mood !== undefined ? ` — Tâm trạng ${e.mood}/5, Kỷ luật ${e.discipline}/5` : ""}`}
                onClick={() => onSelect(d)}
                className="w-4 h-4 rounded-sm"
                style={{ background: scoreColor(avg), outline: isSel ? "2px solid #fbbf24" : "none", outlineOffset: 1 }}
              />
            );
          })}
        </div>
      ))}
    </div>
  );
}

/* ---------------------------------------------------------
   PHƯƠNG PHÁP & BỘ LỌC (SMC · ICT · Wyckoff)
--------------------------------------------------------- */
const SMC_WORKFLOW = [
  {
    step: 1,
    title: "Xác định cấu trúc",
    long: "Trên khung lớn (D1/W1 với chứng khoán, H4 với vàng/BTC), xác định xu hướng đang tăng: đỉnh sau cao hơn đỉnh trước, đáy sau cao hơn đáy trước (HH–HL). Chỉ tìm lệnh Long khi cấu trúc khung lớn đang tăng, hoặc vừa xuất hiện CHoCH tăng sau 1 nhịp giảm.",
    short: "Trên khung lớn, xác định xu hướng đang giảm: đỉnh sau thấp hơn đỉnh trước, đáy sau thấp hơn đáy trước (LH–LL). Chỉ tìm lệnh Short khi cấu trúc khung lớn đang giảm, hoặc vừa xuất hiện CHoCH giảm sau 1 nhịp tăng.",
  },
  {
    step: 2,
    title: "Tìm thanh khoản — đã quét chưa?",
    long: "Chờ giá quét thanh khoản dưới đáy gần nhất (sweep low) — giá xuyên qua đáy cũ trong chốc lát để 'săn' lệnh dừng lỗ của phe bán rồi bật ngược lên. Đây là điều kiện bắt buộc trước khi tìm điểm vào Long, KHÔNG vào lệnh khi chưa thấy dấu hiệu quét đáy.",
    short: "Chờ giá quét thanh khoản trên đỉnh gần nhất (sweep high) — giá xuyên qua đỉnh cũ trong chốc lát để 'săn' lệnh dừng lỗ của phe mua rồi đảo chiều giảm. Đây là điều kiện bắt buộc trước khi tìm điểm vào Short, KHÔNG vào lệnh khi chưa thấy dấu hiệu quét đỉnh.",
  },
  {
    step: 3,
    title: "Tìm OB / FVG — điểm vào lệnh",
    long: "Sau khi giá quét đáy xong, tìm Order Block tăng gần nhất (cây nến giảm cuối cùng trước đợt tăng mạnh phá cấu trúc) hoặc Fair Value Gap tăng phía trên điểm quét. Đặt lệnh Long tại vùng đó, đặt SL ngay dưới đáy vừa bị quét, TP tại đỉnh thanh khoản kế tiếp.",
    short: "Sau khi giá quét đỉnh xong, tìm Order Block giảm gần nhất (cây nến tăng cuối cùng trước đợt giảm mạnh phá cấu trúc) hoặc Fair Value Gap giảm phía dưới điểm quét. Đặt lệnh Short tại vùng đó, đặt SL ngay trên đỉnh vừa bị quét, TP tại đáy thanh khoản kế tiếp.",
  },
];

const KNOWLEDGE = [
  {
    group: "Khung phân tích tổng quát DIAKIM System",
    items: [
      {
        title: "Chuỗi phân tích Top-Down",
        body: "MACRO → MARKET REGIME → MONEY FLOW → STOCK STRENGTH → MARKET STRUCTURE → LIQUIDITY → SMC/ICT → EXECUTION. Luôn đi từ bối cảnh lớn (vĩ mô) xuống chi tiết (điểm vào lệnh), không phân tích chart trước khi có bias vĩ mô.",
      },
      {
        title: "Công thức tổng quát vào lệnh",
        body: "NARRATIVE → BIAS → LIQUIDITY → SWEEP → STRUCTURE SHIFT → DISPLACEMENT → OB/FVG → PREMIUM/DISCOUNT → ENTRY → TARGET LIQUIDITY → RISK MANAGEMENT. Đây là xương sống của mọi quyết định vào lệnh trong hệ thống.",
      },
      {
        title: "Macro / Narrative",
        body: "Theo dõi lãi suất, lạm phát, GDP, tỷ giá, thanh khoản hệ thống, dòng vốn, tâm lý Risk-on/Risk-off. Mục tiêu là xác định bối cảnh và bias trước khi phân tích biểu đồ — không giao dịch ngược dòng vĩ mô mà không có lý do rõ ràng.",
      },
      {
        title: "Market Regime — Chế độ thị trường",
        body: "Phân loại thị trường đang ở trạng thái nào: Bull Market, Bear Market, Sideway, Accumulation (tích lũy) hay Distribution (phân phối). Mỗi chế độ đòi hỏi cách tiếp cận entry/quản trị rủi ro khác nhau.",
      },
      {
        title: "Money Flow & Sector Rotation",
        body: "Phân tích dòng tiền vào/ra thị trường, sự luân chuyển giữa các nhóm ngành, sức mạnh tương đối của từng ngành, và cổ phiếu nào đang mạnh/yếu hơn VN-Index. Ưu tiên các mã thuộc nhóm ngành đang được dòng tiền lớn ưu ái.",
      },
      {
        title: "SMT Divergence — Phân kỳ liên thị trường",
        body: "So sánh các tài sản/chỉ số có tương quan: VN-Index vs VN30, VN30 vs nhóm dẫn dắt, Bank Index vs VN-Index, cổ phiếu vs ngành của nó. Khi có phân kỳ (1 bên tạo đỉnh/đáy mới nhưng bên kia không xác nhận), đó là bằng chứng bổ sung củng cố nhận định — SMT KHÔNG phải tín hiệu vào lệnh độc lập, chỉ dùng để tăng độ tin cậy.",
      },
      {
        title: "Market Maker Model / Power of 3",
        body: "Mô hình 3 giai đoạn: Accumulation (Tích lũy) → Manipulation (Thao túng/quét thanh khoản) → Distribution (Phân phối/xu hướng thật). Ví dụ: giá tích lũy đi ngang → quét thanh khoản hai bên để gom lệnh → displacement mạnh → xu hướng thật bắt đầu. Hiểu mô hình này giúp tránh bị 'lừa' bởi các cú di chuyển giả trong giai đoạn thao túng.",
      },
      {
        title: "Nguyên tắc hệ thống — Không được làm",
        body: "Không mua mọi Order Block, không mua mọi FVG, không giao dịch ngược HTF bias mà không có lý do xác đáng, không dùng quá nhiều khái niệm cùng lúc gây rối loạn quyết định. Chỉ hành động khi đủ chuỗi: BIAS → LIQUIDITY → SWEEP → MSS → DISPLACEMENT → OB/FVG → PREMIUM/DISCOUNT → ENTRY → TARGET LIQUIDITY → RISK MANAGEMENT.",
      },
    ],
  },
  {
    group: "Cấu trúc thị trường (SMC/ICT)",
    items: [
      {
        title: "Market Structure — Cấu trúc thị trường",
        body: "Xu hướng tăng khi giá tạo đỉnh sau cao hơn đỉnh trước và đáy sau cao hơn đáy trước (HH–HL). Xu hướng giảm khi tạo LH–LL. Luôn xác định cấu trúc trên khung lớn (D1/W1) trước, rồi mới soi khung nhỏ (H1/H4) để tìm điểm vào lệnh cùng chiều. Với cổ phiếu VN: dùng khung Tuần để xác định xu hướng chính, khung Ngày để vào lệnh. Với vàng/BTC: dùng H4 xác định xu hướng, M15–H1 để vào lệnh vì biến động nhanh hơn.",
      },
      {
        title: "BOS & CHoCH — Phá vỡ cấu trúc & Đổi tính chất",
        body: "BOS (Break of Structure): giá phá vỡ đỉnh/đáy gần nhất theo đúng chiều xu hướng hiện tại — xác nhận xu hướng tiếp diễn. CHoCH (Change of Character): giá phá vỡ theo chiều ngược xu hướng — tín hiệu sớm cho khả năng đảo chiều, cần cẩn trọng trước khi vào lệnh ngược dòng.",
      },
      {
        title: "Liquidity — Thanh khoản",
        body: "Giá thường bị 'hút' về các vùng có nhiều lệnh dừng lỗ tập trung (đỉnh/đáy cũ, vùng đi ngang). Trước khi đảo chiều thật, giá hay quét thanh khoản (liquidity sweep) qua đỉnh/đáy đó để 'săn' lệnh dừng lỗ của đám đông rồi mới quay đầu. Với chứng khoán VN cần lưu ý biên độ ±7% (HOSE) làm giới hạn thanh khoản trong phiên.",
      },
      {
        title: "Order Block — Vùng lệnh lớn",
        body: "Là cây nến cuối cùng ngược chiều trước khi giá di chuyển mạnh (impulsive move) phá cấu trúc. Đây là dấu vết dòng tiền lớn (tổ chức/cá mập) đã vào lệnh. Giá thường quay lại kiểm tra (retest) vùng này trước khi đi tiếp — là vùng entry có xác suất cao khi kết hợp thêm các yếu tố khác.",
      },
      {
        title: "Fair Value Gap (FVG) — Khoảng trống giá trị",
        body: "Khoảng trống giữa 3 cây nến liên tiếp khi giá di chuyển quá nhanh, để lại 'lỗ hổng' chưa được giao dịch hai chiều. Giá có xu hướng quay lại lấp (mitigate) FVG trước khi tiếp tục xu hướng — có thể dùng làm vùng vào lệnh bổ sung cho Order Block.",
      },
      {
        title: "Premium / Discount Zone — Vùng đắt / rẻ",
        body: "Chia biên độ dao động (từ đáy tới đỉnh gần nhất) theo Fibonacci: vùng dưới 50% là Discount (rẻ, ưu tiên tìm lệnh Mua), vùng trên 50% là Premium (đắt, ưu tiên tìm lệnh Bán). Chỉ nên vào lệnh Mua ở vùng Discount và lệnh Bán ở vùng Premium để có tỷ lệ R:R tốt.",
      },
      {
        title: "Kill Zone — Khung giờ vàng (ICT)",
        body: "Với vàng/BTC (giao dịch 24/24): phiên London (14h–17h VN) và phiên New York (19h30–22h VN) thường biến động mạnh, thanh khoản cao — ưu tiên tìm setup trong khung giờ này. Với chứng khoán VN: 30 phút đầu phiên (9h–9h30) và 30 phút cuối phiên (14h–14h30) thường có biến động và thanh khoản lớn nhất.",
      },
    ],
  },
  {
    group: "Phương pháp Wyckoff",
    items: [
      {
        title: "4 giai đoạn thị trường Wyckoff",
        body: "Tích lũy (Accumulation): dòng tiền lớn gom hàng sau xu hướng giảm, giá đi ngang. Tăng giá (Markup): giá bứt phá khỏi vùng tích lũy, xu hướng tăng rõ ràng. Phân phối (Distribution): dòng tiền lớn xả hàng sau xu hướng tăng, giá đi ngang ở vùng đỉnh. Giảm giá (Markdown): giá phá vỡ vùng phân phối, xu hướng giảm.",
      },
      {
        title: "Spring & Upthrust — Cú hất giả",
        body: "Spring: giá phá đáy vùng tích lũy trong chốc lát rồi bật lại mạnh — 'bẫy' người bán hoảng loạn, dấu hiệu dòng tiền lớn chuẩn bị đẩy giá lên. Upthrust: ngược lại, giá phá đỉnh vùng phân phối rồi rơi mạnh trở lại — bẫy người mua FOMO, báo hiệu khả năng giảm.",
      },
      {
        title: "SOS / SOW / LPS / LPSY",
        body: "SOS (Sign of Strength): cây nến tăng mạnh kèm khối lượng lớn, xác nhận sức mạnh phe mua sau Spring. LPS (Last Point of Support): nhịp hồi về kiểm tra lại vùng hỗ trợ trước khi tăng tiếp — điểm vào lệnh Mua đẹp. SOW (Sign of Weakness) và LPSY (Last Point of Supply) là các khái niệm đối xứng cho phe bán trong giai đoạn Phân phối/Giảm giá.",
      },
      {
        title: "Effort vs Result — Nỗ lực và Kết quả (Volume)",
        body: "So sánh khối lượng giao dịch (nỗ lực) với biến động giá (kết quả). Khối lượng lớn nhưng giá đi rất ít là dấu hiệu hấp thụ (absorption) — dòng tiền lớn đang âm thầm gom/xả, thường xuất hiện trước các điểm đảo chiều quan trọng.",
      },
    ],
  },
  {
    group: "Chỉ báo kết hợp & Chu kỳ sóng cá nhân",
    items: [
      {
        title: "RSI — vùng quá mua / quá bán",
        body: "RSI ≥ 70 là vùng quá mua (tiềm ẩn khả năng điều chỉnh giảm), RSI ≤ 30 là vùng quá bán (tiềm ẩn khả năng hồi tăng). Dùng làm bộ lọc xác nhận thêm cho tín hiệu SMC/Wyckoff — không dùng đơn lẻ, vì RSI có thể duy trì quá mua/quá bán lâu trong xu hướng mạnh.",
      },
      {
        title: "Fibonacci 0.618–0.786 — Vùng bật giá (OTE)",
        body: "Đây là vùng thoái lui sâu (Optimal Trade Entry theo ICT), nơi giá thường bật lại mạnh nhất trong 1 nhịp điều chỉnh trước khi tiếp diễn xu hướng chính. Kết hợp với Order Block/FVG nằm đúng trong vùng 0.618–0.786 sẽ cho điểm entry có xác suất cao hơn.",
      },
      {
        title: "Chu kỳ sóng cá nhân (thống kê riêng)",
        body: "Theo thống kê cá nhân của bạn: sóng giảm thường kéo dài khoảng 2 tháng, sóng tăng thường kéo dài 2–4 tháng. Khi 1 nhịp sóng đã đi đủ hoặc vượt thời gian trung bình này, xác suất chuẩn bị đảo chiều/kết thúc nhịp sóng sẽ tăng lên — dùng làm yếu tố xác nhận thời điểm, kết hợp cùng RSI, Fibonacci và các tín hiệu SMC/Wyckoff khác, không dùng làm tín hiệu vào lệnh độc lập.",
      },
    ],
  },
  {
    group: "Ghi chú riêng theo thị trường",
    items: [
      {
        title: "Chứng khoán Việt Nam",
        body: "Thanh khoản thấp hơn, dễ bị 'kê' giá bởi dòng tiền lớn ở các mã vốn hoá nhỏ — ưu tiên áp dụng SMC/Wyckoff cho nhóm vốn hoá vừa/lớn có thanh khoản ổn định. Biên độ dao động ±7% (HOSE)/±10% (HNX)/±15% (UPCOM) khiến các vùng liquidity hình thành nhanh trong phiên. Chú ý chu kỳ T+2.5 khi tính điểm hoà vốn/chốt lời.",
      },
      {
        title: "Vàng (Gold/XAUUSD)",
        body: "Chịu ảnh hưởng mạnh bởi tin tức vĩ mô (lãi suất Fed, CPI, DXY) — nên kiểm tra lịch kinh tế trước khi vào lệnh theo SMC/ICT vì tin tức có thể phá vỡ cấu trúc kỹ thuật bất ngờ. Kill zone London/New York thường cho setup rõ ràng nhất.",
      },
      {
        title: "Bitcoin (BTC) & Crypto",
        body: "Giao dịch 24/7 nên cấu trúc hình thành liên tục, dễ bị nhiễu bởi tin tức on-chain/quy định. Thanh khoản tập trung nhiều ở các sàn lớn — chú ý vùng thanh lý (liquidation zone) trên các sàn phái sinh, một biến thể hiện đại của khái niệm liquidity trong SMC.",
      },
    ],
  },
];

const MACRO_ITEMS = [
  { id: "rates", label: "Chính sách lãi suất thuận lợi cho rủi ro" },
  { id: "inflation", label: "Lạm phát trong tầm kiểm soát" },
  { id: "gdp", label: "Tăng trưởng GDP tích cực" },
  { id: "fx", label: "Tỷ giá ổn định" },
  { id: "liquiditySys", label: "Thanh khoản hệ thống dồi dào" },
  { id: "capitalFlow", label: "Dòng vốn đang vào ròng" },
];

const REGIME_OPTIONS = ["Bull Market", "Bear Market", "Sideway", "Accumulation (Tích lũy)", "Distribution (Phân phối)"];

const OB_SCORE_ITEMS = [
  { id: "sweep", label: "Liquidity Sweep — đã quét thanh khoản", points: 2 },
  { id: "displacement", label: "Displacement — cú di chuyển mất cân bằng thật", points: 2 },
  { id: "mssBos", label: "MSS / BOS — xác nhận dịch chuyển cấu trúc", points: 2 },
  { id: "fvg", label: "FVG — có khoảng trống giá trị đi kèm", points: 1 },
  { id: "htfAlign", label: "HTF Alignment — đúng chiều xu hướng khung lớn", points: 2 },
  { id: "premiumDiscount", label: "Đúng vùng Premium/Discount", points: 1 },
];
const OB_SCORE_MAX = OB_SCORE_ITEMS.reduce((s, i) => s + i.points, 0);

function computeMacroBias(macro) {
  const count = MACRO_ITEMS.reduce((s, m) => s + (macro[m.id] ? 1 : 0), 0);
  if (count >= 4) return "Risk-on";
  if (count <= 2) return "Risk-off";
  return "Trung tính";
}

function computeOBScore(ob) {
  return OB_SCORE_ITEMS.reduce((s, i) => s + (ob[i.id] ? i.points : 0), 0);
}

function computeDealingZone(range) {
  const high = Number(range.high), low = Number(range.low), current = Number(range.current);
  if (!range.high || !range.low || !range.current || isNaN(high) || isNaN(low) || isNaN(current) || high <= low) return null;
  const pct = Math.max(0, Math.min(100, ((current - low) / (high - low)) * 100));
  let zone = "Equilibrium";
  if (pct >= 55) zone = "Premium";
  else if (pct <= 45) zone = "Discount";
  return { pct: Math.round(pct), zone };
}

function computeRR(risk) {
  const entry = Number(risk.entry), sl = Number(risk.sl), tp = Number(risk.tp);
  if (!risk.entry || !risk.sl || !risk.tp || isNaN(entry) || isNaN(sl) || isNaN(tp)) return null;
  const riskAmt = Math.abs(entry - sl);
  const rewardAmt = Math.abs(tp - entry);
  if (riskAmt === 0) return null;
  return Math.round((rewardAmt / riskAmt) * 100) / 100;
}

function computeDiakimVerdict(f) {
  const obScore = computeOBScore(f.ob);
  const zoneInfo = computeDealingZone(f.dealingRange);
  const rr = computeRR(f.risk);
  const dir = f.direction;

  if (!(f.structure.clear && f.structure.bos)) {
    return { level: "invalid", color: "#f87171", label: "Chưa xác định cấu trúc HTF đúng chiều", detail: "Cần cấu trúc khung lớn rõ ràng và BOS xác nhận trước khi đi tiếp." };
  }
  if (!f.liquidity.identified) {
    return { level: "invalid", color: "#f87171", label: "Chưa xác định vùng thanh khoản mục tiêu", detail: "Xác định buy-side/sell-side liquidity (đỉnh/đáy cũ, Equal High/Low) trước khi chờ sweep." };
  }
  if (!f.liquidity.swept) {
    return { level: "wait", color: "#fbbf24", label: "Chưa quét thanh khoản — chờ thêm", detail: "Liquidity sweep là điều kiện bắt buộc trước MSS và displacement." };
  }
  if (!f.structure.mss) {
    return { level: "wait", color: "#fbbf24", label: "Chưa có MSS/CHoCH xác nhận", detail: "Cần Market Structure Shift rõ ràng sau khi quét thanh khoản." };
  }
  if (!f.displacement) {
    return { level: "wait", color: "#fbbf24", label: "Chưa có Displacement thật", detail: "Phân biệt breakout yếu và displacement thật (nến thân lớn, tốc độ cao, phá cấu trúc, tạo FVG)." };
  }
  if (obScore < 7) {
    return { level: "weak", color: "#fbbf24", label: `OB/FVG chưa đủ điểm chất lượng (${obScore}/${OB_SCORE_MAX})`, detail: "Ưu tiên vùng OB/FVG đạt từ 7/10 điểm trở lên." };
  }
  if (zoneInfo) {
    const aligned = dir === "Long" ? zoneInfo.zone === "Discount" : zoneInfo.zone === "Premium";
    if (!aligned) {
      return { level: "weak", color: "#fbbf24", label: `Điểm vào đang ở vùng ${zoneInfo.zone}, chưa tối ưu cho ${dir}`, detail: dir === "Long" ? "Ưu tiên Buy tại vùng Discount." : "Ưu tiên Sell tại vùng Premium." };
    }
  }
  if (rr !== null && rr < 2) {
    return { level: "weak", color: "#fbbf24", label: `Tỷ lệ R:R = 1:${rr} — chưa đạt tối thiểu 1:2`, detail: "Điều chỉnh Entry/SL/TP để đạt R:R tối thiểu 1:2." };
  }
  return { level: "strong", color: "#34d399", label: "Đủ điều kiện Setup chuẩn DIAKIM System", detail: "Đã đi đủ chuỗi: Bias → Liquidity → Sweep → MSS → Displacement → OB/FVG (≥7 điểm) → Premium/Discount → Risk Management." };
}

async function analyzeChartWithAI(images, direction) {
  const content = [];
  images.forEach((img) => {
    const [meta, data] = img.split(",");
    const mediaType = (meta.match(/data:(.*?);base64/) || [, "image/jpeg"])[1];
    content.push({ type: "image", source: { type: "base64", media_type: mediaType, data } });
  });
  content.push({
    type: "text",
    text: `Bạn là chuyên gia phân tích kỹ thuật theo phương pháp SMC (Smart Money Concepts) và ICT, áp dụng framework: BIAS → LIQUIDITY → SWEEP → STRUCTURE SHIFT (MSS/BOS) → DISPLACEMENT → ORDER BLOCK/FVG → PREMIUM/DISCOUNT. Người dùng đang cân nhắc lệnh chiều "${direction}". Phân tích (các) biểu đồ nến trong ảnh.

Trả lời CHỈ bằng JSON hợp lệ, không markdown, không chữ nào ngoài JSON, đúng định dạng:
{
  "structureClear": true/false,
  "bos": true/false,
  "liquiditySwept": true/false,
  "mss": true/false,
  "displacement": true/false,
  "fvg": true/false,
  "premiumDiscountAligned": true/false,
  "summary": "1-2 câu tóm tắt nhận định tổng quan bằng tiếng Việt, có ủng hộ chiều ${direction} hay không",
  "reasoning": {
    "structureClear": "giải thích ngắn 1 câu",
    "bos": "giải thích ngắn 1 câu",
    "liquiditySwept": "giải thích ngắn 1 câu",
    "mss": "giải thích ngắn 1 câu",
    "displacement": "giải thích ngắn 1 câu",
    "fvg": "giải thích ngắn 1 câu",
    "premiumDiscountAligned": "giải thích ngắn 1 câu, nêu ước lượng vùng Premium/Discount/Equilibrium nếu xác định được"
  }
}

Không đánh giá các yếu tố Macro, Market Regime, Money Flow, SMT Divergence, Risk Management vì không thể xác định từ 1 ảnh biểu đồ — người dùng sẽ tự nhập.`,
  });

  const response = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ model: "claude-sonnet-4-6", max_tokens: 1100, messages: [{ role: "user", content }] }),
  });
  const data = await response.json();
  const text = (data.content || []).map((b) => b.text || "").join("");
  const clean = text.replace(/```json|```/g, "").trim();
  return JSON.parse(clean);
}

function StepToggle({ title, hint, checked, onToggle, size = "normal", points }) {
  return (
    <button
      onClick={onToggle}
      className="w-full flex items-center justify-between gap-3 px-4 py-3 rounded-lg text-left transition-colors"
      style={{ background: checked ? "rgba(52,211,153,0.08)" : "#0a0e14", border: `1px solid ${checked ? "rgba(52,211,153,0.35)" : "#1c2432"}` }}
    >
      <div className="flex-1">
        <div className={size === "small" ? "text-xs text-slate-300" : "text-sm text-slate-200 font-medium"}>
          {title} {points !== undefined && <span className="text-amber-400 font-data">+{points}</span>}
        </div>
        {hint && <div className="text-[11px] text-slate-500 mt-0.5">{hint}</div>}
      </div>
      <span className="flex-shrink-0 w-11 h-6 rounded-full relative transition-colors" style={{ background: checked ? "#34d399" : "#263042" }}>
        <span className="absolute top-0.5 w-5 h-5 rounded-full bg-white transition-all" style={{ left: checked ? 22 : 2 }} />
      </span>
    </button>
  );
}

function SectionCard({ step, title, subtitle, children }) {
  return (
    <Card className="p-5 mb-4">
      <div className="flex items-center gap-2.5 mb-1">
        <span className="w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold flex-shrink-0" style={{ background: "linear-gradient(90deg,#f59e0b,#fbbf24)", color: "#1a1206" }}>{step}</span>
        <span className="text-sm font-semibold text-slate-100">{title}</span>
      </div>
      {subtitle && <div className="text-xs text-slate-500 mb-3 ml-8">{subtitle}</div>}
      <div className="mt-3">{children}</div>
    </Card>
  );
}

function computePretradeChecklist(f) {
  const obScore = computeOBScore(f.ob);
  const zoneInfo = computeDealingZone(f.dealingRange);
  const rr = computeRR(f.risk);
  const dir = f.direction;
  const zoneAligned = !zoneInfo || (dir === "Long" ? zoneInfo.zone === "Discount" : zoneInfo.zone === "Premium");

  return [
    { label: "Cấu trúc khung lớn rõ ràng, đúng chiều lệnh", passed: f.structure.clear && f.structure.bos },
    { label: "Đã xác định vùng thanh khoản mục tiêu", passed: f.liquidity.identified },
    { label: "Giá đã quét thanh khoản (Liquidity Sweep)", passed: f.liquidity.swept },
    { label: "Có MSS / CHoCH xác nhận dịch chuyển cấu trúc", passed: f.structure.mss },
    { label: "Có Displacement thật (không phải breakout yếu)", passed: f.displacement },
    { label: `OB/FVG đạt điểm chất lượng (≥7/${OB_SCORE_MAX})`, passed: obScore >= 7 },
    { label: "Điểm vào đúng vùng Premium/Discount", passed: zoneAligned },
    { label: "Đã có Entry / SL / TP cụ thể", passed: !!(f.risk.entry && f.risk.sl && f.risk.tp) },
    { label: "Tỷ lệ R:R đạt tối thiểu 1:2", passed: rr === null ? false : rr >= 2 },
  ];
}

function MethodScreener({ screener, setScreener }) {
  const [subTab, setSubTab] = useState("filter");
  const [openGroup, setOpenGroup] = useState(-1);

  const emptyForm = {
    symbol: "", market: "Chứng khoán", date: todayISO(), direction: "Long",
    macro: { rates: false, inflation: false, gdp: false, fx: false, liquiditySys: false, capitalFlow: false },
    macroOverride: "auto",
    regime: "Bull Market",
    moneyFlow: { sector: "", relStrength: false },
    structure: { clear: false, bos: false, mss: false },
    liquidity: { identified: false, swept: false },
    displacement: false,
    ob: { sweep: false, displacement: false, mssBos: false, fvg: false, htfAlign: false, premiumDiscount: false },
    dealingRange: { high: "", low: "", current: "" },
    smt: { confirmed: false, note: "" },
    risk: { entry: "", sl: "", tp: "" },
    note: "", images: [],
  };
  const [form, setForm] = useState(emptyForm);
  const [editingId, setEditingId] = useState(null);
  const [uploading, setUploading] = useState(false);
  const [analyzing, setAnalyzing] = useState(false);
  const [aiResult, setAiResult] = useState(null);
  const [aiError, setAiError] = useState("");

  const macroBiasAuto = computeMacroBias(form.macro);
  const effectiveBias = form.macroOverride === "auto" ? macroBiasAuto : form.macroOverride;
  const obScore = computeOBScore(form.ob);
  const zoneInfo = computeDealingZone(form.dealingRange);
  const rr = computeRR(form.risk);
  const verdict = computeDiakimVerdict(form);

  function set(path, value) {
    setForm((f) => {
      const next = { ...f };
      let obj = next;
      for (let i = 0; i < path.length - 1; i++) {
        obj[path[i]] = { ...obj[path[i]] };
        obj = obj[path[i]];
      }
      obj[path[path.length - 1]] = value;
      return next;
    });
  }
  function toggle(path) {
    setForm((f) => {
      let cur = f;
      for (const p of path) cur = cur[p];
      set(path, !cur);
      return f;
    });
  }

  async function handleImages(e) {
    const files = Array.from(e.target.files || []).slice(0, 2 - form.images.length);
    if (!files.length) return;
    setUploading(true);
    try {
      const urls = await Promise.all(files.map((f) => compressImage(f, 900, 0.82)));
      setForm((f) => ({ ...f, images: [...f.images, ...urls].slice(0, 2) }));
    } finally {
      setUploading(false);
    }
  }
  function removeImage(idx) {
    setForm((f) => ({ ...f, images: f.images.filter((_, i) => i !== idx) }));
    setAiResult(null);
  }

  async function runAIAnalysis() {
    if (form.images.length === 0) return;
    setAnalyzing(true);
    setAiError("");
    setAiResult(null);
    try {
      const r = await analyzeChartWithAI(form.images, form.direction);
      setForm((f) => ({
        ...f,
        structure: { ...f.structure, clear: r.structureClear ?? f.structure.clear, bos: r.bos ?? f.structure.bos, mss: r.mss ?? f.structure.mss },
        liquidity: { identified: (r.liquiditySwept ?? f.liquidity.swept) ? true : f.liquidity.identified, swept: r.liquiditySwept ?? f.liquidity.swept },
        displacement: r.displacement ?? f.displacement,
        ob: {
          ...f.ob,
          sweep: r.liquiditySwept ?? f.ob.sweep,
          displacement: r.displacement ?? f.ob.displacement,
          mssBos: (r.mss || r.bos) ?? f.ob.mssBos,
          fvg: r.fvg ?? f.ob.fvg,
          htfAlign: (r.structureClear && r.bos) ?? f.ob.htfAlign,
          premiumDiscount: r.premiumDiscountAligned ?? f.ob.premiumDiscount,
        },
      }));
      setAiResult(r);
    } catch (err) {
      setAiError("Không thể phân tích ảnh lúc này, thử lại sau hoặc nhập thủ công.");
    }
    setAnalyzing(false);
  }

  function save() {
    if (!form.symbol.trim()) return;
    const entry = { ...form, symbol: form.symbol.toUpperCase(), verdictLabel: verdict.label, verdictLevel: verdict.level, obScore };
    if (editingId) {
      setScreener((prev) => prev.map((s) => (s.id === editingId ? { ...entry, id: editingId } : s)));
    } else {
      setScreener((prev) => [{ id: Date.now(), ...entry }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(s) {
    setEditingId(s.id);
    setForm({ ...emptyForm, ...s });
    setAiResult(null);
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(emptyForm);
    setAiResult(null);
    setAiError("");
  }
  function remove(id) {
    setScreener((prev) => prev.filter((s) => s.id !== id));
    if (editingId === id) cancelEdit();
  }

  return (
    <div>
      <PageHeader title="DIAKIM Market Structure System" sub="Phân tích top-down: Macro → Market Regime → Money Flow → Market Structure → Liquidity → SMC/ICT → Execution." />
      <div className="px-6 md:px-10">
        <div className="flex gap-2 mb-6">
          <button onClick={() => setSubTab("filter")} className="px-4 py-2 rounded-md text-sm font-medium flex items-center gap-2" style={subTab === "filter" ? { background: "linear-gradient(90deg,#7c3aed,#a855f7)", color: "#fff" } : { background: "#0d1119", color: "#94a3b8", border: "1px solid #1c2432" }}>
            <Gauge size={15} /> Phân tích Top-Down
          </button>
          <button onClick={() => setSubTab("knowledge")} className="px-4 py-2 rounded-md text-sm font-medium flex items-center gap-2" style={subTab === "knowledge" ? { background: "linear-gradient(90deg,#7c3aed,#a855f7)", color: "#fff" } : { background: "#0d1119", color: "#94a3b8", border: "1px solid #1c2432" }}>
            <Layers size={15} /> Kiến thức khung DIAKIM
          </button>
        </div>

        {subTab === "knowledge" && (
          <div className="space-y-4">
            <Card className="p-5" style={{ background: "linear-gradient(135deg, #1d1033 0%, #150a24 60%, #0d1119 100%)", border: "1px solid #2a1840" }}>
              <div className="text-sm font-semibold text-amber-300 mb-1 flex items-center gap-2"><Compass size={15} /> Quy trình vào lệnh SMC/ICT — 3 bước cốt lõi</div>
              <div className="text-xs text-slate-500 mb-4">Phần lõi của bước Structure → Liquidity → Entry trong khung phân tích Top-Down đầy đủ bên trên.</div>
              <div className="space-y-3">
                {SMC_WORKFLOW.map((s) => (
                  <div key={s.step} className="rounded-lg p-4" style={{ background: "#0a0e14", border: "1px solid #1c2432" }}>
                    <div className="flex items-center gap-2.5 mb-3">
                      <span className="w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold flex-shrink-0" style={{ background: "linear-gradient(90deg,#f59e0b,#fbbf24)", color: "#1a1206" }}>{s.step}</span>
                      <span className="text-sm font-semibold text-slate-100">{s.title}</span>
                    </div>
                    <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
                      <div className="rounded-md p-3" style={{ background: "rgba(52,211,153,0.06)", border: "1px solid rgba(52,211,153,0.2)" }}>
                        <div className="text-[11px] font-semibold uppercase tracking-wide mb-1.5 flex items-center gap-1.5" style={{ color: "#34d399" }}><TrendingUp size={12} /> Long (Mua)</div>
                        <div className="text-[13px] text-slate-300 leading-relaxed">{s.long}</div>
                      </div>
                      <div className="rounded-md p-3" style={{ background: "rgba(248,113,113,0.06)", border: "1px solid rgba(248,113,113,0.2)" }}>
                        <div className="text-[11px] font-semibold uppercase tracking-wide mb-1.5 flex items-center gap-1.5" style={{ color: "#f87171" }}><TrendingDown size={12} /> Short (Bán)</div>
                        <div className="text-[13px] text-slate-300 leading-relaxed">{s.short}</div>
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </Card>

            {KNOWLEDGE.map((g, gi) => (
              <Card key={gi} className="overflow-hidden">
                <button onClick={() => setOpenGroup(openGroup === gi ? -1 : gi)} className="w-full p-4 flex items-center justify-between text-left">
                  <span className="text-sm font-semibold text-amber-300">{g.group}</span>
                  {openGroup === gi ? <ChevronUp size={16} className="text-slate-500" /> : <ChevronDown size={16} className="text-slate-500" />}
                </button>
                {openGroup === gi && (
                  <div className="px-4 pb-4 space-y-4" style={{ borderTop: "1px solid #1c2432" }}>
                    {g.items.map((it, ii) => (
                      <div key={ii} className="pt-4">
                        <div className="text-sm font-medium text-slate-200 mb-1.5">{it.title}</div>
                        <div className="text-[13.5px] text-slate-400 leading-relaxed">{it.body}</div>
                      </div>
                    ))}
                  </div>
                )}
              </Card>
            ))}
          </div>
        )}

        {subTab === "filter" && (
          <>
            <Card className="p-5 mb-4" style={{ background: "linear-gradient(135deg, #1d1033 0%, #150a24 55%, #0d1119 100%)", border: "1px solid #2a1840" }}>
              <div className="flex items-center justify-between mb-3">
                <div className="text-sm font-semibold text-amber-300 flex items-center gap-2"><ShieldCheck size={15} /> Việc cần làm trước khi vào lệnh</div>
                {(() => {
                  const list = computePretradeChecklist(form);
                  const done = list.filter((i) => i.passed).length;
                  return <span className="text-xs font-data" style={{ color: done === list.length ? "#34d399" : "#94a3b8" }}>{done}/{list.length}</span>;
                })()}
              </div>
              <div className="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-1.5">
                {computePretradeChecklist(form).map((item, i) => (
                  <div key={i} className="flex items-center gap-2 text-xs py-1">
                    <span className="w-4 h-4 rounded-full flex items-center justify-center flex-shrink-0 text-[10px] font-bold" style={{ background: item.passed ? "#34d399" : "#2a1840", color: item.passed ? "#0a0e14" : "#64748b" }}>
                      {item.passed ? "✓" : i + 1}
                    </span>
                    <span style={{ color: item.passed ? "#cbd5e1" : "#64748b" }}>{item.label}</span>
                  </div>
                ))}
              </div>
            </Card>

            <Card className="p-5 mb-4">
              {editingId && (
                <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
                  <Pencil size={12} /> Đang chỉnh sửa {form.symbol} — <button onClick={cancelEdit} className="underline text-slate-500">hủy</button>
                </div>
              )}
              <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
                <Input placeholder="Mã / Cặp (VD: HPG, XAUUSD, BTC)" value={form.symbol} onChange={(e) => setForm({ ...form, symbol: e.target.value })} />
                <select value={form.market} onChange={(e) => setForm({ ...form, market: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
                  <option>Chứng khoán</option><option>Vàng</option><option>BTC/Crypto</option>
                </select>
                <Input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} />
                <div className="flex rounded-md overflow-hidden border" style={{ borderColor: "#263042" }}>
                  <button onClick={() => setForm({ ...form, direction: "Long" })} className="flex-1 text-sm font-medium py-2 flex items-center justify-center gap-1" style={{ background: form.direction === "Long" ? "#34d39922" : "transparent", color: form.direction === "Long" ? "#34d399" : "#64748b" }}>
                    <TrendingUp size={14} /> Long
                  </button>
                  <button onClick={() => setForm({ ...form, direction: "Short" })} className="flex-1 text-sm font-medium py-2 flex items-center justify-center gap-1" style={{ background: form.direction === "Short" ? "#f8717122" : "transparent", color: form.direction === "Short" ? "#f87171" : "#64748b" }}>
                    <TrendingDown size={14} /> Short
                  </button>
                </div>
              </div>
            </Card>

            <SectionCard step={1} title="Macro / Narrative" subtitle="Xác định bối cảnh vĩ mô và bias trước khi phân tích chart">
              <div className="grid grid-cols-1 md:grid-cols-2 gap-2 mb-3">
                {MACRO_ITEMS.map((m) => (
                  <StepToggle key={m.id} title={m.label} checked={form.macro[m.id]} onToggle={() => set(["macro", m.id], !form.macro[m.id])} size="small" />
                ))}
              </div>
              <div className="flex items-center gap-3">
                <span className="text-xs text-slate-500">Bias vĩ mô:</span>
                <select value={form.macroOverride} onChange={(e) => setForm({ ...form, macroOverride: e.target.value })} style={inputStyle} className="px-2 py-1.5 rounded-md text-xs outline-none">
                  <option value="auto">Tự động ({macroBiasAuto})</option>
                  <option value="Risk-on">Risk-on (ép buộc)</option>
                  <option value="Risk-off">Risk-off (ép buộc)</option>
                </select>
                <span className="text-xs font-data px-2 py-1 rounded-full" style={{ background: effectiveBias === "Risk-on" ? "#34d39922" : effectiveBias === "Risk-off" ? "#f8717122" : "#94a3b822", color: effectiveBias === "Risk-on" ? "#34d399" : effectiveBias === "Risk-off" ? "#f87171" : "#94a3b8" }}>
                  {effectiveBias}
                </span>
              </div>
            </SectionCard>

            <SectionCard step={2} title="Market Regime" subtitle="Thị trường đang ở chế độ nào">
              <select value={form.regime} onChange={(e) => setForm({ ...form, regime: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
                {REGIME_OPTIONS.map((r) => <option key={r}>{r}</option>)}
              </select>
            </SectionCard>

            <SectionCard step={3} title="Money Flow & Sector Rotation" subtitle="Dòng tiền và sức mạnh tương đối (đặc biệt quan trọng với chứng khoán VN)">
              <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
                <Input placeholder="Nhóm ngành đang được dòng tiền ưu ái" value={form.moneyFlow.sector} onChange={(e) => set(["moneyFlow", "sector"], e.target.value)} />
                <StepToggle title="Mã đang mạnh hơn VN-Index" checked={form.moneyFlow.relStrength} onToggle={() => set(["moneyFlow", "relStrength"], !form.moneyFlow.relStrength)} size="small" />
              </div>
            </SectionCard>

            <SectionCard step={4} title="Market Structure" subtitle="HTF bias và chất lượng cấu trúc, không chỉ nhìn phá đỉnh/đáy đơn thuần">
              <div className="space-y-2">
                <StepToggle title="Cấu trúc khung lớn rõ ràng đúng chiều" hint={form.direction === "Long" ? "HH → HL → HH → HL" : "LL → LH → LL → LH"} checked={form.structure.clear} onToggle={() => set(["structure", "clear"], !form.structure.clear)} />
                <StepToggle title="BOS — Break of Structure xác nhận" checked={form.structure.bos} onToggle={() => set(["structure", "bos"], !form.structure.bos)} />
                <StepToggle title="MSS / CHoCH — dịch chuyển cấu trúc" checked={form.structure.mss} onToggle={() => set(["structure", "mss"], !form.structure.mss)} />
              </div>
            </SectionCard>

            <SectionCard step={5} title="Liquidity" subtitle={form.direction === "Long" ? "Sell-side liquidity: đáy cũ, Equal Low, Swing Low, SL phe mua, Sell Stop" : "Buy-side liquidity: đỉnh cũ, Equal High, Swing High, SL phe bán, Buy Stop"}>
              <div className="space-y-2">
                <StepToggle title="Đã xác định vùng thanh khoản mục tiêu" checked={form.liquidity.identified} onToggle={() => set(["liquidity", "identified"], !form.liquidity.identified)} />
                <StepToggle title="Giá đã Sweep vùng thanh khoản đó" hint="Liquidity Sweep không tự động đồng nghĩa đảo chiều — cần MSS + Displacement xác nhận thêm" checked={form.liquidity.swept} onToggle={() => set(["liquidity", "swept"], !form.liquidity.swept)} />
              </div>
            </SectionCard>

            <SectionCard step={6} title="Displacement" subtitle="Phân biệt breakout yếu và displacement thật">
              <StepToggle title="Có cú di chuyển mất cân bằng thật" hint="Nến thân lớn, tốc độ cao, phá cấu trúc, tạo FVG" checked={form.displacement} onToggle={() => setForm({ ...form, displacement: !form.displacement })} />
            </SectionCard>

            <Card className="p-5 mb-4">
              <div className="text-xs uppercase tracking-wide text-slate-500 font-medium mb-3">Ảnh biểu đồ nến (tối đa 2 ảnh) — AI hỗ trợ điền nhanh bước 4–8</div>
              <div className="grid grid-cols-2 gap-2">
                {form.images.map((img, idx) => (
                  <div key={idx} className="relative rounded-md overflow-hidden border" style={{ borderColor: "#263042", background: "#000", height: 150 }}>
                    <img src={img} alt={`biểu đồ ${idx + 1}`} className="w-full h-full object-contain" />
                    <button onClick={() => removeImage(idx)} className="absolute top-1 right-1 rounded-full p-1" style={{ background: "rgba(10,14,20,0.8)" }}><X size={12} className="text-slate-300" /></button>
                  </div>
                ))}
                {form.images.length < 2 && (
                  <label className="flex flex-col items-center justify-center gap-1.5 text-xs rounded-md cursor-pointer border border-dashed" style={{ borderColor: "#263042", height: 150, color: "#94a3b8" }}>
                    <Upload size={16} />
                    {uploading ? "Đang tải..." : `Tải ảnh biểu đồ (${form.images.length}/2)`}
                    <input type="file" accept="image/*" multiple onChange={handleImages} className="hidden" />
                  </label>
                )}
              </div>
              {form.images.length > 0 && (
                <div className="mt-3">
                  <Btn onClick={runAIAnalysis} disabled={analyzing}>
                    {analyzing ? <><Sparkles size={15} className="animate-pulse" /> Đang phân tích...</> : <><Sparkles size={15} /> Phân tích bằng AI & tự điền</>}
                  </Btn>
                  {aiError && <div className="text-xs text-red-400 mt-2">{aiError}</div>}
                </div>
              )}
              {aiResult && (
                <div className="mt-3 p-4 rounded-md" style={{ background: "rgba(168,85,247,0.06)", border: "1px solid rgba(168,85,247,0.25)" }}>
                  <div className="text-xs font-semibold text-purple-300 mb-2 flex items-center gap-1.5"><Sparkles size={13} /> Nhận định AI</div>
                  <div className="text-sm text-slate-200">{aiResult.summary}</div>
                </div>
              )}
            </Card>

            <SectionCard step={7} title="Order Block / FVG — Chấm điểm chất lượng" subtitle={`Ưu tiên vùng đạt từ 7/${OB_SCORE_MAX} điểm trở lên`}>
              <div className="space-y-2 mb-3">
                {OB_SCORE_ITEMS.map((it) => (
                  <StepToggle key={it.id} title={it.label} points={it.points} checked={form.ob[it.id]} onToggle={() => set(["ob", it.id], !form.ob[it.id])} size="small" />
                ))}
              </div>
              <div className="flex items-center gap-3">
                <div className="flex-1 h-2.5 rounded-full" style={{ background: "#161d2a" }}>
                  <div className="h-2.5 rounded-full" style={{ width: `${(obScore / OB_SCORE_MAX) * 100}%`, background: obScore >= 7 ? "#34d399" : "#fbbf24" }} />
                </div>
                <span className="text-sm font-data font-semibold" style={{ color: obScore >= 7 ? "#34d399" : "#fbbf24" }}>{obScore}/{OB_SCORE_MAX}</span>
              </div>
            </SectionCard>

            <SectionCard step={8} title="Premium / Discount" subtitle="Xác định Dealing Range từ High đến Low — 50% là Equilibrium">
              <div className="grid grid-cols-3 gap-2 mb-3">
                <Input placeholder="Đỉnh (High)" value={form.dealingRange.high} onChange={(e) => set(["dealingRange", "high"], e.target.value)} />
                <Input placeholder="Đáy (Low)" value={form.dealingRange.low} onChange={(e) => set(["dealingRange", "low"], e.target.value)} />
                <Input placeholder="Giá hiện tại" value={form.dealingRange.current} onChange={(e) => set(["dealingRange", "current"], e.target.value)} />
              </div>
              {zoneInfo && (
                <div className="flex items-center gap-2">
                  <span className="text-xs font-data px-2 py-1 rounded-full" style={{ background: zoneInfo.zone === "Premium" ? "#f8717122" : zoneInfo.zone === "Discount" ? "#34d39922" : "#94a3b822", color: zoneInfo.zone === "Premium" ? "#f87171" : zoneInfo.zone === "Discount" ? "#34d399" : "#94a3b8" }}>
                    {zoneInfo.zone} ({zoneInfo.pct}%)
                  </span>
                  <span className="text-[11px] text-slate-500">{form.direction === "Long" ? "Ưu tiên Buy tại Discount" : "Ưu tiên Sell tại Premium"}</span>
                </div>
              )}
            </SectionCard>

            <SectionCard step={9} title="SMT Divergence" subtitle="So sánh tài sản/chỉ số tương quan — bằng chứng bổ sung, không phải tín hiệu độc lập">
              <StepToggle title="Có phân kỳ SMT xác nhận" checked={form.smt.confirmed} onToggle={() => set(["smt", "confirmed"], !form.smt.confirmed)} />
              <Input className="mt-2" placeholder="So sánh với (VD: VN30, Bank Index, ngành...)" value={form.smt.note} onChange={(e) => set(["smt", "note"], e.target.value)} />
            </SectionCard>

            <SectionCard step={10} title="Risk Management / Trade Plan" subtitle="SL tại vùng invalidation, TP tại thanh khoản mục tiêu">
              <div className="grid grid-cols-3 gap-2 mb-2">
                <Input placeholder="Entry" value={form.risk.entry} onChange={(e) => set(["risk", "entry"], e.target.value)} />
                <Input placeholder="Stop Loss" value={form.risk.sl} onChange={(e) => set(["risk", "sl"], e.target.value)} />
                <Input placeholder="Take Profit" value={form.risk.tp} onChange={(e) => set(["risk", "tp"], e.target.value)} />
              </div>
              {rr !== null && (
                <span className="text-xs font-data px-2 py-1 rounded-full" style={{ background: rr >= 2 ? "#34d39922" : "#f8717122", color: rr >= 2 ? "#34d399" : "#f87171" }}>
                  R:R = 1:{rr}
                </span>
              )}
            </SectionCard>

            <Card className="p-5 mb-4">
              <div className="flex items-center gap-2 mb-2">
                <ShieldCheck size={16} style={{ color: verdict.color }} />
                <span className="text-sm font-semibold" style={{ color: verdict.color }}>{verdict.label}</span>
              </div>
              <div className="text-xs text-slate-500 mb-4">{verdict.detail}</div>
              <Textarea rows={2} placeholder="Ghi chú thêm về setup này..." value={form.note} onChange={(e) => setForm({ ...form, note: e.target.value })} />
              <div className="flex justify-end mt-3">
                <Btn onClick={save}>{editingId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Lưu vào lịch sử</>}</Btn>
              </div>
            </Card>

            {screener.length === 0 ? (
              <Card className="p-10"><EmptyHint text="Chưa có lượt phân tích nào được lưu. Đi qua đủ 10 bước ở trên rồi lưu lại." /></Card>
            ) : (
              <div className="space-y-2">
                {screener.map((s) => {
                  const v = computeDiakimVerdict({ ...emptyForm, ...s });
                  return (
                    <Card key={s.id} className="p-4 flex flex-col md:flex-row md:items-center gap-3 md:gap-6">
                      {s.images && s.images[0] ? (
                        <img src={s.images[0]} alt={s.symbol} className="w-16 h-16 object-cover rounded border border-slate-700 flex-shrink-0" />
                      ) : (
                        <div className="w-16 h-16 rounded border border-dashed flex items-center justify-center flex-shrink-0" style={{ borderColor: "#263042" }}><ImageOff size={14} className="text-slate-700" /></div>
                      )}
                      <div className="w-24">
                        <div className="font-data font-bold text-amber-400">{s.symbol}</div>
                        <div className="text-[11px] text-slate-600">{s.market} · {s.date}</div>
                      </div>
                      <span className="text-xs font-medium px-2 py-1 rounded-full w-16 text-center flex-shrink-0" style={{ background: s.direction === "Short" ? "#f8717122" : "#34d39922", color: s.direction === "Short" ? "#f87171" : "#34d399" }}>{s.direction}</span>
                      <div className="text-xs font-data text-slate-400 w-16">OB {s.obScore ?? computeOBScore(s.ob || {})}/{OB_SCORE_MAX}</div>
                      <div className="text-xs flex-1" style={{ color: v.color }}>{v.label}</div>
                      <div className="text-xs text-slate-500 flex-1 italic hidden lg:block truncate">{s.note}</div>
                      <div className="flex gap-3 self-start">
                        <button onClick={() => startEdit(s)} className="text-slate-600 hover:text-amber-400"><Pencil size={15} /></button>
                        <button onClick={() => remove(s.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={15} /></button>
                      </div>
                    </Card>
                  );
                })}
              </div>
            )}
          </>
        )}
      </div>
    </div>
  );
}

/* ---------------------------------------------------------
   HIỆU SUẤT GIAO DỊCH
--------------------------------------------------------- */
function computeGroupStats(trades) {
  const closed = trades.filter((t) => t.pl !== "" && t.pl !== undefined && t.pl !== null && !isNaN(Number(t.pl)));
  const wins = closed.filter((t) => Number(t.pl) > 0);
  const losses = closed.filter((t) => Number(t.pl) < 0);
  const totalPL = closed.reduce((s, t) => s + Number(t.pl), 0);
  const winRate = closed.length ? Math.round((wins.length / closed.length) * 100) : 0;
  const avgWin = wins.length ? wins.reduce((s, t) => s + Number(t.pl), 0) / wins.length : 0;
  const avgLoss = losses.length ? losses.reduce((s, t) => s + Number(t.pl), 0) / losses.length : 0;
  const grossWin = wins.reduce((s, t) => s + Number(t.pl), 0);
  const grossLoss = Math.abs(losses.reduce((s, t) => s + Number(t.pl), 0));
  const profitFactor = grossLoss > 0 ? Math.round((grossWin / grossLoss) * 100) / 100 : grossWin > 0 ? Infinity : 0;

  const sorted = [...closed].sort((a, b) => (a.date < b.date ? -1 : 1));
  let cum = 0;
  const equity = sorted.map((t) => {
    cum += Number(t.pl);
    return { date: (t.date || "").slice(5), value: cum };
  });

  const byMonth = {};
  closed.forEach((t) => {
    const m = (t.date || "").slice(0, 7);
    if (!m) return;
    byMonth[m] = (byMonth[m] || 0) + Number(t.pl);
  });
  const monthly = Object.entries(byMonth).sort(([a], [b]) => (a < b ? -1 : 1)).map(([m, v]) => ({ month: m.slice(2), value: v }));

  const best = closed.length ? closed.reduce((a, b) => (Number(a.pl) > Number(b.pl) ? a : b)) : null;
  const worst = closed.length ? closed.reduce((a, b) => (Number(a.pl) < Number(b.pl) ? a : b)) : null;

  // Chuỗi thắng/thua liên tiếp
  let curStreak = 0, curType = null, maxWinStreak = 0, maxLossStreak = 0, run = 0, runType = null;
  sorted.forEach((t) => {
    const type = Number(t.pl) > 0 ? "win" : Number(t.pl) < 0 ? "loss" : null;
    if (type === null) return;
    if (type === runType) run += 1;
    else { run = 1; runType = type; }
    if (type === "win") maxWinStreak = Math.max(maxWinStreak, run);
    else maxLossStreak = Math.max(maxLossStreak, run);
  });
  curType = runType;
  curStreak = run;

  // Hiệu suất theo cảm xúc
  const emoMap = {};
  closed.forEach((t) => {
    const emo = t.emotion || "Không ghi";
    if (!emoMap[emo]) emoMap[emo] = { emotion: emo, count: 0, wins: 0, pl: 0 };
    emoMap[emo].count += 1;
    if (Number(t.pl) > 0) emoMap[emo].wins += 1;
    emoMap[emo].pl += Number(t.pl);
  });
  const byEmotion = Object.values(emoMap)
    .map((e) => ({ ...e, winRate: Math.round((e.wins / e.count) * 100) }))
    .sort((a, b) => b.count - a.count);

  // Hiệu suất theo mã/cặp
  const symMap = {};
  closed.forEach((t) => {
    const sym = t.ticker || t.symbolLabel || "?";
    if (!symMap[sym]) symMap[sym] = { symbol: sym, count: 0, wins: 0, pl: 0 };
    symMap[sym].count += 1;
    if (Number(t.pl) > 0) symMap[sym].wins += 1;
    symMap[sym].pl += Number(t.pl);
  });
  const bySymbol = Object.values(symMap)
    .map((s) => ({ ...s, winRate: Math.round((s.wins / s.count) * 100) }))
    .sort((a, b) => Math.abs(b.pl) - Math.abs(a.pl))
    .slice(0, 6);

  return {
    closedCount: closed.length, winCount: wins.length, lossCount: losses.length, winRate, totalPL, avgWin, avgLoss, profitFactor,
    equity, monthly, best, worst,
    curStreak, curType, maxWinStreak, maxLossStreak,
    byEmotion, bySymbol,
  };
}

function StatMini({ label, value, color, hint }) {
  return (
    <Card className="p-4">
      <div className="text-[11px] text-slate-500 uppercase tracking-wide mb-1.5">{label}</div>
      <div className="font-data text-xl font-semibold" style={{ color: color || "#e2e8f0" }}>{value}</div>
      {hint && <div className="text-[11px] text-slate-600 mt-1">{hint}</div>}
    </Card>
  );
}

function PerformanceGroup({ title, trades, moneyFmt }) {
  const s = computeGroupStats(trades);
  if (s.closedCount === 0) {
    return (
      <Card className="p-8 mb-6">
        <div className="text-sm font-semibold text-slate-300 mb-2">{title}</div>
        <EmptyHint text="Chưa có lệnh nào đã chốt lãi/lỗ để thống kê." />
      </Card>
    );
  }
  return (
    <div className="mb-8">
      <div className="text-sm font-semibold text-slate-200 mb-3 flex items-center gap-2"><Activity size={15} className="text-amber-400" /> {title}</div>
      <div className="grid grid-cols-2 lg:grid-cols-6 gap-3 mb-4">
        <StatMini label="Số lệnh đã chốt" value={s.closedCount} />
        <StatMini label="Tỷ lệ thắng" value={`${s.winRate}%`} hint={`${s.winCount} thắng · ${s.lossCount} thua`} />
        <StatMini label="Tổng lãi/lỗ" value={`${s.totalPL >= 0 ? "+" : ""}${moneyFmt(s.totalPL)}`} color={s.totalPL >= 0 ? "#34d399" : "#f87171"} />
        <StatMini label="Lãi TB / lệnh thắng" value={`+${moneyFmt(s.avgWin)}`} color="#34d399" />
        <StatMini label="Profit Factor" value={s.profitFactor === Infinity ? "∞" : s.profitFactor} color={s.profitFactor >= 1.5 ? "#34d399" : s.profitFactor >= 1 ? "#fbbf24" : "#f87171"} hint="Lãi gộp / Lỗ gộp" />
        <StatMini
          label="Chuỗi hiện tại"
          value={s.curType ? `${s.curStreak} ${s.curType === "win" ? "thắng" : "thua"}` : "—"}
          color={s.curType === "win" ? "#34d399" : s.curType === "loss" ? "#f87171" : undefined}
          hint={`Kỷ lục: ${s.maxWinStreak} thắng · ${s.maxLossStreak} thua liên tiếp`}
        />
      </div>
      {s.curType === "loss" && s.curStreak >= 3 && (
        <div className="flex items-center gap-2 text-xs px-3 py-2 rounded-md mb-4" style={{ background: "#f8717112", border: "1px solid #f8717130", color: "#f87171" }}>
          <AlertTriangle size={13} /> Đang thua {s.curStreak} lệnh liên tiếp — cân nhắc giảm khối lượng hoặc tạm dừng để bình tĩnh lại trước khi vào lệnh tiếp theo.
        </div>
      )}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <Card className="p-4">
          <div className="text-xs text-slate-500 mb-2 font-medium">Đường cong vốn (Equity Curve)</div>
          <ResponsiveContainer width="100%" height={180}>
            <AreaChart data={s.equity}>
              <defs>
                <linearGradient id={`eq-${title}`} x1="0" y1="0" x2="0" y2="1">
                  <stop offset="0%" stopColor="#fbbf24" stopOpacity={0.35} />
                  <stop offset="100%" stopColor="#fbbf24" stopOpacity={0} />
                </linearGradient>
              </defs>
              <CartesianGrid strokeDasharray="3 3" stroke="#1c2432" vertical={false} />
              <XAxis dataKey="date" stroke="#475569" tick={{ fontSize: 10 }} />
              <YAxis stroke="#475569" tick={{ fontSize: 10 }} width={50} />
              <Tooltip contentStyle={{ background: "#0d1119", border: "1px solid #263042", fontSize: 12, borderRadius: 6 }} labelStyle={{ color: "#94a3b8" }} formatter={(v) => moneyFmt(v)} />
              <Area type="monotone" dataKey="value" stroke="#fbbf24" fill={`url(#eq-${title})`} strokeWidth={2} />
            </AreaChart>
          </ResponsiveContainer>
        </Card>
        <Card className="p-4">
          <div className="text-xs text-slate-500 mb-2 font-medium">Lãi/Lỗ theo tháng</div>
          <ResponsiveContainer width="100%" height={180}>
            <BarChart data={s.monthly}>
              <CartesianGrid strokeDasharray="3 3" stroke="#1c2432" vertical={false} />
              <XAxis dataKey="month" stroke="#475569" tick={{ fontSize: 10 }} />
              <YAxis stroke="#475569" tick={{ fontSize: 10 }} width={50} />
              <Tooltip contentStyle={{ background: "#0d1119", border: "1px solid #263042", fontSize: 12, borderRadius: 6 }} labelStyle={{ color: "#94a3b8" }} formatter={(v) => moneyFmt(v)} />
              <Bar dataKey="value" radius={[3, 3, 0, 0]}>
                {s.monthly.map((m, i) => <Cell key={i} fill={m.value >= 0 ? "#34d399" : "#f87171"} />)}
              </Bar>
            </BarChart>
          </ResponsiveContainer>
        </Card>
      </div>
      {(s.best || s.worst) && (
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mt-3">
          {s.best && (
            <div className="text-xs px-3 py-2 rounded-md" style={{ background: "#34d39912", border: "1px solid #34d39930" }}>
              <span className="text-emerald-400 font-medium">Lệnh tốt nhất:</span> <span className="text-slate-300">{s.best.ticker || s.best.symbolLabel} ({s.best.date}) +{moneyFmt(s.best.pl)}</span>
            </div>
          )}
          {s.worst && (
            <div className="text-xs px-3 py-2 rounded-md" style={{ background: "#f8717112", border: "1px solid #f8717130" }}>
              <span className="text-red-400 font-medium">Lệnh tệ nhất:</span> <span className="text-slate-300">{s.worst.ticker || s.worst.symbolLabel} ({s.worst.date}) {moneyFmt(s.worst.pl)}</span>
            </div>
          )}
        </div>
      )}

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 mt-4">
        {s.byEmotion.length > 0 && (
          <Card className="p-4">
            <div className="text-xs text-slate-500 mb-3 font-medium flex items-center gap-1.5"><Brain size={13} /> Hiệu suất theo trạng thái tâm lý</div>
            <div className="space-y-2">
              {s.byEmotion.map((e, i) => (
                <div key={i} className="flex items-center gap-3 text-xs">
                  <span className="w-24 truncate text-slate-300">{e.emotion}</span>
                  <div className="flex-1 h-2 rounded-full" style={{ background: "#161d2a" }}>
                    <div className="h-2 rounded-full" style={{ width: `${e.winRate}%`, background: e.winRate >= 50 ? "#34d399" : "#f87171" }} />
                  </div>
                  <span className="font-data w-10 text-right" style={{ color: e.winRate >= 50 ? "#34d399" : "#f87171" }}>{e.winRate}%</span>
                  <span className="text-slate-600 w-8 text-right">{e.count} lệnh</span>
                </div>
              ))}
            </div>
            <div className="text-[11px] text-slate-600 mt-3">Đối chiếu xem tâm lý nào đang khiến bạn thắng nhiều nhất — và cảnh giác với tâm lý có tỷ lệ thắng thấp.</div>
          </Card>
        )}
        {s.bySymbol.length > 0 && (
          <Card className="p-4">
            <div className="text-xs text-slate-500 mb-3 font-medium">Hiệu suất theo mã / cặp giao dịch nhiều nhất</div>
            <div className="space-y-1.5">
              {s.bySymbol.map((sym, i) => (
                <div key={i} className="flex items-center justify-between text-xs py-1" style={{ borderBottom: i < s.bySymbol.length - 1 ? "1px solid #161d2a" : "none" }}>
                  <span className="font-data font-semibold text-amber-400">{sym.symbol}</span>
                  <span className="text-slate-500">{sym.count} lệnh · {sym.winRate}% thắng</span>
                  <span className="font-data font-medium" style={{ color: sym.pl >= 0 ? "#34d399" : "#f87171" }}>{sym.pl >= 0 ? "+" : ""}{moneyFmt(sym.pl)}</span>
                </div>
              ))}
            </div>
          </Card>
        )}
      </div>
    </div>
  );
}

function PerformanceStats({ journal, journalGold }) {
  return (
    <div>
      <PageHeader title="Hiệu suất giao dịch" sub="Thống kê tỷ lệ thắng, lãi/lỗ và đường cong vốn — tách riêng Chứng khoán và Vàng/Dầu/BTC." />
      <div className="px-6 md:px-10">
        <PerformanceGroup title="Chứng khoán" trades={journal} moneyFmt={fmtBillion} />
        <PerformanceGroup title="Vàng · Dầu · BTC" trades={journalGold} moneyFmt={fmt} />
      </div>
    </div>
  );
}

/* ---------------------------------------------------------
   CRM CHĂM SÓC KHÁCH HÀNG
--------------------------------------------------------- */
const CRM_STATUSES = [
  { id: "Tiềm năng", color: "#94a3b8" },
  { id: "Đang chăm sóc", color: "#fbbf24" },
  { id: "Khách hàng", color: "#34d399" },
  { id: "Không quan tâm", color: "#f87171" },
];
function crmStatusColor(status) {
  return CRM_STATUSES.find((s) => s.id === status)?.color || "#94a3b8";
}
function isOverdue(dateStr) {
  if (!dateStr) return false;
  return dateStr < todayISO();
}
function isDueToday(dateStr) {
  return dateStr === todayISO();
}

function CRM({ crm, setCrm }) {
  const empty = {
    name: "", phone: "", zalo: "", email: "", status: "Tiềm năng",
    source: "", aum: "", lastContact: "", nextFollowUp: "", note: "",
  };
  const [form, setForm] = useState(empty);
  const [editingId, setEditingId] = useState(null);
  const [showForm, setShowForm] = useState(false);
  const [search, setSearch] = useState("");
  const [filterStatus, setFilterStatus] = useState("all");

  function save() {
    if (!form.name.trim()) return;
    if (editingId) {
      setCrm((prev) => prev.map((c) => (c.id === editingId ? { ...form, id: editingId } : c)));
    } else {
      setCrm((prev) => [{ id: Date.now(), ...form }, ...prev]);
    }
    cancelEdit();
  }
  function startEdit(c) {
    setEditingId(c.id);
    setForm({ ...empty, ...c });
    setShowForm(true);
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function cancelEdit() {
    setEditingId(null);
    setForm(empty);
    setShowForm(false);
  }
  function remove(id) {
    setCrm((prev) => prev.filter((c) => c.id !== id));
    if (editingId === id) cancelEdit();
  }

  const stats = useMemo(() => {
    const counts = {};
    CRM_STATUSES.forEach((s) => { counts[s.id] = 0; });
    crm.forEach((c) => { counts[c.status] = (counts[c.status] || 0) + 1; });
    const totalAum = crm.reduce((s, c) => s + (Number(c.aum) || 0), 0);
    return { counts, totalAum };
  }, [crm]);

  const dueSoon = useMemo(() => {
    return crm.filter((c) => c.nextFollowUp && (isOverdue(c.nextFollowUp) || isDueToday(c.nextFollowUp)));
  }, [crm]);

  const filtered = useMemo(() => {
    return crm
      .filter((c) => filterStatus === "all" || c.status === filterStatus)
      .filter((c) => {
        if (!search.trim()) return true;
        const q = search.toLowerCase();
        return (c.name || "").toLowerCase().includes(q) || (c.phone || "").includes(q) || (c.zalo || "").toLowerCase().includes(q);
      })
      .sort((a, b) => (a.nextFollowUp || "9999") < (b.nextFollowUp || "9999") ? -1 : 1);
  }, [crm, search, filterStatus]);

  return (
    <div>
      <PageHeader title="CRM Khách hàng" sub="Quản lý danh sách khách hàng, tiến độ chăm sóc và lịch nhắc liên hệ." />
      <div className="px-6 md:px-10">
        <div className="grid grid-cols-2 lg:grid-cols-5 gap-3 mb-6">
          {CRM_STATUSES.map((s) => (
            <StatCard key={s.id} label={s.id} value={stats.counts[s.id]} valueColor={s.color} />
          ))}
          <StatCard label="Tổng tài sản ước tính" value={fmtBillion(stats.totalAum)} valueColor="#fbbf24" />
        </div>

        {dueSoon.length > 0 && (
          <Card className="p-4 mb-6" style={{ background: "rgba(248,113,113,0.06)", border: "1px solid rgba(248,113,113,0.25)" }}>
            <div className="text-sm font-semibold text-red-400 mb-3 flex items-center gap-2"><BellRing size={15} /> Cần liên hệ hôm nay / đã quá hạn ({dueSoon.length})</div>
            <div className="space-y-1.5">
              {dueSoon.map((c) => (
                <div key={c.id} className="flex items-center justify-between text-sm py-1.5" style={{ borderBottom: "1px solid #2a1418" }}>
                  <div className="flex items-center gap-2">
                    <span className="font-medium text-slate-200">{c.name}</span>
                    <span className="text-xs px-2 py-0.5 rounded-full" style={{ background: `${crmStatusColor(c.status)}22`, color: crmStatusColor(c.status) }}>{c.status}</span>
                  </div>
                  <div className="flex items-center gap-3">
                    <span className="text-xs" style={{ color: isOverdue(c.nextFollowUp) ? "#f87171" : "#fbbf24" }}>
                      {isOverdue(c.nextFollowUp) ? "Quá hạn" : "Hôm nay"}: {c.nextFollowUp}
                    </span>
                    {c.phone && <a href={`tel:${c.phone}`} className="text-slate-400 hover:text-amber-400"><Phone size={14} /></a>}
                  </div>
                </div>
              ))}
            </div>
          </Card>
        )}

        <div className="flex flex-col md:flex-row gap-3 mb-4">
          <div className="flex-1 relative">
            <Input placeholder="Tìm theo tên, SĐT, Zalo..." value={search} onChange={(e) => setSearch(e.target.value)} />
          </div>
          <select value={filterStatus} onChange={(e) => setFilterStatus(e.target.value)} style={inputStyle} className="px-3 py-2 rounded-md text-sm outline-none">
            <option value="all">Tất cả trạng thái</option>
            {CRM_STATUSES.map((s) => <option key={s.id} value={s.id}>{s.id}</option>)}
          </select>
          <Btn onClick={() => (showForm ? cancelEdit() : (setShowForm(true), setEditingId(null), setForm(empty)))}>
            {showForm ? <><X size={15} /> Đóng</> : <><UserPlus size={15} /> Thêm khách hàng</>}
          </Btn>
        </div>

        {showForm && (
          <Card className="p-4 mb-6">
            {editingId && (
              <div className="text-xs text-amber-400 mb-3 flex items-center gap-2">
                <Pencil size={12} /> Đang chỉnh sửa {form.name || "khách hàng"}
              </div>
            )}
            <div className="grid grid-cols-2 md:grid-cols-3 gap-3 mb-3">
              <Input placeholder="Họ tên khách hàng" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} />
              <Input placeholder="Số điện thoại" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} />
              <Input placeholder="Zalo" value={form.zalo} onChange={(e) => setForm({ ...form, zalo: e.target.value })} />
              <Input placeholder="Email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} />
              <select value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value })} style={inputStyle} className="w-full px-3 py-2 rounded-md text-sm outline-none">
                {CRM_STATUSES.map((s) => <option key={s.id}>{s.id}</option>)}
              </select>
              <Input placeholder="Nguồn (giới thiệu, MXH...)" value={form.source} onChange={(e) => setForm({ ...form, source: e.target.value })} />
              <Input placeholder="Tài sản ước tính (VNĐ)" value={form.aum} onChange={(e) => setForm({ ...form, aum: e.target.value })} />
              <Field label="Lần liên hệ gần nhất">
                <Input type="date" value={form.lastContact} onChange={(e) => setForm({ ...form, lastContact: e.target.value })} />
              </Field>
              <Field label="Lịch hẹn liên hệ tiếp theo">
                <Input type="date" value={form.nextFollowUp} onChange={(e) => setForm({ ...form, nextFollowUp: e.target.value })} />
              </Field>
            </div>
            <Textarea rows={2} placeholder="Ghi chú chăm sóc (nhu cầu, khẩu vị rủi ro, lịch sử trao đổi...)" value={form.note} onChange={(e) => setForm({ ...form, note: e.target.value })} />
            <div className="flex justify-end mt-3">
              <Btn onClick={save}>{editingId ? <><Check size={15} /> Lưu</> : <><Plus size={15} /> Thêm khách hàng</>}</Btn>
            </div>
          </Card>
        )}

        {filtered.length === 0 ? (
          <Card className="p-10"><EmptyHint text="Chưa có khách hàng nào khớp. Thêm khách hàng đầu tiên ở trên." /></Card>
        ) : (
          <div className="space-y-2">
            {filtered.map((c) => (
              <Card key={c.id} className="p-4 flex flex-col md:flex-row md:items-center gap-3 md:gap-6">
                <div className="w-40 flex-shrink-0">
                  <div className="font-semibold text-slate-100">{c.name}</div>
                  <span className="text-[11px] px-2 py-0.5 rounded-full inline-block mt-1" style={{ background: `${crmStatusColor(c.status)}22`, color: crmStatusColor(c.status) }}>{c.status}</span>
                </div>
                <div className="flex flex-wrap gap-4 text-xs text-slate-400 flex-1">
                  {c.phone && <a href={`tel:${c.phone}`} className="flex items-center gap-1 hover:text-amber-400"><Phone size={12} /> {c.phone}</a>}
                  {c.zalo && <span className="flex items-center gap-1"><MessageCircle size={12} /> {c.zalo}</span>}
                  {c.aum && <span className="font-data text-slate-300">TS: {fmtBillion(c.aum)}</span>}
                  {c.nextFollowUp && (
                    <span className="flex items-center gap-1" style={{ color: isOverdue(c.nextFollowUp) ? "#f87171" : isDueToday(c.nextFollowUp) ? "#fbbf24" : "#64748b" }}>
                      <Clock size={12} /> Hẹn: {c.nextFollowUp}
                    </span>
                  )}
                </div>
                <div className="text-xs text-slate-500 italic flex-1 hidden lg:block truncate">{c.note}</div>
                <div className="flex gap-3 self-start">
                  <button onClick={() => startEdit(c)} className="text-slate-600 hover:text-amber-400"><Pencil size={15} /></button>
                  <button onClick={() => remove(c.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={15} /></button>
                </div>
              </Card>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}

/* ---------------------------------------------------------
   BIỂU ĐỒ TRADINGVIEW
--------------------------------------------------------- */
function useTradingViewScript() {
  const [loaded, setLoaded] = useState(typeof window !== "undefined" && !!window.TradingView);
  useEffect(() => {
    if (typeof window === "undefined") return;
    if (window.TradingView) { setLoaded(true); return; }
    const existing = document.getElementById("tradingview-widget-script");
    if (existing) {
      existing.addEventListener("load", () => setLoaded(true));
      return;
    }
    const script = document.createElement("script");
    script.id = "tradingview-widget-script";
    script.src = "https://s3.tradingview.com/tv.js";
    script.async = true;
    script.onload = () => setLoaded(true);
    document.body.appendChild(script);
  }, []);
  return loaded;
}

function TradingViewChart({ symbol, interval = "D", height = 560 }) {
  const loaded = useTradingViewScript();
  const containerId = useMemo(() => `tv_chart_${Math.random().toString(36).slice(2)}`, []);

  useEffect(() => {
    if (!loaded || !window.TradingView) return;
    const el = document.getElementById(containerId);
    if (el) el.innerHTML = "";
    try {
      new window.TradingView.widget({
        autosize: true,
        symbol,
        interval,
        timezone: "Asia/Ho_Chi_Minh",
        theme: "dark",
        style: "1",
        locale: "vi_VN",
        toolbar_bg: "#0d1119",
        enable_publishing: false,
        hide_top_toolbar: false,
        hide_legend: false,
        save_image: false,
        container_id: containerId,
      });
    } catch (e) {
      // bỏ qua nếu widget lỗi tải, khung sẽ trống
    }
  }, [loaded, symbol, interval, containerId]);

  return (
    <div style={{ height, background: "#0a0e14" }} className="w-full rounded-lg overflow-hidden relative">
      {!loaded && (
        <div className="absolute inset-0 flex items-center justify-center text-sm text-slate-600">Đang tải biểu đồ...</div>
      )}
      <div id={containerId} style={{ height: "100%", width: "100%" }} />
    </div>
  );
}

const CHART_PRESETS = [
  { label: "VN-Index", symbol: "HOSE:VNINDEX" },
  { label: "VN30", symbol: "HOSE:VN30" },
  { label: "Vàng (XAUUSD)", symbol: "OANDA:XAUUSD" },
  { label: "Dầu WTI", symbol: "TVC:USOIL" },
  { label: "BTC/USDT", symbol: "BINANCE:BTCUSDT" },
  { label: "ETH/USDT", symbol: "BINANCE:ETHUSDT" },
  { label: "EURUSD", symbol: "FX:EURUSD" },
  { label: "DXY", symbol: "TVC:DXY" },
];
const CHART_TIMEFRAMES = [
  { label: "M1", value: "1" }, { label: "M15", value: "15" }, { label: "H1", value: "60" },
  { label: "H4", value: "240" }, { label: "D1", value: "D" }, { label: "W1", value: "W" }, { label: "MN", value: "M" },
];

function ChartView() {
  const [symbol, setSymbol] = useState("HOSE:VNINDEX");
  const [customSymbol, setCustomSymbol] = useState("");
  const [interval, setInterval_] = useState("D");

  return (
    <div>
      <PageHeader title="Biểu đồ TradingView" sub="Xem biểu đồ nến trực tiếp ngay trong hệ thống — chứng khoán VN, vàng, dầu, BTC, forex." />
      <div className="px-6 md:px-10">
        <Card className="p-4 mb-4">
          <div className="flex flex-wrap gap-2 mb-3">
            {CHART_PRESETS.map((p) => (
              <button
                key={p.symbol}
                onClick={() => setSymbol(p.symbol)}
                className="px-3 py-1.5 rounded-md text-xs font-medium"
                style={symbol === p.symbol ? { background: "linear-gradient(90deg,#f59e0b,#fbbf24)", color: "#1a1206" } : { background: "#0a0e14", color: "#94a3b8", border: "1px solid #263042" }}
              >
                {p.label}
              </button>
            ))}
          </div>
          <div className="flex flex-col md:flex-row gap-2 mb-3">
            <Input placeholder="Nhập mã TradingView (VD: HOSE:HPG, BINANCE:ETHUSDT, FX:GBPUSD)" value={customSymbol} onChange={(e) => setCustomSymbol(e.target.value)} />
            <Btn variant="ghost" onClick={() => { if (customSymbol.trim()) setSymbol(customSymbol.trim().toUpperCase()); }}>Xem biểu đồ</Btn>
          </div>
          <div className="flex gap-1.5 flex-wrap">
            {CHART_TIMEFRAMES.map((tf) => (
              <button
                key={tf.value}
                onClick={() => setInterval_(tf.value)}
                className="px-2.5 py-1 rounded text-[11px] font-data font-medium"
                style={interval === tf.value ? { background: "linear-gradient(90deg,#7c3aed,#a855f7)", color: "#fff" } : { background: "#0a0e14", color: "#64748b", border: "1px solid #263042" }}
              >
                {tf.label}
              </button>
            ))}
          </div>
        </Card>

        <Card className="p-2 overflow-hidden">
          <TradingViewChart symbol={symbol} interval={interval} height={560} />
        </Card>

        <div className="text-[11px] text-slate-600 mt-3">
          Mã cần đúng định dạng SÀN:MÃ theo TradingView — VD: <span className="font-data">HOSE:HPG</span> (chứng khoán VN), <span className="font-data">OANDA:XAUUSD</span> (vàng), <span className="font-data">BINANCE:BTCUSDT</span> (crypto), <span className="font-data">FX:EURUSD</span> (forex). Nếu không hiện biểu đồ, thử đổi sàn (HOSE/HNX/UPCOM) hoặc kiểm tra đúng chính tả mã.
        </div>
      </div>
    </div>
  );
}
